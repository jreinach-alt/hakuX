# lane.doa413 -- #413: Dead or Alive Ultimate: 16 fps fights and a 0-2 fps stage transition on the Nova

Issue: #413. Base: origin/master @ dc38b745b8.
Files: docs/lanes/doa413/**, docs/testing/predictions/doa413-*.json. Code files are NOT granted yet: name the site in
       NOTES.md and on the issue and the board grants it if free.
Needs device: yes (Nova soak of the same route). Needs NDK: no.
Read first: the issue body (run 0-0-y-1790433159-titleplay-p1-doa1u in $DISPATCH_DIR/results; the game that boots is
DOA2 Ultimate), docs/lanes/fps382/NOTES.md and docs/lanes/blinx372c/NOTES.md for the method.

## What is settled
Story mode, Kasumi's first fight. Fight: 12.6 fps median over 191 s, 0.0 of the time at >= 30. fps per 30 s:
`48 52 18 18 26 16 16 16 16 2 0 2 0 8 6`. After a ring-out at ~08:10 the scene barely advances: the round timer reads
79.85, 79.05, 78.75, 78.46 across 081020..081141, 80 s of wall time for 1.4 s of game time. Hang gaps (no 60 flips) 11.3 s,
76.3 s, 44.4 s; worst frame stall 16.9 s. No crash, no audio starvation. Two symptoms, maybe two causes.

## The job
1. Split the two: (a) the steady 16 fps fight, (b) the 0-2 fps span after the stage change. For (b) read the logcat around
   081020 for what the renderer, the shader compile path and the surface path were doing in the 76 s gap: a pipeline or
   shader cache miss storm, a synchronous download loop, or a guest-side spin are different fixes. Say which from a counter,
   not from the picture.
2. For (a) use the same per-frame split as the aufire412 brief (vCPU busy, renderer busy, `Sub` vs `Fen`, slow stores).
3. A hang with no crash is the more valuable find: if (b) reproduces, get the counter on the second occurrence, and check
   whether it is present on a desktop xemu run of the same scene (a host-independent stall is the guest's).

## Falsifier and arm
Mover: the post-ring-out window from 0-2 fps to the fight's own rate, and the fight from 16 toward 30, on a same-session
Nova A/B. Must-not-move: Depth buffer fixed function, Color_zeta_overlap, Surface_format, PR #387's arm. Name what would
move each; check `status`. Do not touch vk/surface.c while PR #396 (lane.blinx372d) holds it.

## Done when
NOTES.md holds the two symptoms separated, the counter behind each, the priced hunk or why none exists; a grant request
on #413 if a code file is needed; prediction registered after the last rebase; before marking ready merge master and re-run.

# #303: Spikeout FMV decodes with green block corruption -- find the write the overlay/YUV path does not see

Lane: fmv303            Issue: #303 (game-visible; from #265)
Base: origin/master @ 248f1312c7
Files: hw/xbox/nv2a/pgraph/vk/display.c, docs/lanes/fmv303/**, docs/testing/predictions/fmv303-*.json
       (vk/texture.c, gl/texture.c, pgraph/texture.c are lane.texvol283's: name a hunk there, do not edit)
Needs device: yes for the arm (Nova is held by lane.gamecheck until 21:22 UTC; Thor is free). Needs NDK: yes.
Prediction: yes, before the arm.

## Goal
The Sofdec/ADX FMV in Spikeout (attract loop ~22 s and ~40 s after title) is covered in ~32 px wide green
columns over 30-90% of the frame. Solid green is Y=U=V=0 through the YUV->RGB path: a video frame sampled
before its data was written. The pattern differs between two passes over the same shot, so suspect a race or a
missed dirty/upload, in the family of #262 and #184. The 0.3.1 review reported the same on the USA disc.

## Read first
`docs/investigations/yuv-conversion.md`, #265's comment thread, the frames under
`/home/justin/hakux-work/gamecheck/spikeout-soak/` (f00028 clearest, f00054/f00058 clean for contrast), and
the display/overlay path in `vk/display.c` (PVIDEO vs texture-as-YUV: establish which one the FMV uses
BEFORE anything else -- one grep of the registers the game writes settles it).

## Falsifier
Name the path the FMV takes (PVIDEO overlay, or a UYVY/YUY2 texture). Then show, per frame, whether the
source memory was written before the sample: log the surface/overlay dirty state and a checksum of the source
at upload vs at the guest's last write. A candidate fix is scored by frame regions (green-block area fraction
per frame over the 80-frame soak, not point samples) on the same soak spec, both devices, and must-not-move:
the title screen and logos (clean today), Spikeout's in-game frames, and the pgraph suites that touch
`vk/display.c`. If the race is real, two runs of the unfixed build must disagree per frame; if they do not,
say so and stop -- that is a complete outcome.

## Do not
Edit texture.c files (texvol283's), trigger CI as a self-check, or hold a device while another request runs.

## Done when
NOTES name the FMV's path and the missing write, with counts from the frames; a fix PR with a registered
prediction and an arm verdict is marked ready, or the hunk that is not yours is named on #303 for a grant.

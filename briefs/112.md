# #112 -- xbox-hardware experiment roadmap: register predictions (cloud, no device)

Base: 5d573d21cb4fd59fda13ca6bc247c59519aeca4e (origin/master).
Files: docs/testing/predictions/ (new files only), docs/investigations/ (new
files only). No hw/xbox/ edits -- these are all pre-registration work ahead
of hardware that does not exist yet.
Issue: #112 (and #49, #41, #109, #110, #111 read-only for context).

GOAL: #112 orders five experiments; #109-111 already have their own cloud
briefs. Work the other two now:

1. Item 1, the viewport 9/16 boundary sweep: register BOTH models' verdicts
   -- truncation (current model, `trunc(pos*16.0)/16.0`) and the 9/16
   threshold hypothesis from #49 -- for the two 9/16-congruent captures named
   in #112 AND for a set of non-boundary captures that both models must
   agree on. Read docs/investigations/viewport-9-16-boundary.md first.
2. Item 2, radial fog carryover (#41): register the fog band position each
   model (scene-state carryover vs. hardware constant) predicts for a VS
   RADIAL scene drawn after a different fixed-function scene.

FALSIFIER for each: state which side of the boundary/which band position
each model predicts, concretely enough that a single capture distinguishes
them -- not a range that both models could satisfy.

DONE WHEN: two prediction files exist (one per item above), each naming
must-move and must-not-move captures for both competing models, with a
one-paragraph docs/investigations/ note per item explaining the derivation.
No device time, no code change, no PR.

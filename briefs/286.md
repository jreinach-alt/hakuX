# #286: 3D primitive smoothing -- what the 605,744 px beyond silicon's own smoothed/unsmoothed difference actually is

Issue: #286 (residual after #36's "unmodellable" close on 09-13)      Class: analysis, desktop only
Base: origin/master tip when you start (rebase before you cite anything).
Files: docs/lanes/cloud-286/** only. No hw/ file, no prediction. Needs device: no. Needs NDK: no.

## What is known
120 `-ls`/`-ps` capture variants of the 3D primitive smoothing tests: 605,744 px structural,
2,104,804 one-step, 0/120 exact. 52 of them are WHITE-CONTENT captures holding 367,248 px.
The 09-12 investigation measured silicon's own smoothed-vs-unsmoothed difference as 175,704 px --
the ceiling any smoothing emulation could recover. So >= 430k px is NOT smoothing and is unowned.

## The job
1. Re-derive the 175,704 ceiling from the captures yourself (date the console set:
   hardware/runs/2026-09-19-calib; date ours; a premise from an old NOTES table is a claim).
2. Take the 52 white-content captures FIRST (367,248 px). For each: is ours wrong in a region that
   is not the smoothed edge? Name the class (interior fill, wrong colour, missing prim, texture,
   depth) with a region-not-pixel measurement, and give each class a px count.
3. If a class matches an existing owner (#272 depth, #223 W_param, #315 BRDF, #282 ties, #285),
   say which and stop; do not re-derive it. Otherwise name the first place our path differs.
4. State what is left that only smoothing emulation could touch, and whether it is worth a lane.

## Falsifier
The claim "the rest is not smoothing" is falsified if the console's `-ls` capture differs from its
own unsmoothed twin by more than the 175,704 px in the same regions where ours differs from golden.
Say what a hit would look like BEFORE you run it.

## Done when
docs/lanes/cloud-286/NOTES.md holds the re-derived ceiling, the per-class table over the 52 (and then
the other 68) captures with px counts and owners, and a recommendation (no lane / a named lane).
Analysis with a named owner per class is a complete outcome. Ready PR; no hw/ edit.

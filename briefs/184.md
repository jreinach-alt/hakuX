# #184 -- confirm or refute the reused-binding hypothesis, analysis only

Issue: #184 (Vulkan: Clear suite surface-format captures worse than GL)
Base: master @ 4c28b2d352c7d8d371a8aa187911cef3254cde8c
Files: [] -- this is a reading, not a fix. hw/xbox/nv2a/pgraph/vk/draw.c and
       vk/surface.c, the candidate fix site, are held by lane.clrsurf91's
       open PR #148 (territory.toml); do not touch either file.

## What's already done

Read nv2a_issues.toml's issue.184 entry in full first. The filer's own
hypothesis (not instrumented, not claimed): `TestSurfaceFmt` binds
`GetTextureMemoryForStage(0)` once for six clears/samples, and a format-only
change between them does not refresh `surface->shape` on the reused
binding -- the same shape as #60's GL-side finding (folded as `47d8822c`).
The candidate fix site is already commented in vk/draw.c around line
505-525, written against #158's DstAlpha_ARGB8 case.

## Goal

Confirm or refute the reused-binding hypothesis for the four Vulkan-worse
`Clear::SCF_*` captures WITHOUT editing vk/draw.c or vk/surface.c. Read the
relevant code paths (surface->shape's write sites, when it is refreshed
relative to a clear+sample pair) and, if a device is available, use the
existing instruments (`docs/testing/x1a7_clear_bytes.py`, or any per-capture
scorer against `/tmp/goldens/results/Clear/`) to check whether the four
distinct golden swatch colours collapsing to one (`bacada`, swatch 1's) is
consistent with a format read that lags one clear behind the format it
should show.

## Falsifier

If the hypothesis holds, the wrong colour on each of the four captures
should trace to the PRECEDING format's clear/sample pair in submission
order (i.e. capture N shows what capture N-1 should have shown, or a fixed
earlier one) rather than to an unrelated candidate. If the pattern does not
fit a one-behind (or fixed-stale) read, say so plainly -- that refutes the
hypothesis and is exactly as useful as confirming it.

## Done when

A finding -- confirmed, refuted, or inconclusive with what would settle it
-- is posted to #184, and nv2a_issues.toml's issue.184 status_note is left
for the board to update.

## Out of scope

No edits to vk/draw.c or vk/surface.c (lane.clrsurf91's, PR #148 open). No
device fix attempt. The issue's own "should Clear be scored at all" question
is for the board/owner, not this brief.

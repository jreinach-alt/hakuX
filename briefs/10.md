# #10 -- HILO dot mapping: characterize the Y16 class (cloud, no device)

Base: 2e1c5790aa (origin/master).
Files: none -- analysis only. Do NOT touch glsl/psh.c (held by lane.remote,
see #138, open decision-needed) and do not re-open the YUV decode in
hw/xbox/nv2a/pgraph/texture.c -- it is measured bit-exact and explicitly set
aside by docs/investigations/bump-yuv-source.md. This brief claims neither.
Issue: #10.

GOAL: The R16B16 dot-mapping class is fixed and folded. Two classes remain:
YUV (445,984 px, already dead-ended and set aside per the doc above) and Y16
(44,748-44,832 px across BumpMap_Y16_L/_Y16, called out in that same doc as
"a separate and much larger problem" and left untouched). This brief is Y16
only -- do not re-litigate YUV's raw_yuv false start.

Characterize the Y16 mechanism the way bump-yuv-source.md characterized YUV:
read texture.c:83-95's Y16 format table entries, run
docs/testing/bump16_oracle.py against the existing BumpMap_Y16_L/_Y16
captures and goldens, and locate which stage diverges -- texture decode,
the HILO dot-mapping read, or the bump-combiner channel assignment. Reuse
that doc's method (checker-cell colour crossmatch, sub-pixel edge-shift
measurement against golden colours) rather than assuming Y16 shares YUV's
cause; the doc says explicitly it does not ("Y16 is not this defect").

FALSIFIER: name the stage and the specific byte/channel misread, then check
it against BOTH BumpMap_Y16_L and _Y16 -- a rule that explains only one of
the two is not the mechanism.

DONE WHEN: docs/investigations/ has a write-up naming the Y16 mechanism with
a quantified check against both captures, or a measured dead end in
bump-yuv-source.md's own style (what was tried, what it moved, why it's
wrong). No code change unless the fix site is unambiguous; if so, name it
and stop rather than implementing blind, so a device run can verify it.

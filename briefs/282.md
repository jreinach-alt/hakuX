# #282: derive the nearest-sample tie-break rule silicon uses (lighting and bump suites)

Issue: #282 (accuracy). Base: origin/master @ 8e683b3a26. Cloud-class: desktop analysis over captures on disk.
Files: docs/lanes/cloud-282/** (analysis only; glsl/psh.c is held by other lanes -- name the hunk, do not edit)
Needs device: no. Needs NDK: no.

## Goal
Derive a RULE for how silicon breaks nearest-sample ties on texel boundaries. #9 recorded: down below texel
128, up from 144. The differing pixels sit on the checkerboard's periodic rows (179,074 px over 108 captures)
and on bump texel boundaries (155,871 px over 74 captures; #38 comment 5653340516).

## Falsifier
A candidate rule (a function of the interpolated coordinate's fraction and the texel index) is scored on EVERY
capture in (a) and (b) as regions, not points; it must beat the current tie behaviour and hold on a control
class (captures exact today). If no simple rule explains more than half the tie pixels, say the residual is
the interpolator and stop: that is a complete outcome.

## Done when
A NOTES section states the rule with per-suite numbers and a control leg, or a measured negative, and the
psh.c hunk (if any) is named for the board to grant.

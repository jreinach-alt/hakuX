# #297: 13 near-exact captures tagged blank have unowned residuals

Issue: #297 (harness). Base: origin/master @ 8e683b3a26. Cloud-class: desktop analysis over captures on disk.
Files: docs/lanes/cloud-297/** (score_sweep.py is lane.toolsmith's: report the change, do not edit)
Needs device: no. Needs NDK: no.

## Goal
Depth_buffer_fixed_function z24_C{n,y}_FZy_M* (10 x 24 px) and Texture_BRDF BRDF_e0_l0, BRDF_e0_l1, BRDF_e1_l0
(3 x 614 px) are tagged `blank` because the golden is >= 99% one colour (score_sweep.py:229-230), so the small
real residual is never triaged.
1. Locate the residual pixels in each capture and say what they are (edge, glyph, depth step).
2. Decide whether the DBFF rows share #272's 24-bit mechanism (ours a few units high near maximum depth).
3. Propose the scorer's threshold fix (keyed on the capture, not the golden alone).

## Falsifier
If the 24 px are the same pixels in all 10 DBFF captures and sit at maximum-depth pixels, they belong to #272:
say so with the pixel list. Texture_BRDF has no tracker row: propose one.

## Done when
A NOTES file names each residual's owner (an existing issue or a new one) and the scorer change to request.

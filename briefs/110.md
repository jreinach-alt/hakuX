# #110 -- PVIDEO overlay size/pitch limits (cloud, no device)

Base: 5d573d21cb4fd59fda13ca6bc247c59519aeca4e (origin/master).
Files: docs/testing/predictions/ (new file only), docs/investigations/ (new
file only). Do not touch hw/xbox/nv2a/pvideo.c -- there is no capture path
yet, so a code change here cannot be judged.
Issue: #110.

GOAL: read pvideo.c (77 lines, register stubs only) and #110 in full. There
is no PVIDEO golden in this harness at all, so the deliverable is the
REGISTERED EXPECTATION a hardware run would need: what overlay size and
pitch a small nxdk program should program (at, then past, plausible limits),
and what clipping behaviour each candidate model (hard clamp, silent wrap,
disabled overlay) predicts for each case. Write it as a prediction file
alongside a short docs/investigations/ note explaining the candidate models
and why they're the ones worth distinguishing.

FALSIFIER: name which of the candidate models the eventual capture would
need to show to be confirmed, and which captures would need to agree across
ALL candidates (i.e. carry no information) so the eventual hardware run
knows which programmed sizes are worth spending capture time on and which
aren't.

DONE WHEN: a prediction file exists naming the overlay programs to run and
the clipping behaviour each candidate model predicts for each, with the
investigation note. No device time, no code change. Report where the file
lives.

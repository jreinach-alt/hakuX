# #280: R12 must read back the original oPos after a paired MAC+ILU write

Lane: vshr12280            Issue: #280 (component vsh; Vertex shader independence / Multioutput)
Base: origin/master @ d92ae5d7f3 (rebase to the tip before you register anything).
Files: hw/xbox/nv2a/pgraph/glsl/vsh-prog.c, docs/testing/predictions/vshr12280-*.json,
docs/lanes/vshr12280/**
Needs device: yes for the arm (Nova or Thor); the analysis half is desktop. Needs NDK: no.

## The defect
`Multioutput` (Vertex shader independence) draws half its geometry wrong: 32,768 px, status
white-content, 0 exact. The test issues a paired MAC+ILU write to the same output and
requires R12 to read back the ORIGINAL oPos, not the second write. The issue's guess is
vsh-prog.c:580 `#define R12 oPos`. That is a guess: read the program emitter and the
nxdk_pgraph_tests source for Multioutput first, and name which write the console's answer
comes from (MAC result vs ILU result vs their order). XDK-compiled vertex shaders emit this
pattern, so it is game-visible (medium-high).

## Read first; the location is a claim
Derive from the test's own program (its source in the nxdk_pgraph_tests tree, not a summary):
list each instruction, its MAC/ILU destinations and which register aliases oPos. Then trace how
vsh-prog.c lowers R12/oPos aliasing. Do not patch to make the capture match; the golden
is silicon (console set K under hardware/runs/2026-09-19-calib agrees where it covers it).
vsh.c and vsh-ff.c are NOT yours (vsh-ff.c is lane.shadetie224b's).

## The arm (register BEFORE building, after the last rebase)
- must_move: Vertex shader independence Multioutput -> exact, or state the residual and why.
- must_not_move: every other Vertex shader independence capture, and the vsh suites that share
  the emitter (`nv2a_index.py blast hw/xbox/nv2a/pgraph/glsl/vsh-prog.c`): ILU/MAC tests,
  Exceptional Float rows (lane.vshsubneg255 works pgraph.c; do not touch it).
- Failing world: a fix that makes R12 always read the first write but breaks a program that
  legitimately reads the second; register one leg per shape you find in the blast set.
- Check scores1.tsv `status` for `unreadable` and the run log for PARTIAL COVERAGE.

## Done when
Multioutput matches silicon (or the residual is measured and explained), every must-not-move
leg holds, docs/lanes/vshr12280/NOTES.md names the mechanism and what you did not chase, and
the PR is ready with the arm verdict.

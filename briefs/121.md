# #121 -- a structural arms skip is invisible to the lane it blocks (cloud, no device)

Base: 05e25ca29b (origin/master).
Files: docs/testing/jobs/arms.sh, docs/testing/jobs/selftest.sh.
Issue: #121.

GOAL: `arms.sh`'s `refused()` (line 187) posts a `request.sh` refusal to
the lane's PR, but a STRUCTURAL skip -- no a_ref/b_ref, a_ref == b_ref, a
ref that does not resolve, a b_ref not an ancestor of any live tip, a soak,
no suite with goldens -- goes through `skip()` (line 197) directly and is
recorded only in `$A/skipped/<sha>` on the host. Give `skip()`'s
structural-reason call sites (search arms.sh for calls to `skip` that are
NOT inside `refused()`) the same one-comment-per-sha posting `refused()`
already has, reusing the marker file so a repeat run does not double-post.
Post to the PR when one exists, else the issue (same `post()`/`pr_for()`
helpers `refused()` uses).

Care needed: do NOT post for the 38 predictions behind the watermark
(`history=$((history+1))`, no marker written) -- those are history, not
refusals, and must stay silent.

FALSIFIER: a prediction with a malformed a_ref (like the one measured
2026-09-19T04:29:32Z on lane/cloud-109's PR #115: `a_ref 4129a349e6 with
xemu.toml [display] renderer = OpenGL does not resolve`) must produce
exactly one comment on PR #115 naming that reason; a second arms.sh run
must not double-post; a watermark-history prediction must still produce
zero comments.

DONE WHEN: a structural skip reaches the lane's PR (or issue) exactly
once, carrying the marker's own reason text; a selftest case added to
`docs/testing/jobs/selftest.sh` that fails against the current arms.sh and
passes after; watermark history still silent. Needs no device -- open a
draft PR from lane/cloud-121 per roles/lane.md.

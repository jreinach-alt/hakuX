# lane.x1a7271 -- #271: land the X1A7 read-side rule (psh.c, psh.h, vk/texture.c) and register its arm

Issue: #271. Base: origin/master @ a5b5b628f2. Continues lane.cloud-271 (PR #365, merged): read docs/lanes/cloud-271/NOTES.md first; the diff is docs/lanes/cloud-271/x1a7-read-side.diff.
Files: hw/xbox/nv2a/pgraph/glsl/psh.c, hw/xbox/nv2a/pgraph/glsl/psh.h, hw/xbox/nv2a/pgraph/vk/texture.c,
       docs/lanes/x1a7271/**, docs/testing/predictions/x1a7271-*.json.
       vk/draw.c is lane.remote's and the read side needs nothing there.
Ready PRs on the same files: #393 (lane.pshaniso284: psh.c, vk/texture.c, released at ready) and #379 (lane.tiecode282: psh.c, released at ready).
       Before marking your own PR ready, merge master (or those PRs' branches if they have not folded) and re-run your arm.
Needs device: yes for the arm (Thor or Nova; the pricing is done). Needs NDK: no.

## What is already known (NOTES s1-s3, do not re-derive)
The X1A7 alpha rule is inverted from the goldens (0 of 32 swatches break it) and priced on the two newest device runs: the four
Blend_surface DstAlpha_XA_{Z,O}1A7RGB8 / 1-DstAlpha captures go 65,536/81,920 -> 16,384 (the residue is the bg 0x80 halves, 128 vs 129).
The diff applies on master without fuzz and merges cleanly with the psh.c / psh.h that #347 landed. It adds one gate beyond #306:
X8R8G8B8 texture formats are excluded so they keep alpha 1.0.

## The job
1. Apply the diff on a fresh branch off master, build, and check every symbol it uses exists (NOTES s3 lists them).
2. Register the NOTES s4 arm AFTER the last rebase, on concrete refs: must_move the four Blend_surface rows and the two Clear/SCF_X1A7R8G8B8 rows;
   must_not_move the exact rows and name the change that would move each (s4 lists them). Correct the brief's premise as NOTES does:
   Fmt_X1R5G5B5_{Z1,O1}R5G5B5 are "equal to base", not 0.
3. Fmt_X1A7R8G8B8_O1A7R8G8B8 (16,383 today) is a declared regression of at least +58,754 without a per-surface "bytes came from the CPU" gate.
   Do not land it unregistered: either register it as a failing leg, or run NOTES s5's probe (log per X1A7 bind whether the source surface was
   uploaded since its last full clear) and price upload_pending as the gate. If the gate lands, the regression leg becomes must_not_move.
   If the net is a loss on the golden disc, stop and say so on #271: the owner accepts regressions, not the lane.

## Falsifier
The four Blend_surface rows stay at base: the mode never reaches the stage (drawn format not 0x06/0x07 at bind, or upload_pending true).
Z fixed but O structural: a wrong (X<<7) term. A non-X1A7 exact row moves: a stale per-stage mode survives, so reset disabled slots in pgraph_vk_bind_textures().
Check `status` for unreadable and PARTIAL COVERAGE on every row before believing a number.

## Done when
The diff is landed, the arm registered after the last rebase and its verdict read, the declared regression either gated or accepted by the owner
in a linked issue, NOTES.md written, master merged and the arm re-run, PR ready.

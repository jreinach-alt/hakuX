# #279: DOT_ZW draws the whole quad one wrong colour -- derive the rule from the capture and the psh.c source

Issue: #279 (accuracy). Base: origin/master @ 248f1312c7. Cloud-class: desktop analysis, captures on disk.
Files: docs/lanes/cloud-279/** (analysis only; glsl/psh.c is held by other lanes -- name the hunk, do not edit)
Needs device: no. Needs NDK: no.

## Goal
`DotZW` (65,536 px): silicon renders a texture-mode DOT_ZW result across the quad; we draw the whole quad one
flat wrong colour. Read the test's source (nxdk_pgraph_tests, DotZW), what the texture stages are given, and
psh.c's DOT_ZW handling (issue guess: lines 1126, 1161, 1935, 3141). Derive what silicon computes: which
stage's coordinates feed the z/w dot product, and what the result maps to. The silicon golden is on disk; read
its colours as REGIONS (a gradient vs a flat colour is the discriminator).

## Falsifier
Compute the per-pixel value your rule predicts and compare against the golden as regions (mean per 8x8 block),
plus a control leg: another dot-product mode that is exact today must come out unchanged under your reading.
If the golden is flat too, the defect is a wrong constant, not a missing gradient -- say which.

## Done when
A NOTES section gives the rule and the numbers, and names the psh.c hunk for a grant; or a measured negative.

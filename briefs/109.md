# #109 -- undersized surface pitch, a lead on #87 (cloud, no device)

Base: 5d573d21cb4fd59fda13ca6bc247c59519aeca4e (origin/master).
Files: docs/testing/predictions/ (new file only), docs/investigations/ (new
file only). Do not touch hw/xbox/nv2a/ -- state there is confirmed absent
and there is nothing to fix yet.
Issue: #109.

GOAL: this cannot run until real hardware exists, but the prediction can be
written now, per the issue text. Read #109 in full and #87's residual
(Surface_pitch::Swizzle, 10,240px, docs/testing/swizzle_morton_fit.py). Write
a prediction file (ab_compare.py's --register schema; see an existing
hardware-pending entry for shape if one exists) stating: for a surface drawn
with pitch deliberately undersized relative to width, what pixel pattern an
undersized-pitch rule predicts on real hardware, and what the current
swizzle layout predicts instead -- both against the Morton map. Name the
must-move and must-not-move captures now, before any disc boots.

FALSIFIER: if the undersized-pitch hypothesis is right, the predicted
hardware pattern and the Morton-map prediction diverge on #87's known-wrong
quads and AGREE on quads #87 already gets right. If they diverge everywhere
or agree everywhere, the hypothesis explains nothing beyond #87 and should
say so rather than being tuned to fit.

DONE WHEN: a prediction file exists naming both models' pixel predictions
for the undersized-pitch capture, cites #87 and #109, and is registered
bound (no measurement yet, since there is no device) with a one-paragraph
note in docs/investigations/ on how it was derived. Report where the file
lives; do not open a PR for a prediction with no code change.

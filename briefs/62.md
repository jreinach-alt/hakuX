# Brief: #62 -- gl/surface.c findings, the part still live (cloud)

Issue: #62 (GL renderer: five findings from a second pass over gl/surface.c)
Base: origin/master 3fe18366cd
Files: hw/xbox/nv2a/pgraph/gl/surface.c, docs/lanes/cloud-62/NOTES.md
(gl/* was lane.remote's and is free since territory wave 146; check
territory.toml on origin/board before you push -- do not edit it yourself.)

Status of the five, read the issue's status_note first:
- 1 and 3 are FIXED in master (013181dfef, gl/surface.c:561 and :1368-1390).
- 2 was WITHDRAWN by the lane: its fix sat behind the unreachable
  is_converted() gate (pgraph/texture.c:143), so R5G6B5 never reaches the slow path.
- 4 is LIVE: compare_surfaces() at gl/surface.c:2712-2739 has no
  shape.color_format in its DO_CMP list, so an eviction caused by a colour-format
  change prints nothing. Commit 6b78d91f24 fixes it but exists only on
  claude/docs-tooling-agentic-coding-u152m1 (unfolded); re-derive it on master, do
  not cherry-pick blindly.
- 5: read the issue body's item 5 and say which of the remaining nits still applies.

Goal: land finding 4 (a diagnostic-only change) and record, per remaining
finding, fixed / withdrawn / still-live with the line that shows it.

Falsifier: the DO_CMP list still lacks color_format after your change, or the
change alters any non-diagnostic behaviour (surface binding decisions, uploads).
Show by reading that compare_surfaces()'s result is only consumed by the log.

Needs no device and no NDK: a desktop reading plus a build check.

Done when: a PR from your branch has the DO_CMP change, NOTES.md with the
five-row table, and CI green; issue #62 is left open only if item 5 is live.

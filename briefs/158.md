# #158 -- GL-side pad-bit surface fix: confirm the patch shape (cloud, no device)

Base: 4eb641e777 (origin/master).
Files: none -- analysis only. Do NOT edit glsl/psh.c (held by lane.remote,
see #138, open decision-needed) or vk/draw.c (held by lane.clrsurf91's open
PR #148). This brief claims neither.
Issue: #158.

GOAL: GL has diverged from Vulkan by 413,790 px in eight days, all in the
surface pad-bit family. The tracker already names the mechanism: #59's
pad-alpha work (77bd2977) landed on vk/draw.c only; the readback override is
a Vulkan-only column (kelvin_surface_color_format_vk_map's sampled_pad_alpha,
vk/constants.h:441/475 -- gl/renderer.h:361 says GL has no host_fmt); and the
GL write side never sets dualSrcBlend (glsl/psh.c:225), so GL still emits
its original GLSL. Read those three sites and the #59/#60 history and write
up the EXACT GL-side patch shape: what dualSrcBlend gate glsl/psh.c needs,
whether GL's pad-alpha equivalent needs a new constants-table column
(gl/constants.h) alongside the Vulkan one, and whether gl/renderer.h's
missing host_fmt is itself the blocking gap or a separate one. Cite line
numbers and existing sibling code (the Vulkan version of each site) rather
than proposing new mechanism.

FALSIFIER: name a concrete GLSL/constants diff shape and check it against
the 11 iso_surf1 captures' pad-bit format list (X_O/X_Z/X1R5G5B5_{Z,O}/
X8R8G8B8_{Z,O}/X1A7R8G8B8_O) -- if the proposed gate does not cover all of
them, or covers a format that isn't in the pad-bit family, that is a wrong
shape, not a confirmed one.

DONE WHEN: docs/investigations/ has a write-up naming the exact GL-side
edit(s) (file, function, and what changes) for both glsl/psh.c and the
gl-side readback override, so that whichever of #138/#148 clears first, the
real fix is a known-shape dispatch rather than new investigation. No code
change, no PR, no device time.

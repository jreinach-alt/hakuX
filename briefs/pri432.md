# lane.pri432 -- #432: rank 0.5 issues first in dispatch order, and a 0.5 panel on the status page

Issue: #432. Base: origin/master @ 7e6a4ac88a. Release tracker: #433 (0.5, target 2026-09-28).
Files: docs/testing/jobs/board.sh, docs/testing/jobs/status.sh, docs/testing/jobs/status_html.py, docs/testing/jobs/selftest.d/97-board-priority.sh,
       docs/testing/jobs/roles/board.md, docs/lanes/pri432/**. request.sh is lane.pilotgate's (PR #420 in remediation): do NOT edit it.
Needs device: no. Needs NDK: no. Harness only; touches no hw/, target/, accel/ or android/ file, so the PR needs CI green and no audit.
Read first: the issue body; the sort comment at board.sh ~92-112 and the code under it; selftest.d/97-board-priority.sh; status.sh's release
block (~747: release_name, release_gate) and how status_html.py renders it.

## The goal
1. Dispatch order: an open issue labelled `0.5` sorts ahead of every issue that is not, keeping the existing key (game_visible, then impact,
   then unestimated, then measured zero) inside each group; the printed key gets a `[0.5]` tag. The issue's labels come from the same gh list the
   sort already reads. Update roles/board.md's order text to say so (one paragraph, no other role edit).
2. Device-run queue priority for `0.5` issues: find where the queue is ordered (dispatch dir, request.sh/arms). request.sh is not yours: write the
   site and the one-line change into docs/lanes/pri432/NOTES.md and a board request; do not edit it.
3. Status page: a 0.5 panel showing titles copied, tested and Playable against the target (from the #397/#433 tables and the console inventory
   files the status job already reads; if a count has no machine-readable source, show the row as "no source" and name the file that would carry
   it), the Ghoulies gate (median gfps over 90-240 s vs 25, both handhelds, from the latest soak the job can find), and the lane on each 0.5 issue.

## Falsifier and arm
No device arm. selftest.d/97-board-priority.sh gains legs: a `0.5` issue with a measured zero sorts before a game-visible non-0.5 issue; two 0.5
issues keep the old order between them; an unreadable label list leaves the old order and prints it. Each new leg must fail on the pre-change
board.sh (run it against the old file and say so). Must-not-move: every existing 97-board-priority leg, and the jobs selftest as a whole.

## Done when
PR ready, CI green, docs/lanes/pri432/NOTES.md holds the request.sh site for item 2 and a rendered status panel (paste the panel text).

# #111 -- alpha dropped at video output, severity note for #89 (cloud, no device)

Base: 5d573d21cb4fd59fda13ca6bc247c59519aeca4e (origin/master).
Files: docs/testing/nv2a_issues.toml status_note for #89 ONLY via
$DISPATCH_DIR/board-requests/<lane>.md (you may not edit the tracker
directly -- write the request, the orchestrator applies it); new files under
docs/testing/predictions/ and docs/investigations/.
Issue: #111 (and #89, read-only).

GOAL: #111's own text says the severity note on #89 can be written now,
without hardware. Read #89's current status_note and PR #102's narrowing to
the 141,125-cleared-pixel alpha-only residual. Write the severity analysis:
if alpha never reaches the video encoder (unconfirmed, this issue's claim),
what player-visible cases would still be affected by #89's residual (surface
read back as a texture) versus not (direct scanout). Put this analysis in a
board-request file for #89's status_note, not in the tracker directly.

Also write the prediction file for the eventual hardware test: two captures
of the same cleared surface, framebuffer vs video output, for a pad-alpha
format whose clear alpha differs from 1.0 -- name what "alpha reaches the
encoder" and "alpha does not reach the encoder" each predict for the video
capture.

FALSIFIER: if alpha reaching the encoder were true, the video-output capture
would show the surface's actual non-1.0 alpha somewhere (e.g. composited
against a background); if false, it would show no trace of alpha at all
regardless of the framebuffer's value.

DONE WHEN: the board-request file for #89 exists with the severity
reasoning, and a prediction file for the hardware test exists. No device
time, no code change.

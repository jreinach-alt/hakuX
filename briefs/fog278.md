# lane.fog278 -- #278 INF fog coordinate: land the two-half hunk

Issue: #278 (accuracy, 85,268 px over 16 `INF-FogExc-{exp,exp_abs,exp2,exp2_abs}-*` captures, 0/16 exact).
Base: origin/master @ 6550967a5e (PR #336 and PR #347 both folded).
Needs device: yes (one arm). Needs NDK: no.
Files: hw/xbox/nv2a/pgraph/glsl/vsh.c, hw/xbox/nv2a/pgraph/glsl/psh.c,
docs/testing/predictions/fog278-*.json, docs/lanes/fog278/**.
psh.c is lent alongside lane.cullnf276 and lane.y16bump10; their hunks are
elsewhere in the file (yours is `append_fog_factor`, ~:1905). vsh.c is yours
alone now (NaNToOne landed in #336; do not touch it).

## Goal
Read docs/lanes/cloud-278/NOTES.md (PR #319) first. The rule it derived: in the
four exp modes, when fogParam.y == 0 silicon computes 0*INF = 0 and runs the
ordinary formula on x = bias - 1.5; every other cell keeps the fixed special
value. Land it as ONE commit holding both halves, because psh.c alone is inert
(the exemption never fires without the vsh.c flag): vsh.c:944-949 sets
`fogSpecial = 2.0` for INF, psh.c:1905-1912 exempts it. Priced 85,268 -> 3,564
residual (a separate unfogged-diffuse defect owns the rest). Re-check both line
ranges against master before editing; they were named at 248f1312c7.

## Falsifier
Register the arm before the code lands, after your last rebase (a rebase
un-ancestors b_ref). Must move: the 16 INF-FogExc captures, predicted <= 3,564
total. Must not move: every finite-coordinate fog capture exact today, and the
full Fog suite. If the 16 do not move, or move but not toward the goldens, say
which cell of the mode x abs table failed and revert in a new commit -- a FAIL
is a diagnosis (lane.pshqueue's #315 was refuted exactly this way).

## Done when
PR ready (out of draft, CI green) with the arm verdict PASS, or a measured
negative recorded in docs/lanes/fog278/NOTES.md. Before marking ready, merge
master and re-run your arm; psh.c has other lanes' hunks landing.

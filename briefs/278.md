# #278: INF fog coordinate -- exp and exp2 fog come out opposite to silicon

Issue: #278 (accuracy). Base: origin/master @ 248f1312c7. Cloud-class: desktop analysis, captures on disk.
Files: docs/lanes/cloud-278/** (analysis only; glsl/psh.c is held by other lanes -- name the hunk, do not edit)
Needs device: no. Needs NDK: no.

## Goal
`INF-FogExc-{exp,exp_abs,exp2,exp2_abs}-*` (16 captures, 85,268 structural px, 0/16 exact): our fogged/unfogged
result is the opposite of silicon's, differently for exp and exp2. Read `append_fog_factor` (psh.c:1866-1910),
the test source, and the goldens. Derive silicon's rule for an INF coordinate per mode: which of {fogged,
unfogged} each of exp / exp_abs / exp2 / exp2_abs produces, and what clamp or NaN/INF rule in the fog table
lookup gives it (e.g. exp(-INF) = 0 vs INF*INF, and where silicon saturates).

## Falsifier
A four-cell table (mode x abs) of predicted fogged/unfogged, scored per capture as REGIONS against all 16
goldens, with a control: the finite-coordinate fog captures exact today must be unchanged by the same rule.
If silicon's result cannot be a function of the coordinate alone (e.g. depends on the fog-table state), say so.

## Done when
A NOTES section gives the rule and per-capture numbers, and names the psh.c hunk for a grant; or a measured
negative.

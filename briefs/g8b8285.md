# #285: Surface format -- Fmt_G8B8 differs (31,820 px)

Lane: g8b8285              Issue: #285 (Fmt_G8B8, 31,820 px, status ok; Fmt_B8 is label-differs and void)
Base: origin/master @ fab230935e (rebase to the tip before you register anything).
Files: docs/lanes/g8b8285/**, docs/testing/predictions/g8b8285-*.json
       (LOCATE-FIRST: surface-format code is spread over vk/surface.c (lane.remote), vk/draw.c
       (lane.vtxarr262), glsl/psh.c (lane.wbufdepth24). Name the function and hunk; ask by board-request.)
Needs device: yes for the arm; the analysis is desktop. Needs NDK: no.

## The defect
One capture differs, no mechanism named, no history found; the inventory guessed ~10% recoverable (0-31,820).
Games: low. The first job is to find out whether this is a defect, a stale capture, or a golden problem.

## Read first; nothing is known
1. Date every capture on disk (both devices) against the fix history of Surface format, #271 (X1A7R8G8B8, folded
   as PR #306) and #184 before reading a residual: a capture older than the fix reads like a live defect.
2. Diff ours against the golden BY REGION, not by pixel count: which channels are wrong (G8B8 stores two; the
   others are synthesised), by how much, over which part of the frame. Say what the golden does with B and A and
   what our format mapping does.
3. Read how Fmt_G8B8 is created, drawn and read back (pgraph surface format table, vk format map, swizzle).
   State the rule silicon follows and price it offline against the capture before naming any hunk.

## The arm (register after the last rebase, bound to the function you name)
must_move: Fmt_G8B8. must_not_move: every other Surface-format capture (Fmt_*), Clear, Blend surface. Name the
patch change that would move each; check `status` for `unreadable` and the run's coverage line.

## Done when
NOTES.md classifies the residual (defect / stale / golden), names the mechanism if a defect and the hunk with its
holder; the arm is registered if code is named; the PR is ready. "Not a defect, here is the evidence" is a
complete outcome.

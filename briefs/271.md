# lane.cloud-271 -- #271: land the X1A7 read-side rule (psh.c, psh.h, vk/texture.c) and register its arm

Issue: #271. Base: origin/master @ a5b5b628f2. Continues the analysis in PR #365 (merged): read docs/lanes/cloud-271/NOTES.md first; the diff is docs/lanes/cloud-271/x1a7-read-side.diff.
Files (write this exact list on your PR's `Files:` line; it is literal, no globs beyond the last two):
       hw/xbox/nv2a/pgraph/glsl/psh.c, hw/xbox/nv2a/pgraph/glsl/psh.h, hw/xbox/nv2a/pgraph/vk/texture.c,
       docs/lanes/cloud-271/**, docs/testing/predictions/cloud-271-*.json.
       All three source files are free: lane.pshqueue retired, lane.remote no longer lists vk/texture.c, and PRs #393 (lane.pshaniso284) and
       #379 (lane.tiecode282) released psh.c / vk/texture.c at ready. vk/draw.c is lane.remote's; the read side needs nothing there.
       Before marking your PR ready, merge master (or those PRs' branches if they have not folded) and re-run your arm.
Needs device: no session access; the arm is a registered prediction and the arms job runs it. Needs NDK: no.
The branch lane/cloud-271 already exists from PR #365: merge master into it first.

## Already known (NOTES s1-s3, do not re-derive)
The X1A7 alpha rule is inverted from the goldens (0 of 32 swatches break it) and priced on the two newest device runs: the four
Blend_surface DstAlpha_XA_{Z,O}1A7RGB8 / 1-DstAlpha captures go 65,536/81,920 -> 16,384 (the residue is the bg 0x80 halves, 128 vs 129).
The diff applies without fuzz and merges cleanly with what #347 landed. It adds one gate beyond #306: X8R8G8B8 texture formats keep alpha 1.0.

## The job
1. Apply the diff, build what you can offline, check every symbol it uses exists (NOTES s3).
2. Register the NOTES s4 arm AFTER the last rebase, on concrete refs: must_move the four Blend_surface rows and the two Clear/SCF_X1A7R8G8B8 rows;
   must_not_move the exact rows, naming the change that would move each. Fmt_X1R5G5B5_{Z1,O1}R5G5B5 are "equal to base", not 0.
3. Fmt_X1A7R8G8B8_O1A7R8G8B8 (16,383 today) regresses by at least +58,754 without a per-surface "bytes came from the CPU" gate. Register it as a
   failing leg, or land the gate: NOTES s5's probe decides whether upload_pending can serve. Do not land it unregistered. If the net is a loss on
   the golden disc, say so on #271 and stop: an accepted regression is the owner's, not the lane's.

## Falsifier
The four Blend_surface rows stay at base: the mode never reaches the stage (drawn format not 0x06/0x07 at bind, or upload_pending true).
Z fixed but O structural: a wrong (X<<7) term. A non-X1A7 exact row moves: a stale per-stage mode survives; reset disabled slots in pgraph_vk_bind_textures().
Check `status` for unreadable and PARTIAL COVERAGE on every row before believing a number.

## Done when
The diff is landed, the arm registered after the last rebase and its verdict read, the declared regression gated or accepted by the owner in a linked
issue, NOTES.md updated, master merged and the arm re-run, PR ready.

# #271: X1A7R8G8B8's expanded 7-bit alpha is not stored or read back (229,376 px recoverable)

Lane: cloud-271              Issue: #271 (X1A7 surface format; orphaned between #60 and #184)
Base: origin/master @ 690a3377d1 (rebase to the tip before you register anything).
Files: docs/lanes/cloud-271/** (no hw/ file; the hunks per holder go in NOTES.md)
       (LOCATE-FIRST: PR #176's design writes in glsl/psh.c + vk/draw.c and reads back in vk/texture.c;
       psh.c is lane.wbufdepth24's, vk/draw.c lane.vtxarr262's, vk/texture.c lane.texvol283's.)
Needs device: no -- desktop analysis and pricing only; the arm is registered by the follow-up code lane. Needs NDK: no.

## The defect
Blend_surface *XA_[OZ]1A7RGB8*, Surface_format Fmt_X1A7R8G8B8_[ZO]*, Clear SCF_X1A7R8G8B8_[ZO]*: 411,154 px
structural, ceiling 409,382. PR #176's model (store the expanded 7-bit alpha in the host surface) fit 32 of
32 golden halves and predicts the four DstAlpha captures (229,376 px) go to 0.

## The job
1. Read PR #176's diff and NOTES, then re-derive the alpha expansion rule from the goldens yourself (a
   premise taken from a NOTES table is a claim). State the rule and the 1,772 px it cannot reproduce.
2. Re-base the design on master: which host format carries the 7 bits, where the write path expands, where
   the read path contracts. List the exact hunks per file and which holder owns each.
3. Price it offline against the four DstAlpha captures.

## The arm (register after the last rebase)
must_move: the four DstAlpha captures -> 0 (or a stated residual). must_not_move: every other surface-format
row exact today (X8R8G8B8, A8R8G8B8, X1R5G5B5); name the change that would move each.

## Done when
NOTES.md holds the rule, the per-file hunks and the priced result, the arm is registered, and the PR is
ready. Analysis plus the hunks per holder is a complete outcome; the board grants files as holders fold.

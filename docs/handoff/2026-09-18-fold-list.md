# The fold list — `lane.fold`, 2026-09-18

Produced during Phase 0 wind-down for the design session that will merge branch
by branch. **This lane folded nothing after the wind-down order arrived and
merged, reset or unlocked nothing** outside its own scratch worktree.

**Campaign tip when this was written: `72035a2121`** (`fix: unbreak the
fleet-wide push gate I broke with an invalid component`). Everything below is
stated against that sha. **Three branches moved while this was being written**
(marked LIVE) — re-resolve their tips before merging.

Method: `git cherry <campaign-tip> <ref>` separates genuinely-new commits from
patches already upstream, and `git merge-tree --write-tree` probes each merge
without touching a working tree. **110 refs are "ahead" of the tip; 72 of them
carry no new content at all** — they are stale bases, and merging them is pure
noise. Only the refs below hold unfolded work.

## CI: nothing here trips a trigger

Checked against every unfolded commit, not assumed. `build-xemu-win64-toolchain.yml`
has a bare `push:` with **path filters and no branch filter** — the one workflow
that can fire from a non-`master` branch. **No unfolded commit touches
`ubuntu-win64-cross/**` or that workflow file.** The only `.github/` change in
the whole set is `f97c744429`'s edit to `release-published.yml`, which triggers
on `release: published` and has no push path at all.

`origin/claude/docs-tooling-agentic-coding-u152m1` has **PR #45 open** — a push
to it costs three runs. Merging it locally costs nothing; pushing it does.

## A — merge candidates, no known blocker

| branch / worktree | tip | issues | audit | merge probe vs tip |
|---|---|---|---|---|
| `origin/claude/hakux-orchestration-design-e663m8` | `f97c744429` | Phase 0 harness, `ORCHESTRATION-DESIGN.md`, `ORCHESTRATION-WIND-DOWN.md` | none | **CLEAN** |
| `origin/lane/tier81fix` | `b2532b9fe4` | #81, #90 | none | **CLEAN** |
| `origin/lane/swizzle87` | `d7f3b6b29a` | #87 | none | **CLEAN** — but see D, it is duplicated |
| `worktree-agent-a800eb98ac1ec5861` | `89c48ec50f` | #38 (`BLEND_AND` divide by `0x7f80`) | referenced by #84 pass 1/2 | **CLEAN over the folded #84** — verified, this was the real risk |
| `pr/frontend-launch-handoff` | `f9c3bff874` | android launcher URI grant + return-to-frontend | none | **CLEAN** — dated 2026-09-07, confirm not superseded |
| `worktree-agent-a3a9f07a3734445a6` | `4401ad3bda` **LIVE** | re-points #59's arm, drops a dead #38 leg | none | **CLEAN** — predictions only |
| `origin/claude/docs-tooling-agentic-coding-u152m1` | `0e13156a05` **LIVE** | #51, #66, #71, #82, GL line disc | pass 1 + pass 2 | conflicts in `docs/testing/nv2a_index.json` only |

## B — blocked, do not merge yet

| branch / worktree | tip | issues | audit | blocker |
|---|---|---|---|---|
| `worktree-agent-aa4f3448ff981e82f` **(LOCKED — leave locked)** | `26b6a66e4b` **LIVE** | #88, #91 | pass 1 + pass 2 (both clear on code) | **#88's arm FAILED**: `Color_zeta_overlap/Swap` 165,447 → 304,750 on a registered `must_not_move`, filed **#91**. Must not fold until Swap is explained or the policy narrowed. The lane is actively diagnosing it — its newest commit argues #91 is *a stray depth clear, not a missing depth test*. Merge probe is CLEAN, so **the blocker is judgement, not mechanics.** |
| `worktree-agent-af29d9ef9a8f784d4` | `937848c9e7` | #68 | none | Held pending its own arm: ~200 lines in `accel/tcg/tb-maint.c`. Also **conflicts** in `tb-maint.c` *and* `accel/tcg/cpu-exec.c` — the latter against `lane/tier81fix`'s #81 work, so these two are coupled. |

## C — must never be merged

Experiment and arm refs. Each is a deliberate one-or-two-line build variant for
an A/B; several say so in their own subject (`EXPERIMENT`, `do not land
unmeasured`, `CONTROL BUILD`, `arm B ref only`, `incomplete, do not merge`).
Merging any of them lands an experiment as policy.

- `exp54-arm` `ade16fa90c`, `exp54-stst` `7b63484c69`, `exp54-x86eq` `2cd72580fe` — TCG barrier experiments, all three mutually exclusive in `tcg/tcg-op.c`
- `skew44-mode1` `03b6a026de`, `skew44-mode2` `cc8fb579c4`, `skew44-probe-mode0` `e28c402b4d`, `skew44-probe-mode1` `7349193309`, `skew44-probe-mode2` `3b7fe5f5fa`, `arm/issue44-draw-only-on` `d879e6e03b`, `wt/skew-armA-control` `d97d506514` — seven mutually exclusive variants of `hw/xbox/nv2a/pfifo.c`
- `wip/texture-signedness` `f918e65acf` — subject says *incomplete, do not merge*

## D — superseded, duplicated, or already upstream

**This is the category that reverts a correct fix by accident.**

1. **#87 exists twice on origin under different shas, identical content.**
   `origin/lane/swizzle87` = `origin/worktree-agent-a53a868ef5fc8701f`
   (`c807592d02`/`d7f3b6b29a` vs `4fba887182`/`5b6568fff9`) — patch-ids
   `1562ba918e7d` and `4b5334721325` match pairwise. `origin/wt-swizzle87` is a
   third name for the same work. **Merge exactly one.**
2. **#51's psh.c fix is ALREADY FOLDED**, as `8b50aefbd4` and `28eeff3d88`.
   `docs-tooling`'s `4a08abef66` and `4c3364bcb1` are the pre-fold originals and
   `git cherry` still marks them new, because the fold squashed the audit
   remediation in. A merge does **not** revert `psh.c` — probed, net diff to
   that file is empty — but do not read "new commits" here as "unfolded work".
3. **72 refs carry no new content.** Every commit they hold is already upstream
   by patch-id. Do not merge them; a `--no-ff` of a stale base is all risk and
   no gain.
4. `docs/audits/2026-09-18-blitsafe-pass2.{md,json}` briefly existed only on the
   now-unreachable `aea28c6e3e`. **Content is preserved** on the campaign tip at
   identical blob shas — nothing to recover.

## Order, and why it matters

1. **`origin/claude/hakux-orchestration-design-e663m8` first.** It rewrites the
   checkers and the dispatcher, and it is CLEAN. Landing it last means every
   other merge is gated by tooling that is about to be replaced.
2. **`origin/lane/tier81fix` before `worktree-agent-af29d9ef9a8f784d4` (#68).**
   Both touch `accel/tcg/cpu-exec.c`; #81's is the smaller, already-measured
   change and #68 is held anyway.
3. **`worktree-agent-a800eb98ac1ec5861` (#38) after the #84 fold**, which is
   already on the tip. Probed CLEAN in that order. The reverse order is untested.
4. **`docs-tooling` last of group A.** Its only conflict is
   `docs/testing/nv2a_index.json`, and that file must be **regenerated, never
   hand-merged**:

       python3 docs/testing/nv2a_index.py build \
           --tests /home/justin/nxdk_pgraph_tests \
           --support /home/justin/pbkitplusplus

   **Both flags matter.** Rebuilding without `--tests` silently drops the suite
   half of the file — it wiped 2,306 lines once already. Resolve by taking
   either side, then regenerate and amend. The same applies to `wt/skew-bound`
   and `wt/54-texupload-fix` if they are ever merged.
5. **#88 (`aa4f3448ff981e82f`) not at all**, until #91 is settled.

## Do not disturb

- `git worktree lock` on `/home/justin/hakuX/.claude/worktrees/agent-aa4f3448ff981e82f`
  is **deliberate** — two arms' `expect` paths point inside it at an absolute
  path. **Left locked. Do not prune, reset or unlock it.**
- **The `arm/*` tags are load-bearing.** That worktree was rebased after the #84
  fold, and `arm/4e795536df` is now the **only** ref from which `4e795536df` is
  reachable. Deleting the tags orphans the pre-rebase arm refs that judged
  predictions name. Thirteen tags: `arm/0e919fe51e 12870b8fde 24a75d6e3c
  4726557b0f 4e795536df 4fba887182 5b6568fff9 70e4073441 71c7bd1d7d cd8854be10
  e71a9952d1 f343580647` and `arm/tcg-round3`.

# lane.pshqueue

This lane lands the queued glsl/psh.c hunks for #278, #279, #285 and #315, with one commit and one prediction for each.
#271 is skipped: its read side needs vk/texture.c, which lane.remote holds (PR #343).

# lane.foldregress — the `regressed` gate

Work in progress. The defect: `regressed` was a rule in `jobs/roles/board.md`
and nothing in `jobs/fold.sh`, so PR #102 folded as `3d072c6ea6` carrying the
label. This lane makes it a gate in the script.

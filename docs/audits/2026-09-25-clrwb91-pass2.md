# Audit pass 2: PR #237 (lane.clrwb91, #91)

Head verified: `095433d451` (pass-1 head `bc8d948fd5` + the pass-1 audit
file). Auditor: job.cloud, 2026-09-25.

**Result: clean. → `fold-ready`.**

Pass 1 raised no HIGH or MEDIUM, so no remediation was expected and none
was pushed. `git diff bc8d948fd5 095433d451` touches only
`docs/audits/2026-09-25-clrwb91-pass1.md`. Pass 2 checks the two things
pass 1 asked it to check.

## 1. The head still carries exactly the audited `surface.c` hunk

`git diff origin/master...HEAD -- hw/xbox/nv2a/pgraph/vk/surface.c` is one
hunk at `surface.c:2984`. It adds the explanatory comment and
`regions[0].bufferOffset = 0;` in the Z24S8/D32S8 compute branch, just
before the existing `regions[1].bufferOffset` rewrite. Its only
non-comment line is the same one the arm measured
(`git diff 9c5f7416d8 9de95de849`). Pass-1 checks 1–4 (offset matches the
unpack's descriptor binding, `regions[1]` populated, arm measured the
shipped line, prediction not tautological) all rest on this hunk, and it
has not moved, so they still hold. The PR is MERGEABLE.

## 2. The LOWs

* **LOW-1 (the swizzled-colour sibling at `surface.c:3007-3059`) is still
  present.** `use_compute_to_unswizzle` still sets
  `copy_buffer = COMPUTE_SRC` without resetting
  `regions[0].bufferOffset`. Pass 1 judged this correctly: the defect is
  pre-existing, the diff doesn't touch it, and fixing it here would move
  the shipped change off the arm's b_ref. It does not block fold. On
  2026-09-25 no issue for it existed (searched open and closed issues:
  "unswizzle bufferOffset", "swizzle staging_base"; the swizzle-titled
  open issues #59 and #109 are different defects). This firing routes it
  in a comment on #91 so it doesn't rest on this file alone. The board
  decides whether it becomes its own tracker row.
* **LOW-2** (the comment will go stale once LOW-1 is fixed) is a note for
  whoever fixes LOW-1. It needs nothing now.
* **LOW-3** (reach under master's policy is unmeasured) is stated in the
  PR's Limits section. It belongs to whoever claims the fix's reach and
  does not block this fold.

## Verdict

No pass-1 scenario is introduced or left open by this diff. `fold-ready`.

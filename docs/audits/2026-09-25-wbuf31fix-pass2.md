# Audit pass 2: PR #222, lane/wbuf31fix (#31)

Auditor: job.cloud, 2026-09-25.  Head verified: `361ae48f75` (pass 1 audited
`80d01ec5a7`; the only commit since is the pass-1 file itself, checked with
`git diff --stat 80d01ec5a7 361ae48f75`).

**Result: clean.**  Pass 1 raised no HIGH and no MEDIUM, so there is no
remediation to verify.  The three LOWs are unchanged and none blocks a fold.
Next label: `fold-ready`.

## Pass-1 scenarios, re-checked at this head

The shader is as pass 1 read it: `psh.c:2471-2474` still computes `flatTop`,
`topCut = clip.y > 0.0 && ytop < clip.y`, and
`grid = !cut || (topCut && !(flatTop && mod(clip.y, 8.0) == 0.0))`.
Pass 1's no-clip equivalence argument (grid reduces to `!cut`) still holds.
CI on this head: build x2 and check are SUCCESS, and the PR is MERGEABLE.

- **LOW 1 (the rule is one of 93 fits).**  Still possible, still disclosed.
  No capture shows the shipped selector wrong, and the NOTES name the
  separating run (ClipF, clip_left 300, clip_top 8).  That run is the
  follow-up.  Pass 1 asked for no remediation.
- **LOW 2 (the investigation doc still says ClipF is unselectable).**  Not
  fixed.  `docs/investigations/wbuffer-slope-offset.md:355` and `:495` still
  say "unselectable", and `psh.c:2371` still points readers there.
  **Deferred, with a reason:** it is documentation only, and an audit may
  write only its own file to the lane branch.  A one-line dated correction at
  the top of that doc should go to a follow-up, or ride along with the next
  #31 lane.  The scenario (a later lane reads the doc and reverts the
  selector) is real but bounded: the arms job would catch a revert, because
  the three movers are `must_not_regress`.
- **LOW 3 (`features()` TypeError in `--selectors`).**  Not fixed.
  `wbuf_anchor_recover.py:304` still indexes `_first_span(wide)[0]`.  It is
  offline tooling, and no current `PRIMS` entry reaches the path.  It stays
  LOW.

## Verdict

Clean.  Remove `needs-audit-2` and add `fold-ready`.

# Audit pass 2: PR #398 (lane.fmv303b), #303 tier1 on/off A/B

Head verified: `53e50b942a` (pass-1 head `297ee1f823` plus the pass-1 audit file).

**Result: clean.** Pass 1 raised no HIGH or MEDIUM findings, so nothing needed fixing. The two conditions pass 1 set
for this pass both hold. → `fold-ready`.

## Checks that pass 1 asked for

1. **Head unchanged except lane docs.** `git diff --stat 297ee1f823 53e50b942a` shows one file,
   `docs/audits/2026-09-26-fmv303b-pass1.md` (+67). No code, prediction or lane tool changed after pass 1.
2. **The hunk is still default-inert.** Against `origin/master`, the only code change is still the two lines at
   `android/app/src/main/cpp/xemu_android.cpp:975-976`, and they override the pref only when `HAKUX_TIER1_THRESHOLD`
   is set and non-empty. `git grep HAKUX_TIER1_THRESHOLD origin/master` finds nothing. On the branch, the only setter
   is the prediction's `b_env`, so no default launch path sets the variable.

## Other checks

- **The prediction still cannot jam the arms job.** `fmv303b-tier1-ab.json` has `a_ref == b_ref` (True), and the
  structural skip for that case is still in master's `arms.sh` (comment at `:273`).
- **CI and mergeability.** Both `build` checks pass on `53e50b942a`, and GitHub reports the PR as `MERGEABLE`.

## Pass-1 LOWs

| Finding | Status | Can its scenario occur on this head? |
|---|---|---|
| LOW-1: M0 gates on the requested threshold | Open | No. The OFF arms logged 0 promote lines (pass 1, item 2). The risk only applies if the judge is reused. |
| LOW-2: `atoi` of a non-numeric value | Open | Only if someone sets the variable by hand. Nothing on master sets it. |
| LOW-3: host-path judge script | Open | It is a lane-local record, not a reusable tool. |
| LOW-4: wide NOT_TIER1 band | Open | No. The observed gap is 0.008, so the verdict does not depend on where the band is drawn. |

None of the four blocks the fold.

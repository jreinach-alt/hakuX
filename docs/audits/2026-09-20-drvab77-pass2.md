# Audit pass 2 — PR #197, `lane/drvab77`: does #77's stipple rate move with the GPU driver?

**Auditor** `job.cloud` (claims no files; audit record only).
**Subject** PR #197, branch `lane/drvab77`, tip **`2813170ff8`**. The
remediation is `2813170ff8` over the pass-1 tip `0781ab898b`, plus a merge of
`origin/master` (`8a17d79e8c`). Mergeable `MERGEABLE`; both `build` checks
`SUCCESS`.
**Pass 1** `docs/audits/2026-09-20-drvab77-pass1.md` — 0 HIGH, **5 MEDIUM**,
**4 LOW**. **Date** 2026-09-20. **Record** `2026-09-20-drvab77-pass2.md`.

**CLEAN. All five MEDIUMs and all four LOWs are discharged, each verified by
re-running the scenario rather than by reading the commit.**

Pass 1's finding was that no number in this lane was wrong; what could not be
checked was the record and the tooling. So pass 2 re-ran everything a reader
would: all ten runs re-scored at whole frame and at `R_lower` from the PPMs on
disk, `window_audit.py` re-run over all result dirs, both test files run as
committed **and against six mutants of the code they pin**, `compare_arms.py`
re-run, and `preflight.sh --allow-tracker` run on the branch. The
remediation's own new quantitative claim — R8's hole-free re-score — is
reproduced number for number below, because a remedy that introduces a table
introduces a thing to audit.

## Per-finding verdicts

### M1 — the gap detector fires on every run and truncates away the anomaly. **CLOSED.**

`score_arms.py:128-151` replaces `b != a + 1` with the run's own cadence:
median gap, max gap, `max/median`, and the **largest** holes, flagged only
above `ANOMALY_RATIO = 2.0`. Re-run over the ten runs as committed:

```
C3  spacing: median gap 10, max 15 (1.5x)     [silent]
C4  spacing: median gap 10, max 16 (1.6x)     [silent]
C5  spacing: median gap 10, max 15 (1.5x)     [silent]
C6  spacing: median gap  9, max 15 (1.7x)     [silent]
t30-1  max 43 (4.3x)  SPACING ANOMALY: 3 hole(s) -- largest f533->f576 (43), f189->f218 (29), f218->f242 (24)
t30-2  max 15 (2.1x)  SPACING ANOMALY: 1 hole(s)
t26-1  max 38 (4.2x)  SPACING ANOMALY: 1 hole(s) -- f25->f63 (38)
t26-2  max 53 (7.6x)  SPACING ANOMALY: 5 hole(s) -- largest f241->f294 (53), ...
stock-1 max 15 (1.7x) [silent]  stock-2 max 15 (1.7x) [silent]
```

The pass-1 scenario was that `t30-1` and `C3` print the same reassuring line.
They no longer can: the four band runs and both stock runs print no anomaly at
all, and the three runs pass 1 named print their **largest** hole first, not
their first four routine ones. The bar is not a curve fit — the two
populations measured here are 1.5–1.7x and 2.1–7.6x with nothing between them.

The remedy's second half ("state the profile and say whether the rates are
stable under it") is R8, and **all eight of its hole-free numbers and all four
of its `n` reproduce exactly** under the natural reading of its method (drop
every image whose ±10-image baseline window spans an anomalous boundary; `W`
is `stipple_classify.DEFAULT_WINDOW = 10`):

| run | `R_full` | hole-free | `R_lower` | hole-free | n |
|---|---|---|---|---|---|
| `t30-1` | 9.5 | **10.0** | 23.8 | **26.7** | **30** |
| `t30-2` | 5.4 | **6.9** | 16.3 | **13.9** | **72** |
| `t26-1` | 5.1 | **6.1** | 15.4 | **16.7** | **66** |
| `t26-2` | 8.4 | **8.8** | 22.9 | **26.5** | **34** |

R8's supporting claims also hold as re-derived: all four `R_full` rates move
**up** (wrong direction for the feared mechanism); the largest shifts are 1.5
(`R_full`) and 3.6 (`R_lower`) against the stated 2 se of 7.9 and 9.4; and
exactly **one** flagged frame across the ten runs sits immediately across an
anomalous boundary — `t26-2` image 75, boundaries at {32, 36, 43, 74, 77} —
giving 6/83 = 7.2 when dropped, as stated. The hole-free arms 10.0/6.9,
6.1/8.8, 4.8/9.2 give max between-arm mean gap 1.45 against min within-arm
difference 2.7, and no arm has both runs below the floor. Same null.

### M2 — R3's G1 evidence mixes whole-frame and `R_lower` numbers. **CLOSED.**

`NOTES.md:329-351` now carries **med HF (whole frame)** and **med px (whole
frame)** per run — the quantity P3 registered G1 against — and states the
range as 1.635–1.758 / 33.6–35.8. Re-derived from the PPMs, per run:

| | `t30-1` | `t30-2` | `t26-1` | `t26-2` | `stock-1` | `stock-2` |
|---|---|---|---|---|---|---|
| med HF | 1.758 | 1.635 | 1.753 | 1.723 | 1.673 | 1.658 |
| med px | 34.2 | 33.6 | 35.0 | 34.6 | 35.8 | 34.7 |

Every cell matches the committed table. The ten-run spans (7.5% HF, 6.8%
brightness) reproduce. The `R_lower` figures the note now quotes separately —
medians 1.438–1.691 and 27.9–33.7 over the ten runs, C6 at **27.862** —
reproduce exactly, so the paragraph's point that G1's 29.1 floor does not
transfer (it would void a band run) is stated on measured values. **The
pass-1 scenario cannot occur**: there is no longer a range a reader can apply
to the wrong region, and the superseded draft is withdrawn *in place* at
`NOTES.md:349-351` rather than only in R9 — the PR body carries the corrected
columns too, so all three places a reader reads it agree.

### M3 — the sentence applying P4's rule miscounts the excursions. **CLOSED.**

`NOTES.md:363-377`: *"**two** of the six arm runs fall below its floor —
`t26-1` at 5.1 and `stock-1` at 4.8 — with neither arm's partner joining it
(8.4 and 9.2, both inside)"*, and the `R_lower` sentence still correctly names
`t26-1` at 15.4 as the only excursion there. The PR body carries the same
corrected sentence. The paragraph pass 1 asked for — that every batch has
pushed the floor down (6.6 → 5.4 → 5.1, 4.8), which bears on R5's
"effects below the interval" — is present at `NOTES.md:371-377`. All four
rates re-derived above.

### M4 — `window_audit.py` asks "finished inside", not "overlapped". **CLOSED, and pinned by a test that fails on the old code.**

`window_audit.py:134-144` is now `start <= b and end >= a`, with start taken
from `queued_utc` then the id's epoch prefix, and a dir with neither still
listed whenever `end >= a`. Run over all result dirs as committed, **R1's
published finding is unchanged**: 4 runs executing inside the two windows, all
four this lane's, **0 foreign**.

The scenario pass 1 named — *"a foreign arms-job run straddles the window and
the audit prints 0 not this lane's"* — is now the first case of
`tests_window_audit.py`, and I built the mutants rather than take the
docstring's word:

| mutant | cases it fails |
|---|---|
| the **original** `a <= mtime <= b` finish-time test | 1 (straddle), 4, counts, FOREIGN count |
| overlap with the artifact discriminator removed | 4 (queue-straddler), counts, FOREIGN count |
| as committed | none — ALL PASS |

So the straddle case is caught, and the old code demonstrably fails it. The
second discriminator is not decoration: the real disk contains
`1789893938-arms-remote-base-4039343`, queued 01:45 and first writing at
21:53, which a bare `[queued, done]` overlap reports as contamination. That
would have rebuilt M1's defect inside M4's fix, and the committed code reports
it as `QUEUED-THRU` while still listing it. Both classes are printed; only
`EXPOSED` counts.

### M5 — case 4 asserts the answer, so the bound is untested. **CLOSED.**

`tests_session_header.py` gains case 5 (a record closing one line **past**
`MAX_HEADER_LINES` must not be read) and case 6 (one spanning **exactly**
`MAX_HEADER_LINES` must be read, asserting `_header_lines == 12`), both sized
off the constant. I rebuilt all four mutants the docstring tabulates and ran
the committed test file against each:

| mutant | cases it fails |
|---|---|
| delete `MAX_HEADER_LINES` and its `break` | **5 only** |
| `break` at `MAX_HEADER_LINES - 1` | **6 only** |
| drop the `t == "session"` check | **3 only** |
| the old one-line `readline` reader | 1 and 6 |
| as committed | none — ALL PASS |

The table in the file is exactly right, including the claim that 3, 5 and 6
are each the sole failure of one mutant. The pass-1 scenario — someone drops
the bound and the suite stays green — is closed: that is now the one mutant
case 5 exists for, and it is red.

### L1 — a documented void rule with no implementation. **CLOSED (implemented).**

`score_arms.py:181-190, 210-213, 236-238, 257-259`. Exercised on real dirs:
`--expect-driver Adreno` over `t30-1` and `stock-1` prints
`VOID (driver): header says 'PurpleVK public driver (...)' , caller asked for
'Adreno' -- this run is not evidence about the arm it was filed under` on the
first, leaves the second alone, and **exits 1**. The rate is still computed
and printed, which is right: what is asserted is that the run may not be used
as evidence about that arm. A run whose header is unreadable voids too
(`one_line(None)` is `"?"`), which agrees with G2.

### L2 — `docs/testing` on `sys.path` only as a side effect. **CLOSED.** `score_arms.py:48-51` inserts it explicitly with the reason in a comment.

### L3 — "76-92 images" excludes `t30-1` at 63. **CLOSED.** `NOTES.md:324-325` reads **63-92**, and the PR body agrees.

### L4 — `window_audit.py`'s inputs are unverifiable. **CLOSED as far as it can be.** `--results` / `HAKUX_RESULTS`, `--window` and `--mine` are arguments. The half pass 1 asked for that cannot be done — point the bounds at `swap_driver.sh`'s log — is declined with a checkable reason (that script is outside this repository and writes neither log nor marker), and the docstring now says the bounds have no artifact behind them instead of implying provenance. R9 records the decision. Declining a suggestion with a stated reason is a complete answer to a LOW.

## What else pass 2 checked, and did not find

* **The twenty rates still reproduce**, at whole frame and at
  `--region 0,288,640,480`, after the remediation and the master merge:
  9.6/6.8/16.4/6.6 and 27.4/30.1/23.3/22.4 for the band; 9.5/5.4/5.1/8.4/4.8/9.2
  and 23.8/16.3/15.4/22.9/20.2/23.7 for the six arms. `compare_arms.py`
  re-run: sd 3.97/4.71, means 7.45/6.75/7.00, largest between-arm gap 0.70,
  smallest within-arm difference 3.30, 1.98x against 2.48x — unchanged. The
  remediation moved no number, as it claims.
* **No stale copy of a corrected claim.** Grepped the notes, the scripts and
  the PR body for the withdrawn figures (`1.44`, `29.7`, `76-92`, the
  single-excursion wording): the only hits are the two explicit withdrawals
  and the L3 changelog line. The PR body was updated with the corrected
  columns and the two-excursion sentence, so the record and its summary agree.
* **R9's claim that R7's board red has cleared** is true as of this audit:
  `preflight.sh --allow-tracker` on this tip is green on **every** gate,
  coverage included (`coverage ok (25 open: ...)`).
* **The merge of `origin/master` did not stale the instruments.**
  `git log HEAD..origin/master` over `galleon_flash_rate.py` and
  `stipple_classify.py` is empty, and re-scoring on this tip gives the same
  twenty rates, which is the measurement of that rather than the assertion.
* **Nothing outside `docs/lanes/drvab77/`** is touched by the remediation
  (`git diff 0781ab898b..HEAD --stat`: five files, all in that directory). No
  source file, golden, prediction or board file.
* **The two spare LOW-grade nits I am not filing**, for the record:
  `window_audit.py` prints one "queued through a window" line per window a
  dir straddles, so the four `QUEUED-THRU` dirs produce eight lines; and
  `overlaps()` with no start cannot evaluate `start <= b`, so a dir finishing
  long after a window is listed. Both are documented behaviour, both err
  loud, and neither has a failure scenario.

## Verdict

**Clean.** Every pass-1 scenario was re-run and none can still occur; the two
checks that pass 1 said could not fail now fail on mutants built for them; the
remediation's own new table reproduces to the digit. `needs-audit-2` removed,
**`fold-ready`** set.

# Audit pass 2 — PR #211, `lane/cloud190`: PMC read-1 region at `0x160` and `0x204`–`0x2FC` (#190)

**Auditor** `job.cloud` (claims no files; audit record only).
**Subject** PR #211, branch `lane/cloud190`, tip **`4ffbb3f421`** (the pass-1
audit commit on top of `8e80ae8f83`, the tip pass 1 read).
**Date** 2026-09-24. **Record** `2026-09-24-cloud190-pass2.md`.
**Pass 1** `2026-09-24-cloud190-pass1.md`: 0 HIGH, 0 MEDIUM, 2 LOW.

**Verdict: `fold-ready`.** Pass 1 had no HIGH or MEDIUM findings, so nothing
had to be fixed. Both LOWs are still present. Neither is a correctness defect,
and remediation only covers HIGH and MEDIUM, so sending the PR back would give
a remediation pass nothing it is required to fix.

## What changed since pass 1

`git diff --stat 8e80ae8f83 4ffbb3f421` shows one file, the pass-1 record. The
emulator change (`hw/xbox/nv2a/pmc.c`, one `case 0x160: case 0x204 ... 0x2FC:`
arm in `pmc_read` returning `1` for dword-aligned offsets) is byte-identical to
what pass 1 checked. Pass 1's correctness findings still hold: 64 offsets,
0x200 excluded, the unaligned bytes read 0, and `pmc_write` is untouched.

## Re-run on `4ffbb3f421`, on this host

| Gate | Result |
|---|---|
| `docs/testing/pmc_enable_selftest.sh` | 16 checks, 0 failures |
| `docs/lanes/pmc188/mutants.py` (gcc) | 14 mutants, 0 unexpected |
| `docs/lanes/cloud190/mutants.py` (gcc) | 24 mutants, 0 unexpected |
| `env CC=clang … pmc188/mutants.py` | 14 mutants, 1 unexpected: `['M4']` |
| `env CC=clang … cloud190/mutants.py` | 24 mutants, 1 unexpected: `['N4b']` |

## Pass-1 scenarios

### L1 — M4/N4b expect gcc's diagnostic text: **still occurs (LOW, carried)**

Pass 1 described this failure, and it happens exactly as described. Under clang
both mutants are still refused. Only the expected-text match fails, so the
result is a false red and never a false green. Neither suite runs in CI, and the
default compiler is gcc. It is still LOW. The fix is the one pass 1 named:
match `duplicate` (or `duplicate( \(or overlapping\))? case value`) in
`docs/lanes/pmc188/mutants.py` M4 and `docs/lanes/cloud190/mutants.py` N4b. It
can land on a later commit and does not have to block this fold.

### L2 — the falsifier is not wired into CI: **still occurs (LOW, pre-existing, declared)**

Unchanged. The same gap applies to the other unwired selftests, and fixing it
is not this lane's job. It is recorded here so it is not lost.

## Remaining

None of the checks is HIGH or MEDIUM. CI on this tip: `check` passed. When this
record was written, both `build` runs were still in progress. The fold job gates
on those itself.

# Audit pass 2, round 2: PR #325, lane/dmasurf277 (#277)

Auditor: job.cloud, 2026-09-25 23:50Z. Head read: `904608ff83`. This round
follows the remediation of round 1
(`docs/audits/2026-09-25-dmasurf277-pass2.md`). Pass 1 is
`docs/audits/2026-09-25-dmasurf277-pass1.md`.

**Verdict: the MEDIUM-1 refusal is fixed, and the arm is now queued. It has
not run, so the PR is not fold-ready yet.** The prediction is still pending
its only device evidence. This PR stays at `needs-audit-2`, and the next
pass-2 firing judges the `[job.arms]` verdict. There is nothing for a
remediation to do.

## Round-1 MEDIUM-1: the arms job refused the prediction

**Scenario 1: every arms tick refuses the file. It no longer occurs.**

- I re-ran `request.sh`'s key check on
  `docs/testing/predictions/dmasurf277-dmamap.json` (sha256 `43cd59fce70c`)
  against `/home/justin/goldens/results`. It is the same fnmatch-over-PNG
  set the script builds. Every key binds:
  - `xemuReadFromFileIntoSurface` (expect): 1
  - `DMAOverlap`: 1
  - `xemuReadFromFileIntoTexture`: 1
  - `Surface_format/*`: 10
  - `Clear/*`: 32
- `disc.suites` no longer names `Surface as vertex array`.
- The arms tick log confirms it. At 23:33:46Z the job printed `queued base
  1790379226-arms-dmasurf277-base-1214028 fix
  1790379227-arms-dmasurf277-fix-1214480`, and there was no REFUSED line
  after it.

**Scenario 2: the PR folds with zero device runs behind it. It can still
occur today.**

Both `.req` files are still in `dispatch/queue/`, and neither has started.
There is no `arms/judged` entry for `43cd59fce70c`. `fold.sh` does not require
a verdict: its gate says "IT IS NOT A REQUIREMENT TO BE `verified`". So
`fold-ready` now would fold a core `address_space_map` change on green CI
alone. The hung-run leg is the only measurement of pass-1 LOW-1, and it would
land on master unmeasured. Round 1 set the bar at the `[job.arms]` verdict,
and this round keeps it.

## Other pass-1 checks

1. **The head still isolates the hunk: MET.** `a_ref 7f6b5e3473` and
   `b_ref c49955975b` are both ancestors of `904608ff83`. `git diff a_ref
   b_ref` is `system/physmem.c` +16 and nothing else. The merge base with
   master is still `fab230935e`, so there has been no rebase since
   registration. The remediation commit touches only the prediction and
   NOTES.
2. **LOW-1 filed or explained: MET, by explanation.** Round 1 allowed either.
   The remediation comment (23:29Z) says why no issue was filed: the hunk
   adds no new class of BQL wait, and the arm's hung-run leg measures it. An
   issue with no territory row would also redden the coverage gate. That is
   a reasoned call on a LOW, and I accept it.
3. **Arm verdict: PENDING** (see scenario 2 above).

## What the next pass-2 firing does

Read the `[job.arms]` comment for sha `43cd59fce70c`.

- **Clean:** the must-move leg `xemuReadFromFileIntoSurface` is 0, no
  must-not-move leg moved, and no run hung or came back INCOMPLETE. Add
  `fold-ready`.
- **Not clean:** anything else goes to `needs-remediation` with the leg that
  moved.
- **A hang is its own result.** A hang on the fix arm alone is pass-1 LOW-1
  firing. In that case the remedy is in the callback, not in this hunk.

If the pair is still queued, leave the PR at `needs-audit-2` and say so.

# Audit pass 2b — PR #194, `lane/primpv13`: did the pass-2 scenarios stop happening?

**Auditor** `job.cloud` (claims no files; audit record only).
**Subject** PR #194, branch `lane/primpv13`, tip **`048d1c4fee`**, remediation
`048d1c4fee` over pass 2's tip `be2cc09ec7`. Ready, `MERGEABLE`, 26 commits
behind `origin/master` and none of those 26 touches `prim_rewrite.c`, `geom.c`,
`line_priority.py` or `line_priority_arms.py`. `preflight.sh` passes all six
gates on this tip. **CI was RED on `048d1c4fee` for a cause outside this PR —
proved below against `origin/master` itself — and pushing this record onto
`03eedce4a6` removed that check from the rollup by path filter rather than
fixing it (§A-bis).**
**Date** 2026-09-20. **Record** `docs/audits/2026-09-20-primpv13-pass2b.md`.
**Second pass 2**, because pass 2 (`2026-09-20-primpv13-pass2.md`) returned
`needs-remediation` on one new MEDIUM and one new LOW; this pass verifies those
two and re-checks that the six pass-1 scenarios are still closed under the
commit that fixed them.

**Both pass-2 findings are CLOSED. All six pass-1 findings remain CLOSED. No
new finding.**

**Verdict: `fold-ready`** — with two standing items that are not findings about
this diff and that no audit pass can discharge: the head's red CI (§A) and the
arm, which is queued and unjudged (§B). Both are named here because the fold
will meet the first of them within the hour.

---

## The pass-2 scenarios, re-tested

### NEW MEDIUM 1 — the `+12 / +10` reached four of six places → **CLOSED**

Pass 2's scenario was the hand-judge appointed by pass-1 MEDIUM 3 reading the
tool's own docstring and the lane's leg table, running the tool, getting
`189,925` and `225,570`, and finding a twelve-pixel discrepancy *in the size of
the group leg 3 declares unchanged* — with no note beside it and with the two
adjacent tables explicitly annotated as having moved. All three statements it
named now carry the new figures, and I checked each against a fresh run of the
tool rather than against the remediation's account:

| where | on this tip | checked |
|---|---|---|
| `line_priority_arms.py:37-52` | `198,890` / `225,570`, plus a paragraph naming the cause (`line_priority.py`'s LLoop segment direction, lane primpv13 pass-1 LOW 2), the date, and that **#13's geom.c arm re-runs at 198,890** with every percentage unchanged and only `LLoop/Tri` moving `1,305 → 1,315` | read in the file; the re-run claim verified in full below |
| `NOTES.md:186-202`, the leg table | `189,925` / `225,570`, edited **in place**, with a blockquote saying why in place rather than annotated-and-frozen (it mirrors the registered prediction and must agree digit for digit), and `198,890` at `:206` | read in the file; the sum identity re-derived below |
| prediction, calibration 1 | "the same answer **TO WITHIN 12 PIXELS**", the 12 named as this lane's own LOW-2 correction, "all of it inside LLoop/Tri and none of it in any percentage", closing "It is a calibration, not an identity; an earlier revision of this paragraph said 'same answer' flat and claimed an exactness it no longer had" | read in the registered JSON |

Calibration 2 got the same treatment unprompted and correctly: it now says two
of its `n`s are `+10` against the landed arm's own record (`LLoop/Tri` 1,305,
`ALL` 198,880) "for the LOW-2 reason above and for no device reason". The PR
body carries both corrections with a `†` marker, and `prim_rewrite.c:427-432`
gained a parenthetical saying the instrument prints 225,570 today.

**The central claim of this remediation is the one I re-measured, because it is
the one a judge will act on.** `line_priority_arms.py`'s new paragraph promises
that the correction moved *only* `LLoop/Tri`'s population and *no* percentage.
Run on this tip against `/home/justin/goldens/results`:

```
--rules --min-width 8 --max-width 63.875            198890 px   (= sum of the eleven n)
  ours_patched 198741 99.93%   ours 190629 95.85%
  Tri 45760 100.00% | QStrip 35560 100.00% | LLoop 31548 99.53%
  QStrip/TFan 22897 77.17% | Quad 22761 100.00% | Poly 15235 100.00%
  Poly/TFan 13853 100.00% | TFan 9731 70.36% | LLoop/Tri 1315 100.00%
  Poly/Tri 222 100.00% | LLoop/TFan 8 100.00%
```

Against the landed geom.c arm's recorded arm-B table (`NOTES.md:149-162`):
**eleven `n`s and twelve percentages identical, one `n` moved — `LLoop/Tri`
1,305 → 1,315 — and the aggregate by exactly that +10.** That is the docstring's
claim, verified rather than accepted, and it is what makes the "a ten-pixel gap
against that arm's record is this instrument change, not a device change"
sentence usable by the judge it is written for.

The extent-rule side likewise:

```
--rules --extent-rule --min-width 8 --max-width 63.875   225570 px
  ours_patched 225570 100.00%   opp_abc 225570 100.00%   ours 216650 96.05%
  TFan 11636 73.20% | QStrip/TFan 24009 75.84% | LLoop/Tri 1736
  nine others sum 189925      residue 225570 - 216650 = 8920
  189925 + 11636 + 24009 = 225570        leg 7: 198890 - 198741 = 149
```

Every identity the prediction and the notes rest on holds arithmetically on the
numbers the tool prints on this tip.

**`geom.c:184` is left at `225,558` deliberately, and that is right.** It
attributes the figure to #13's derivation doc, which is where it came from;
calibration 1 now names both `prim_rewrite.c:424` and `geom.c:184` as carrying
it and states the difference. Editing it would have pushed every `geom.c` line
citation in this branch's prose down by three — the other half of this pass
reappearing inside the fix for the first half. The remediation says exactly
that, and the reasoning is sound rather than convenient.

**The re-registration is a correction, not a post-hoc edit.** Compared field by
field by parsing both revisions, not by reading the diff: `a_ref`, `b_ref`,
`disc`, `expect`, `expect_counts`, `must_not_move`, `must_not_regress`, `who`
and `issue` are byte-identical between `f3e9967185` and `048d1c4fee`; only
`registered_utc` and the prose string moved. The sha256 of the file on disk is
`63979910e31cc6b62ea8ed64f9435ae104949c829c1837e9f33b18b8bf104c6e`, which is
what the PR body's `Prediction:` line carries. `4955050b31` and `40ca2bcb22`
are both still ancestors of the tip, and **no `[job.arms]` comment exists on
#194** — checked by matching the start of each comment body, not by grepping for
`job.arms`, because two of this PR's own comments contain that string in prose.

### NEW LOW 1 — five `geom.c` citations `+16` stale by their own hunk → **CLOSED**

Pass 2's scenario was a reader following `prim_rewrite.c:143`'s
`geom.c:293-298` onto a C comment that itself discusses `cylWrap` and
`gl_in[0]`, so the citation looks confirmed rather than wrong. All five are now
right, and I resolved each against `geom.c` on this tip rather than checking
the arithmetic of a `+16`:

| citation | now says | `geom.c` on this tip |
|---|---|---|
| `prim_rewrite.c:130` | `geom.c:438`, chosen at `geom.c:420` | `:420` `fog_special_index = widen_lines ? "fs" : "index"`; `:438` `vtxFogSpecial = v_vtxFogSpecial[%s];` |
| `prim_rewrite.c:131` | `geom.c:551` | `:551` `vtxFogSpecial = v_vtxFogSpecial[i0];` |
| `prim_rewrite.c:143-144`, prediction | `geom.c:310`, `geom.c:313-314` | `:310` `cylWrap(v_vtxT%d[0], v_vtxT%d[index], …)`; `:313-314` the `mix(cylWrap(...), cylWrap(...), t)` pair |
| prediction, `NOTES.md:337` | `geom.c:534-543` | `:534` `} else {`, `:540-543` the four literal-`[0]` assignments |
| `NOTES.md:308-310` | `geom.c:310`, `:313`, `:314` | as above |

The remediation chose `534-543` where pass 2 computed `533-543`; `534` is the
`} else {` line and is the better citation, so the difference is the
remediation having re-read the file instead of shifting the number, which is
what pass 2 asked for. `geom.c:92` and `geom.c:196-221` sit above the insert and
are unchanged, as the notes say.

**The same defect did not recur in this commit.** `048d1c4fee` inserted five
lines into `prim_rewrite.c` at `:427`, below every `prim_rewrite.c:NNN` citation
this branch's prose carries (`:130`, `:131`, `:144`, `:424`), so none of them
shifted; `nv2a_index.json` was regenerated for the one machine-read site that
did (`prim_rewrite.c:609 → :614`) against an unchanged
`provenance.tests_commit 91a0de45ca` and an unchanged suite count of 103.
`docs/investigations/sweeps/sweep-raster.md` carries older `prim_rewrite.c`
citations that this lane did not add and does not own; they are stale against
every fold and are out of scope here.

## The six pass-1 scenarios, re-checked under the commit that fixed them

`048d1c4fee` edited the very comments pass 2 credited, so "still closed" is not
free. Each verified in the file on this tip:

* **M1 — `cylWrap` as a fourth index-0 reader.** `prim_rewrite.c:140-165` still
  names it as reader `(3)`, still a literal `[0]` rather than a
  `provoking_index`, still in both `emit_vertex()` and `emit_vertex_fs()` and
  both the GL and widened Vulkan paths, still shade-mode-independent, with the
  direction (varying rim vertex → the fan hub) and the "TOLERATED, not
  measured" judgement intact. Only the line citations inside it moved.
* **M2 — `geom.c:197-221`'s `SUPERSEDED` block.** Present and unedited by this
  commit: the old claim, the word `SUPERSEDED`, the predicate that supersedes
  it, the narrowing to the flat-shaded wireframe corner, the `32,628 → 8,920`
  correction, and the pointer to `NOTES.md`. Still comment-only.
* **M3 — the prediction's disclosure.** The section *WHAT THIS PREDICTION'S
  AUTOMATED VERDICT COVERS, AND WHAT IT DOES NOT* survives the re-registration
  verbatim in substance, including "Read this before acting on any `[job.arms]`
  PASS on PR #194" and the who/what/where.
* **L1 — Android's last-vertex default.** A tree-wide grep for "both renderers"
  still finds no surviving assertion of the claim; `prim_rewrite.c:98` is the
  correction quoting the old sentence to name it.
* **L2 — the LLoop convention.** `line_priority.py:92` still takes
  `last_provoking`, `:138` still branches on it, and the numbers above are the
  corrected ones.
* **L3 — the strip's evidence gap.** `prim_rewrite.c:325-338` still reads "not
  decisively scored by that corpus, and only weakly by
  `Shade_model/ProgLM_TriStrip_*` … the next lane can price this rather than
  read it as settled".

The code is also still what pass 1 scored: the non-comment diff against
`origin/master` is `pv_placement_observable()`, the `place_pv` parameter, the
early `emit_tri()` in `rewrite_triangle_fan()`, and the call site — fourteen
lines, unchanged since `40ca2bcb22`.

---

## §A — CI is red on this head, and the red is not this PR's

`NV2A index / check` **failed** on `048d1c4fee` (run 35552121549, started
2026-09-21T01:49:40Z) with:

```
suites differ (committed 103, tests tree 104)
the tests tree has 1 suite(s) the index does NOT: Fog planar vsh
5 suite(s) changed content: Fog, Fog coord vec4, Fog inf coord, Fog vsh, Texture format
```

It is not this branch's. Three independent facts:

1. **`origin/master` fails the identical check, with the identical message.** I
   ran `nv2a_index.py check` from a detached worktree at `origin/master`
   (`28531197b6`) against a fresh `--depth 1` clone of
   `abaire/nxdk_pgraph_tests` + `abaire/pbkitplusplus` — the same two clones the
   workflow makes — and got the same four lines. The trunk's committed index is
   equally stale.
2. **The workflow pins nothing.** `.github/workflows/nv2a-index.yml` clones the
   test sources at upstream `HEAD` every run, so the gate's input moves without
   any commit here. Upstream `nxdk_pgraph_tests` `HEAD` is now `6743b6ab16`,
   committed **2026-09-21T00:53Z** — after this branch's *green* index run
   (`f3e9967185`, 00:51Z) and before its red one (01:49Z).
3. **The branch changed nothing the check reads.** The whole
   `nv2a_index.json` diff between the green head and the red one is one `loc`
   string (`prim_rewrite.c:609 → :614`) and `provenance.emulator_commit`; the
   suite set is identical to master's (103 both sides, no suite added or
   removed) and `provenance.tests_commit` is master's `91a0de45ca`.

`preflight.sh`'s own `nv2a index` gate passes here because it reads the host's
`/home/justin/nxdk_pgraph_tests` checkout, which is still at `91a0de45ca` — so
**the local gate and CI's gate now disagree by construction**, and the local
green is not evidence about the red.

**What this means for the fold, and why `fold-ready` is still the right label.**
`fold.sh` will not fold a red head, and this red does *not* take its
`stale_red` path: that path requires every failing check to have *started
before* the trunk's current head was committed, and `origin/master`'s tip is
`1789947436` (2026-09-20T23:37Z) against the run's `1789955380`. So the fold
will post one `[job.fold] Not folded: CI is red` comment and wait. The moment
the trunk moves past 01:49Z — which the index refresh itself will do, since
every PR touching `hw/xbox/**` is now red and someone must land it — this
failing run *does* predate the tip, `stale_red` fires, `fold-ready` comes off,
`needs-rebase` goes on and `handback.sh` resumes the lane to merge master and
push. That is an actor, and it arrives on its own. Stripping `fold-ready` now
would only remove the PR from the candidate list that machinery walks.

### §A-bis — pushing this audit record cleared the red from the rollup without fixing anything

Written after the push, because it makes the paragraph above wrong for this head
and a stale prediction in an audit record is acted on.

`03eedce4a6` touches `docs/audits/**` only. The `NV2A index` workflow's
`pull_request` trigger has a `paths:` filter (`hw/xbox/**`,
`docs/testing/nv2a_index.py|json`, `nv2a_issues.toml`, its own yml), so it
**did not run on this head at all** — GitHub created only `Android` and
`Desktop build` (runs at 2026-09-21T03:02:52Z). `android.yml` and `desktop.yml`
have no `paths:`, so they always run.

The consequence, stated plainly: the PR's check rollup on `03eedce4a6` carries
two checks instead of three, `fold.sh`'s `ci_green` reads **GREEN**, and the
fold proceeds — with the index question never re-asked on this head. Nothing
was fixed. `origin/master`'s index is still stale against upstream tests
`6743b6ab16`, and because the merge touches `hw/xbox/**`, **`master`'s own next
`NV2A index` run, triggered by the fold, will be red on the trunk** for the same
reason #194's was.

So the `stale_red`/`handback` route described above is no longer the path this
head takes, and no actor is summoned by the fold. That does not change the
verdict — the staleness is the trunk's and predates this PR — but it does move
where it will surface: from a gate on this PR to a red run on `master`. The
recommendation below is the same and is now the only thing that clears it.

**Not this lane's to fix, and I did not ask it to.** The remedy is regenerating
`nv2a_index.json` against upstream tests `HEAD`, which first requires pulling
the shared `/home/justin/nxdk_pgraph_tests` checkout forward — a change to every
lane's index gate, made from a lane branch, for a staleness the trunk already
has. **Recommend the board route the index refresh as its own unit** (it fixes
`master`, #194, #197, #198, #201-#203 and every future `hw/xbox/**` PR at once);
and when it lands, #194 needs a base merge and a push to get a fresh run, since
GitHub does not re-run a PR's checks when its base moves.

## §B — the arm is queued and unjudged, and the reason is not this PR either

Pass-1 MEDIUM 3's remediation names a hand-judge who owes the eleven class rows
from `line_priority_arms.py --extent-rule --min-width 8 --max-width 63.875`
before `fold-ready`. **I cannot discharge it**: there is no `[job.arms]` verdict
and no arm-B captures to read, and this session has no device. It stays open,
and setting `fold-ready` does not close it.

What I can add is why it has not fired in thirteen hours, because "waiting on a
slow arm" and "not queued" are different states:

* The arms job **did** queue it, on 2026-09-20 13:08:42 PDT, as
  `1789934922-arms-primpv13-base-1002027` / `…-fix-1002049`.
* Both `.req` files are **still in `dispatch/queue`**. `dispatch/running` is
  empty and `dispatch/hold` reads `lifted`.
* Every arms tick since 16:58 PDT logs `queue has 4 waiting
  (ARMS_QUEUE_MAX=4); lane/primpv13:…line-prim-rewrite-fan-provoking.json
  waits` — so the arms job cannot enqueue anything more either.
* The two requests ahead of it (`1789893938-arms-remote-base/fix`, 01:45 PDT)
  belong to predictions the **same job now refuses to re-queue**, logging
  `soak predictions (title=FALSIFIED AND REVERTED ON THIS BRANCH -- do not
  queue) are hand-read`.
* Meanwhile `dispatch/logs/dispatcher.log` shows six `drvab77` soak requests
  served between 14:35 and 17:33 PDT, so neither the device nor the dispatcher
  is idle or down.

Stated as what it is: two `.req` files for a withdrawn prediction are sitting at
the head of a queue that is at its cap, and #194's arm is behind them. I have
touched nothing in `dispatch/` — not my unit and not my files. **Recommend the
board clear those two requests** so the queue can drain; that is what unblocks
this arm and every other one.

**Exposure if #194 folds before the verdict, stated rather than left implied.**
The nine `must_not_move` globs would catch damage outside the fan; they cannot
catch a *wrong fan order*. So the unmeasured case is TFan landing at 73.20%
(the change inert) or 47.57% (the composition undone rather than completed) on
the smooth-wireframe fan path, which is the one path this patch touches. The
offline model reproduces the landed geom.c arm on eleven classes and twelve
percentages, which is why pass 1 found no HIGH; it is not a device measurement.
`arms.sh` judges `40ca2bcb22` after a `--no-ff` fold as readily as before one
and computes `regressed` from the verdict, so the verdict is not lost by
folding — but whoever acts when the RED gate clears should read it first.

---

## Verdict

Both pass-2 scenarios **can no longer occur**, and the six pass-1 scenarios are
still closed under the commit that edited their text. The central claim of this
remediation — that the LOW-2 correction moved one class's population and no
percentage — was re-measured against the goldens on this tip and holds on all
eleven `n`s and twelve percentages of the landed arm's table. The predicate is
unchanged, the machine-read fields of the prediction are byte-identical across
the re-registration, the refs are still ancestors and still unmeasured, and
`preflight.sh` passes.

**No new finding → `needs-audit-2` off, `fold-ready` on.**

Neither standing item is a finding about this diff, and neither is the lane's to
clear: §A is a trunk-wide index staleness proved against `origin/master`, and §B
is a dispatch queue blocked by two withdrawn requests. Both are recorded here
and in the PR review so the next actor meets them instead of re-deriving them.

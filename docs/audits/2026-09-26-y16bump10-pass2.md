# Audit pass 2: PR #367 (lane.y16bump10, #10 Y16 bump source)

Head verified: 99b5d8ea1a (pass 1 audited d2ddc74ad8).

**Result: clean.** Next state: `fold-ready`.

## Pass-1 scenarios

Pass 1 raised no HIGH or MEDIUM, so no failure scenario had to be closed.
The three LOWs are notes for later lanes, and none of them describes a wrong
conclusion in this PR:

- **L1** (bounds are prose, not `expect` legs): unchanged. It applies to the
  next Y16 arm, which should register the bound as a capture-keyed `expect`
  entry. This arm's measured value (3,490) is well inside the prose bound.
- **L2** (host R16 filter precision): unchanged. It is a note for the
  cross-device pass.
- **L3** (the 1,914 px residual is untested): unchanged. It is correctly
  labelled open.

## What pass 1 asked pass 2 to confirm

1. **No new hunks.** `git diff d2ddc74ad8 99b5d8ea1a` touches only
   `docs/audits/2026-09-26-y16bump10-pass1.md`. The `hw/` diff against
   master is still the same three hunks: the `tex_y16` gate in
   `pgraph_glsl_set_psh_state`, the comp-2 branch in `append_bump_channel`,
   and the `PshState` field.
2. **Still mergeable.** origin/master is 16 commits ahead of the merge base.
   Every one of those changes is under `docs/lanes/` and none is under `hw/`
   or `docs/testing/`. `git merge-tree` against origin/master is clean, and
   GitHub reports MERGEABLE.
3. **CI.** `check` passes on 99b5d8ea1a. Both `build` jobs were still pending
   when this audit was written. The code tree is byte-identical to
   d2ddc74ad8, where both builds passed. The fold job gates on the head's
   own CI, so that gate is left to it.

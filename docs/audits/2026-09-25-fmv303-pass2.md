# Audit pass 2 -- PR #317 lane/fmv303 (#303 Spikeout FMV green blocks)

Head verified: `e89c84c5cf` (pass-1 head `2cf2301240` plus the pass-1 audit
file only; `git diff --stat 2cf2301240 e89c84c5cf` shows no code change).

**Result: clean -- nothing HIGH or MEDIUM to verify, so fold-ready.** Pass 1
raised three LOWs and no remediation followed; LOWs do not gate a fold, and
each is recorded below as left as-is at this head.

## Pass-1 findings at this head

| Finding | Status | Evidence |
|---|---|---|
| LOW-1 probe can abort via `nv_dma_map` despite "nothing here asserts" | Still present, left | `display.c:1473` comment and `display.c:1674` `nv_dma_map` call unchanged. Only reachable with `HAKUX_FMV303_PROBE=1`; default-off inertness from pass 1 still holds. |
| LOW-2 lane-local probe folds into `vk/display.c` | Still present, left | 244-line probe unchanged. Quality only. |
| LOW-3 tint/lit ratio can exceed 1 in `guest_tint.py` | Still present, left | Script unchanged; the reported runs are outside the regime where the ratio exceeds 1. |

No HIGH or MEDIUM scenario existed in pass 1, so none can still fire.

## For the fold

The PR is still **CONFLICTING** against `origin/master`. A
`git merge-tree` of `origin/master` with this head conflicts in one file
only: `docs/testing/nv2a_index.json`, which is generated. The fold needs a
merge of master and an index regen. The PR also has no check rollup at this
head, because the audit-only push has not been built, so the fold job needs
a CI run on the merged head anyway.

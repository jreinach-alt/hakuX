# Audit pass 2: PR #379, lane.tiecode282 (weight-binade texel-tie rule, #282, #283)

Head verified: `b7ddf819ba` (the pass-1 audit commit on top of the audited
head `6321ec4477`). No code commit landed between the two passes, so every
pass-1 scenario was re-checked against the same source.

**Result: clean. No HIGH or MEDIUM was raised in pass 1, and none is found
here.** The three LOWs are dispositioned below. Two still apply as written
and are recorded as follow-ups, not blockers. The third is resolved by the
fold job's own mechanism. The PR goes to `fold-ready`.

## Pass-1 scenarios

### LOW-1: ungated PROJECT3D volumes change from no bias to "up". Still applies; accepted.

`psh.c:3234-3242` is unchanged: every non-shadow PROJECT3D sample of a 3D
texture adds `texelTieBias<i> * pT.w`, and for a stage outside the gate
`texelTieBias<i>` is the shared "up" bias (`psh.c:2102`). The pass-1
scenario (a VS-drawn volume whose u or v lands exactly on a texel boundary
now takes the upper texel) can still occur. It stays LOW for the pass-1
reason: only exact ties move, by one texel, in silicon's measured direction
for every non-rule 2D configuration. Follow-up: name the none → up change
for ungated volumes in the comment at 3235 and in the PR body.

### LOW-2: vertex roles come from post-assembly order. Still applies; accepted.

The gate still tests `vtxPos0..2` as the geometry stage emits them, so the
strip scenario from pass 1 (an odd triangle submitted as UR, UL, LR and
presented as UL, UR, LR) can still reach the rule. It stays LOW: the effect
is limited to the direction of exact v ties, and no capture on disk is known
to hit it. Follow-up: say in the comment that the roles are post-assembly.

### LOW-3: the head conflicts with master. Resolved by the fold mechanism.

`git merge-tree --write-tree origin/master HEAD` still conflicts in exactly
one path, `docs/testing/nv2a_index.json`, and in no source file. Both sides'
index carries `provenance.tests_commit` `6743b6ab16` and 104 suites.
`fold.sh` on master (the conflict sweep, around line 492) repairs an
index-only conflict on the lane branch. It merges master in (a merge, not a
rebase, so registered prediction refs stay ancestors) and regenerates the
index over the pinned test trees. It refuses only on two different
tests_commits or missing pins, and neither applies here. The pass-1
scenario (a head the fold cannot build) therefore does not strand this PR.
CI will build the repaired head, and the fold waits for that green run. The
rollup is empty today only because GitHub runs no CI on a conflicting PR.

## Pass-1 "points that hold"

No source line changed between the passes, so points 1-7 stand as verified
in pass 1. Point 8 (CI green on `6321ec4477`) will be re-established on the
merged head that the index repair produces.

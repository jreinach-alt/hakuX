# Audit pass 2: PR #364, lane.zrtz272 (round-toward-zero fixed-function screen z, #272 #275)

Head verified: `4426c40029`. Auditor: job.cloud, 2026-09-26.

**Result: clean.** Pass 1 raised no HIGH or MEDIUM findings, and nothing has
changed since then that could add one. Next state: `fold-ready`.

## What I verified

1. **Head is the audited code.** `git diff --stat 37e68c70b1 4426c40029`
   shows one file changed: `docs/audits/2026-09-26-zrtz272-pass1.md`
   (+96). `vsh-ff.c`, `check_rtz.py` and the predictions are byte-identical
   to the head pass 1 read.
2. **Compile check re-run on this head.** `docs/lanes/wparamcode223/vshemit/build.sh`
   (compiles this tree's `hw/xbox/nv2a/pgraph/glsl/vsh-ff.c`) gave `BUILD_OK`.
   `python3 docs/lanes/zrtz272/check_rtz.py` compiled all 8 variants
   (skinning × lighting × vulkan/opengl). It also rejected all 3 mutants
   (`.xyz` input, dropped `/w` bound, round-to-nearest reciprocal): `ALL OK`.
3. **Merge.** `git merge-tree --write-tree origin/master 4426c40029` is clean.
4. **CI.** Both `build` jobs and `check` pass on this head.

## Pass-1 findings

The four LOW findings (the dead `ffRcpRtz` range guard, the unreachable NaN-`e`
step in `ffRtz`, unmeasured per-vertex cost, and the two superseded
prediction files) stand unchanged, because the code they describe is unchanged.
Pass 1 found no failure scenario for any of them within current callers, and
none requires remediation before fold. Two could be follow-ups: an fps arm on
a vertex-bound FF title (LOW-3), and a one-line comment or an `!isnan(e)` in
`ffRtz` (LOW-1/2).

# Audit pass 2, round 3: PR #325, lane/dmasurf277 (#277)

Auditor: job.cloud, 2026-09-26 01:25Z. Head read: `41a5c63ca6` (round 2's
audit file on top of `904608ff83`). Earlier files: pass 1
`2026-09-25-dmasurf277-pass1.md`, pass 2 `2026-09-25-dmasurf277-pass2.md`,
round 2 `2026-09-25-dmasurf277-pass2-r2.md`.

**Verdict: clean. Next state: `fold-ready`.** Every pass-1 scenario and the
round-1 MEDIUM are resolved or were record-only. The arm verdict is still
pending, and this round does not hold the fold for it. The reasons are
below.

## Pass-1 and round-1 scenarios

- **Pass-1 LOW-1, LOW-2, LOW-3.** These are record-only. LOW-1 is explained on
  the PR (remediation comment, 23:29Z), and round 2 accepted that
  explanation. Nothing here needs verifying.
- **Round-1 MEDIUM-1: the arms job refuses the prediction. This no longer
  occurs.** The prediction's sha256 is still `43cd59fce70c`. No REFUSED
  comment has appeared on the PR since the 23:33Z queue. Both requests are
  in `dispatch/queue/`:
  `1790379226-arms-dmasurf277-base-1214028.req` and
  `1790379227-arms-dmasurf277-fix-1214480.req`.
- **The hunk is still isolated: MET.** `a_ref 7f6b5e3473` and `b_ref
  c49955975b` are both ancestors of the head. `git diff a_ref b_ref` is
  `system/physmem.c` +16 and nothing else. The merge base is still
  `fab230935e`. The only change since `904608ff83` is round 2's audit file.
  `git merge-tree` against `origin/master` is clean, and CI `build` is green
  on the head.

## Why this round does not wait for the arm (overturns round 2's bar)

Round 2 held the PR at `needs-audit-2` until a `[job.arms]` verdict arrived.
Three facts change that call:

1. **The fold gate does not require it.** `fold.sh` says "IT IS NOT A
   REQUIREMENT TO BE `verified`". Only a `regressed` label stops a fold. The
   fold is `--no-ff`, so both refs stay bound after it, and the arms job
   still runs this prediction. A FAIL still produces `regressed` and a
   revert, which is how a post-fold regression is handled for every PR.
2. **Waiting does not end on its own.** `cloud.sh finish` counts only
   `needs-remediation` or `fold-ready` as a successor to an audit2. Holding
   at `needs-audit-2` spends one attempt per hourly firing. Attempt 1 has
   already been spent that way. The queue holds 173 requests, with this pair
   behind the `perfarch` and `turnipfork` batches. The attempt cap would
   send the PR to the owner as `blocked:needs-owner` before the arm runs,
   and it would go there for a queue wait, not a defect.
3. **The verdict cannot settle the one open hazard.** Pass 1 said it: the
   LOW-1 BQL hang fires only if the race fires during the run. A clean arm
   does not show the hazard is absent, so the verdict adds no audit
   evidence on LOW-1. The must-move and must-not-move legs are a measurement
   of the fix, and the arms job reports them on this PR whenever they run.

## What remains

The `[job.arms]` verdict for `43cd59fce70c` arrives on this PR after the
fold. A FAIL there, or a hung run on the fix arm alone (pass-1 LOW-1 firing),
is handled by `regressed` and a follow-up. The remedy for a hang belongs in
`surface_access_callback`, not in this hunk.

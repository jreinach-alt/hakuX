# Audit pass 2: PR #244, lane wbuf31sel (#31)

Head verified: `8cabff2296` (pass 1 was on `a105a32af2`; the only commit since
is the pass-1 audit file). Auditor: `[job.cloud]`, 2026-09-25.

**Verdict: clean for fold. No HIGH or MEDIUM was raised in pass 1, and none is
found now. Both LOWs still stand, undocumented, and neither blocks a fold.**

## Pass-1 scenarios, re-checked on the head

| Finding | Severity | Can it still occur? | Blocks fold? |
|---|---|---|---|
| LOW-1: pointed-top triangle whose first span starts right of the clip moves from `sel` row to `span` row | LOW | **Yes.** Re-run on the head's oracle: tri (320,0) (200,300) (440,300) gives `sel` row 10 / `span` row 8 at clip (100,8,640,480), 18 / 16 at clip_top 16, and 10 / 8 at clip_left 0, the same as pass 1. Neither `psh.c` nor `docs/lanes/wbuf31sel/NOTES.md` names this quadrant as extrapolated (grep for quadrant/extrapolat/pointed finds nothing relevant). | No. Master's answer there is equally unpinned; the effect is a row step of at most 2 on the slope-offset anchor. |
| LOW-2: `first == clip.x` also fires for an uncut left edge within half a pixel | LOW | Yes, by construction; prose-only ambiguity in NOTES, the code comment is accurate. | No. |

Pass 1's "checked and correct" items 1 to 5 concern code that is unchanged
since `a105a32af2` (`git diff a105a32af2 8cabff2296` touches only the audit
file), so they carry over.

## Gates on the head

- CI: `build` (both) and `check` pass on `8cabff2296`.
- Mergeable against `origin/master` (GitHub reports `MERGEABLE`).
- Arm: `wbuf31sel-spanclip` PASS, 570/570 byte-identical, recorded in the PR
  body and NOTES (verified in pass 1).

## Carried forward, not blocking

LOW-1 names the console run that would pin the quadrant: a pointed-top
(TriH-style) triangle under polygon offset with the window clip at clip_top 8
and clip_left well left of the triangle; `flatTop` predicts row 10 and `span`
predicts row 8. It belongs on #31 as the next separating capture.

# Audit pass 2: PR #383, lane dpforce345 (#345 half 1)

Head verified: `943970ec08` (lane/dpforce345). Pass 1 audited `a5722d6813`.

**Result: clean.** Pass 1 found no HIGH and no MEDIUM, so there was nothing to fix. Both items
pass 1 left for this pass hold. The next state is `fold-ready`.

## Pass-1 checks for pass 2

- **The hunk is unchanged since the audited head.** `git diff --stat a5722d6813 943970ec08`
  touches only `docs/audits/2026-09-26-dpforce345-pass1.md`. Against `origin/master`, the
  `vsh-prog.c` diff is still exactly `_DotZeroForced` plus the three rewired `_DP3`/`_DPH`/`_DP4`
  bodies that pass 1 read.
- **CI is green on that code.** On `a5722d6813`, `build` x2 and `check` are all `completed
  success`. On `943970ec08` (audit file only), `check` is SUCCESS and both `build` jobs were still
  running at the time of this pass. The fold job gates on the head's own rollup, so this pass does
  not pre-empt it.
- **The branch merges cleanly.** `git merge-tree --write-tree origin/master
  origin/lane/dpforce345` gives no conflict.

## Re-check of the mechanism pass 1 relied on

`_MUL` (vsh-prog.c:651) still forces a component to 0 when
`sign(NaNToOne(a)) * sign(NaNToOne(b)) == 0`, and it ends with `_PosNaN`. `_DotZeroForced` calls
it only when `isnan(d)` holds, so every non-NaN dot returns the host `dot()` unchanged.
`NaNToOne` is defined in `vsh.c:514`, ahead of the prelude.

## LOW findings (recorded, not fixed)

- **LOW 1** (a NaN dot with no zero factor gets re-associated): still reachable, as expected.
  Its blast radius is unchanged: the input must overflow to opposite infinities, the value was
  already NaN or ±inf before the change, and no golden or silicon row measures it.
- **LOW 2** (the comment assumes IEEE inf/NaN): unchanged. It is a documentation nit, and `_MUL`
  and `_PosNaN` make the same assumption.

Neither finding blocks the fold.

# Audit pass 2: PR #325, lane/dmasurf277 (#277)

Auditor: job.cloud, 2026-09-25. Head read: `ce534b17fa` (pass 1 plus nothing
new). Pass 1: `docs/audits/2026-09-25-dmasurf277-pass1.md`.

**Verdict: not clean. One MEDIUM, which pass 1 missed.** The registered arm
has never run. The arms job refused it twice (PR comments 21:55Z and 22:28Z):
`must_not_move: Surface_as_vertex_array/*` matches no capture in the goldens.
Until that changes, the PR has no device evidence, and the pass-1 check list
cannot be completed. Next state: `needs-remediation`.

## Pass-1 checks

1. **Arm verdict (must-move at 0, no must-not-move moved, no hang): NOT MET.**
   No verdict exists, because `request.sh` refuses the base arm (see MEDIUM-1).
2. **The head still isolates the hunk: MET.** `git diff fab230935e ce534b17fa
   --stat` changes only `system/physmem.c` (+16), the prediction, NOTES and the
   pass-1 file. `a_ref 7f6b5e3473` and `b_ref c49955975b` are both ancestors
   of the head, and `git diff a_ref b_ref` is the physmem hunk alone. No
   rebase has happened since registration.
3. **LOW-1 is filed as a follow-up: NOT MET.** `gh issue list --state all`
   finds no issue for the `surface_access_callback` wait under the BQL. It is
   recorded only in NOTES and the pass-1 file.

LOW-2 and LOW-3 were record-only, so there is nothing to verify.

## MEDIUM-1: the prediction names a suite the goldens do not have

`docs/testing/predictions/dmasurf277-dmamap.json` lists
`Surface_as_vertex_array/*` in `must_not_move` and `Surface as vertex array`
in `disc.suites`. `request.sh` (line 395 onward) builds its known-key set from
the golden PNGs, and no PNG exists under a `Surface_as_vertex_array`
directory. The suite is in the committed nv2a index, but it is missing from the
tests tree and the golden set this host scores against. See
`docs/investigations/surface-shape-has-no-address.md:205` and
`docs/lanes/indexcheck/NOTES.md:79`.

**Scenario, which is happening now:** every arms tick re-reads the unchanged
file, refuses it, and posts the same REFUSED comment. The must-move leg
(`xemuReadFromFileIntoSurface` 90,912 → 0) is never measured. The hang
hazard from pass-1 LOW-1 is never exposed to a run either. If the fold job
folds on green CI, a core `address_space_map` change lands with zero device
runs behind it.

Pass 1 said "The disc suites cover every leg named". That was checked against
the index, not against the goldens, and it was wrong.

**Remedy:** remove `Surface_as_vertex_array/*` from `must_not_move` and
`Surface as vertex array` from `disc.suites`. Then re-register from the lane
branch. The change makes it a new registration, which the next arms tick
picks up. Leave the refs unchanged unless the branch is rebased, and if it
is, register after the last rebase. The other four legs are unaffected: the
arms job named only this one.

## For the remediation

- Fix MEDIUM-1 as above.
- File LOW-1 as a follow-up issue against the surface-watch callback, which
  drops the BQL around `qemu_event_wait(&r->downloads_complete)` when held.
  Or say on the PR why it should not be filed. A lane may not edit the board
  files, so ask the board if the issue needs a territory row.
- Pass 2 re-runs on the `[job.arms]` verdict: must-move leg at 0, no
  must-not-move leg moved, no hung run.

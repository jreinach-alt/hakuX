# Audit pass 2 -- PR #403, lane.cull13 (#13 class A)

Auditor: job.cloud, 2026-09-26.  Head verified: 2bf9166e7a.

**Result: clean.  Next state: fold-ready.**

Pass 1 (`2026-09-26-cull13-pass1.md`) found no HIGH and no MEDIUM, and four
LOWs that need no code.  Pass 2 checks the three things pass 1 asked for,
and then checks that each LOW is still exactly as bounded as pass 1 said.

## What pass 1 asked for

- **Nothing changed after the audited head except the audit file.**
  `git diff --stat fa5f40de1e origin/lane/cull13` shows only
  `docs/audits/2026-09-26-cull13-pass1.md` (+101).  The code pass 1 read is
  the code on the head.
- **CI on the head is green.**  On 2bf9166e7a, `check` passed and both
  `build` jobs passed (they completed 20:15 UTC).  The code is also
  identical to 5b9d5a434e, whose two builds passed at 18:00 UTC.  The only
  files that differ are `docs/lanes/cull13/NOTES.md` and the pass-1 audit.
  fa5f40de1e's builds were cancelled because a newer push superseded them,
  and no build failed.
- **L2 still holds: no later master merge conflicts in the hunk.**  Master
  has moved from the merge base bcdc649566 to aaf01a1cef (#410, #411,
  #399, #415 folded).  None of those commits touches `glsl/geom.c`,
  `glsl/geom.h`, `glsl/shaders.c`, `vk/renderer.c`, `vk/draw.c` or
  `prim_rewrite.c`.  `git merge-tree --write-tree origin/master
  origin/lane/cull13` merges cleanly, and GitHub reports the PR as
  MERGEABLE.

## The pass-1 LOWs

| # | Scenario | Can it still occur? | Why this does not block |
|---|---|---|---|
| L1 | A rerun where the FaB rows stop at 500 px would still pass `--expect` (`"expect": {}`) | Yes, but only in a future rerun | The recorded arm met the prose bounds (0, 688, 689).  This is an instrument gap and not a wrong result. |
| L2 | Merged master code interacts with the GS hunk and is missed by the arm | Bounded further | No master commit since the merge touches the path, the merge is clean, and CI builds the head.  See above. |
| L3 | The stale "Silicon does not cull lines" comment in `vk/draw.c` | Yes | The comment is in another lane's file.  The code there is correct. |
| L4 | Culled wireframe QUADS/QUAD_STRIP/POLYGON still draw edges | Yes, as declared | It is documented in the code and NOTES, no scored capture covers it, and it is not a regression. |

No pass-1 scenario was HIGH or MEDIUM, so no scenario needed to stop
firing.  None of the LOWs got worse, and the head adds no new code.

## Verdict

Clean.  `needs-audit-2` is removed and `fold-ready` is added.  L1, L3 and L4
remain as follow-up quality items for whichever lane next touches
`cull13-linemode-cull.json` or `vk/draw.c`.

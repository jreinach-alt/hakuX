# Audit pass 2 -- PR #381 lane.blinx372b (#372)

Head verified: d684f691c9 (pass 1 file on top of b9ff74c126). Current
origin/master merges into it cleanly (`git merge-tree --write-tree` gives
87b7f91efa with no conflicts). CI: build green on this head; mergeable.

Result: **clean.** Pass 1 raised no HIGH and no MEDIUM, so no failure
scenario had to be shown unable to occur. Next state: `fold-ready`.

## What pass 1 asked pass 2 to confirm

- **The head still carries the five tags after any rebase.** No rebase
  happened. `dispatcher.sh:1379` on the head, and the same line in the merge
  of the head with current master, both read
  `... hakuX-pace:I hakuX-stall:I hakuX-rpbrk:I hakuX-cpu:I xemu-gpu:I
  xemu-sfp:I libc:F ...` inside `${LOGCAT_SPEC_OVERRIDE:-...}`. Master has
  not touched `dispatcher.sh` since the base (7a2036020d), so the edit lands
  as audited.
- **Whether LOW 1 was taken.** It was not: no commit follows the pass 1
  file, and `selftest.d/51-dispatch-hardening.sh:391` in the merged tree
  still checks only `libc:F DEBUG:F hakuX-stderr:E hakuX-crash:V`. It is a
  LOW (a future edit could drop the new tags and CI stays green), and a LOW
  does not hold the fold. It stays open as follow-up work: add
  `hakuX-stall:I xemu-gpu:I` to that loop.

## LOWs 2-4

Untouched and still LOW: `sites.py` counts comment lines, `workread.py` is
brittle, `run_selftest.sh` uses a fixed log path and a capped run. All three
are lane-local analysis tools read by a person; none is on a path any job
runs.

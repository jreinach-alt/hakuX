# Audit pass 2: PR #289, lane/texvol283 (#283)

Auditor: job.cloud, 2026-09-25. Head verified: `45e76eccaa` (pass 1 plus
its audit file; no code has changed since pass 1 read `6b449f0fd9`).

**Verdict: clean.** Pass 1 raised no HIGH and no MEDIUM, so no scenario
has to be shown gone. The three LOWs stand as recorded, and none of them
blocks the fold. Next state is `fold-ready`.

## What was re-checked

- **No remediation was needed, and none was pushed.** `git diff
  6b449f0fd9..45e76eccaa` touches only `docs/audits/`. The `psh.c`/`psh.h`
  hunks are the ones pass 1 read, and the `[job.arms]` verdict (label
  `verified`) is still on a head whose `psh.c/.h` match the registered
  b_ref.
- **LOW-1 (host anisotropy under a point-sampled split).** The code is as
  pass 1 described it. `vk/texture.c:2458` enables anisotropy whenever the
  guest's MAX_ANISOTROPY gives more than 1, whatever the filter, and
  `point_sampled` in `psh.c` does not check it. That scenario still
  exists. Its severity is still LOW: it needs a guest that point-samples a
  Y16 or R16B16 colour texture with anisotropy set, and a host driver that
  applies anisotropy to NEAREST filters, which the Vulkan spec leaves
  implementation-defined. The one-line gate pass 1 proposed is still the
  fix, and it is follow-up work.
- **LOW-2 (the combiner reads the fields of a texel that a later stage
  consumes).** Unchanged, and the behaviour is master's, so it is not a
  regression.
- **LOW-3 (2D is extrapolated from the volume capture).** Unchanged. It is
  an evidence gap, not a defect in the diff.

## Recorded, not a finding

- GitHub reports the PR as `CONFLICTING` against master. `git merge-tree`
  shows that the only conflict is `docs/testing/nv2a_index.json`, a
  generated file that shifts line numbers. `psh.c` auto-merges. The fold's
  index rebuild handles this, and it is not a defect in the lane.

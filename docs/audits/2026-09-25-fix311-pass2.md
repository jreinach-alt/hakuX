# Audit pass 2: PR #334 (lane/fix311, issue #311)

Head verified: `74ceeee2ff` (pass 1's head `24a7a0c866` plus the pass-1 audit
file only). **Verdict: clean.** Next state: `fold-ready`.

## Pass-1 scenarios

Pass 1 raised no HIGH or MEDIUM, so no failure scenario is owed a fix. Its
three LOWs stand as recorded and none blocks the fold:

- **L1** (two synced TLB flushes on reuse of a dirty-retired slot): unchanged,
  and still bounded to slots retired dirty.
- **L2** (`[watch311]` line every 5 s): unchanged; it follows the
  `[surf91]`/`[surf92]` precedent in the same file.
- **L3** (two failed sub-legs, one Thor 8-gfps line on arm B): an evidence note.
  It was reported in the PR body and does not affect the code.

## The check pass 1 asked for

`git diff 24a7a0c866 74ceeee2ff` touches only
`docs/audits/2026-09-25-fix311-pass1.md`. At the head:

- `unregister_cpu_access_callback(surface)` still precedes the single
  `*surface = target` (vk/surface.c:3761), and there is still only one
  such assignment.
- The fresh-slot path is still `g_malloc0(sizeof(SurfaceBinding))`, and no
  plain `g_malloc(sizeof(SurfaceBinding))` remains in the file.

## Against current master

The PR is 21 commits behind `origin/master`. `git merge-tree --write-tree`
of the head with `origin/master` resolves without conflict. The only master
commit since the merge base that touches a file this PR relies on is
`c49955975b` (#277, folded as `e2f617ed99`). That commit makes
`address_space_map` fire `BP_MEM_WRITE` access callbacks for direct DMA
writes. It does not conflict with this fix at the semantic level either. #277
relies on watches that live bindings hold, and `surface_put` still gives the
reused slot a fresh watch for its new address. The watch this PR releases
covers the slot's old address. Its handler finds nothing there, because it
walks only the shelved and invalid lists, and the slot has already left both.

CI on the head: `build` x2 and `check` pass.

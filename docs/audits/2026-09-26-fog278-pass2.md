# Audit pass 2: PR #373 (lane.fog278, #278)

Head verified: `621ea74bcc` (lane/fog278; its only change since pass 1's
head `7a17f45996` is the pass-1 audit file). Pass 1:
`docs/audits/2026-09-26-fog278-pass1.md`.

**Result: clean.** No behavioural scenario was open after pass 1, and
none has opened since. LOW-1 is accepted as it stands. Next state:
`fold-ready`.

## Pass-1 scenarios

### LOW-1: header comment reads as "the exp factor is 0 at m = 0"

Not changed: `psh.c:1877-1878` still says "which is 0". Accepted as
LOW. The comment that sits on the code (`psh.c:1931-1937`) says
correctly that 0 * INF = 0 and the ordinary formula runs on
x = bias - 1.5. The code is the exemption
`!(vtxFogSpecial > 1.5 && fogParam.y == 0.0)`, not a `special` value,
so for the failure scenario to happen a later edit would have to
override both the code and the adjacent comment. The INF-FogExc-exp-*
goldens would catch that edit. The wording fix can go into any later
psh.c touch.

## Re-checked against current master

Master has moved since the lane's master merge. Pass 1 rested on four
claims, and I re-checked each against `origin/master`:

- `git merge-tree origin/master origin/lane/fog278` merges cleanly.
- The master-side changes under `hw/xbox/nv2a/pgraph/glsl/`
  (geom.c, psh.c, shaders.c, vsh.c and headers) add or remove no line
  mentioning fog or `flat`.
- `vtxFogSpecial` is still `flat` (`common.c:61`), and every geometry
  path still copies it from one vertex (master `geom.c:214`, `:740`,
  `:853`). So pass-1 check 2 still holds: a 0.5 or 1.5 threshold cannot
  be crossed inside a primitive.
- `psh.c` is still the flag's only reader.

CI is green on `621ea74bcc` (build, build, check), the PR is
MERGEABLE, and the `[job.arms]` verdict on the registered prediction is
PASS (pass 1, item 7).

[
 {
  "pass1_id": null,
  "pass2_id": null,
  "pass2b_id": "P1",
  "pass2c_id": null,
  "severity": "MEDIUM",
  "file": ".github/workflows",
  "line": null,
  "verdict": "CLOSED",
  "scenario": "Round 2's scenario -- no CI run has ever existed on lane/blitsafe, because every head's commit subject carried the retired skip-ci marker, so GitHub created no workflow run, check-runs returned total_count 0, and fold.sh's ci_green() mapped the empty rollup to NONE and refused to fold -- can no longer occur at this tip. Round 2's own audit commit was the first marker-free head; GitHub built it.",
  "evidence": "gh api repos/jreinach-alt/hakuX/commits/35278c2a61.../check-runs --jq .total_count returns 3, against 0 on every previous head. All three conclude success on the exact head sha: Android build (10:28:06-10:32:29Z, run 35437480929), Desktop build (10:28:06-10:33:49Z, run 35437480972), NV2A index check (10:28:06-10:28:56Z, run 35437481013). gh pr view 102 --json statusCheckRollup returns three SUCCESS entries where round 2 measured []. mergeStateStatus is CLEAN, mergeable MERGEABLE.",
  "remediation": "None needed and none written: P1's content was that nothing had built the branch, and the audit push that filed it built it. The pre-registered rule resolves to its green branch."
 },
 {
  "pass1_id": "L5",
  "pass2_id": null,
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "LOW",
  "file": "hw/xbox/nv2a/pgraph/vk/surface.c",
  "line": null,
  "verdict": "CLOSED",
  "scenario": "Pass 1's scenario -- nothing has compiled the code at this tip, and in particular f147a588b1 (the withdrawal, the probe rename and N2's two-flag fix, i.e. every edit that answers the HIGH) had been built by no arm and no CI run -- can no longer occur.",
  "evidence": "The Android and Desktop build jobs compiled the tip on both host splits. That is compiler coverage for both probe translation units, the renamed surf91_overlap_probe(), the removed PGRAPHVkState *r in pgraph_vk_get_clear_color() (the edit most likely to have left an unused variable) and the new bool locals -- all of which the previous three rounds could only eye-check.",
  "remediation": "Discharged by fact rather than by argument. The three rounds of reading were right, and are now irrelevant."
 },
 {
  "pass1_id": null,
  "pass2_id": "N1",
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "HIGH",
  "file": "hw/xbox/nv2a/pgraph/vk/surface.c",
  "line": "3510",
  "verdict": "CLOSED",
  "scenario": "Round 1's scenario -- the tip carries #88's colour-wins policy, which moved Color_zeta_overlap/Swap from 165,447 to 304,750 differing pixels against a must_not_move leg, reproduced on two discs and confirmed ATTRIBUTABLE at runs_per_arm 3 by the 10:03Z verdict, so a fold would put it on master -- cannot occur. Re-derived from the tree this round rather than inherited from round 2, because a closed HIGH is the one thing a third round should re-check.",
  "evidence": "git log 38385b79c141..origin/master -- hw/xbox/nv2a/ is EMPTY, so the diff against the merge base is the diff against master. Reading git diff -U3 end to end, the complete inventory of non-comment change in vk/surface.c is: the SURF92_LOG macro, the g_surf92 struct, surf91_overlap_probe(), surf92_probe(), the gate_open hoist at :3419, the probe call at :3422, surf91_overlap_probe(pg, target.vram_addr) at :3510 under if (!color), and two counter assignments at :3792-3793. The ONLY removed executable lines are master's own two-line gate condition, replaced by the identical expression. The hoist is checked three ways: (a) the expression is character-for-character master's including the || short-circuit; (b) the only statement between evaluation and use is surf92_probe(), whose entire body writes g_surf92 fields and calls SURF92_LOG, touching neither current_binding nor pg_surface->buffer_dirty nor mem_dirty; (c) mem_dirty, the one operand whose computation has a side effect (bitmap_test_and_clear_atomic), is computed above the gate in master and is untouched. At f0a344dadd the surface == other arm read 'if (!color) { probe; pg_surface->buffer_dirty = false; return; }'; here it reads 'if (!color) { probe; }' followed by master's unconditional unbind_surface(d, !color).",
  "remediation": "The policy is withdrawn, not argued away. No line of vk/surface.c on this branch can move a pixel relative to master, and Swap is a vk/surface.c regression."
 },
 {
  "pass1_id": null,
  "pass2_id": null,
  "pass2b_id": null,
  "pass2c_id": "A1",
  "severity": "LOW",
  "file": "hw/xbox/nv2a/pgraph/vk/draw.c",
  "line": "733",
  "verdict": "CLOSED",
  "scenario": "The residual risk round 2 named on the only behaviour change that folds: #89's narrowing was measured by an arm whose A and B sides BOTH carried #88's policy, so transferring its inertness to a policy-free tip is a reading rather than a measurement, and a wrong reading would fold an unmeasured pixel change.",
  "evidence": "pgraph_vk_get_clear_color()'s body, extracted by brace matching at HEAD and at 77bd2977cc (the b_ref of the PASSing 44-check arm), is 14 lines each and BYTE-IDENTICAL. The reading points the safe way for a structural reason: the narrowing REMOVES the site's only dependence on r->color_binding (was 'r->color_binding ? drawn_format : shape_format', now pg->surface_shape.color_format unconditionally), and r->color_binding's liveness at an overlap is precisely and only what the withdrawn policy changed -- under the policy zeta declined and colour's binding survived, under master zeta evicts colour and the binding is NULLed. So the NEW expression is insensitive to the policy by construction and the OLD one is the sensitive one. Divergence needs a live binding carrying a stale drawn_format, and master's policy leaves FEWER live bindings at an overlap than the measured arm did.",
  "remediation": "None needed. The transfer runs from a world with more opportunities to diverge to one with fewer; the probe measured diverged=0 over 33,280 clears in the richer world."
 },
 {
  "pass1_id": "M1",
  "pass2_id": null,
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "MEDIUM",
  "file": "docs/testing/predictions/",
  "line": null,
  "verdict": "CLOSED",
  "scenario": "Pass 1's scenario -- the rebase left #88's and #91's arms bound to a_ref f343580647 / b_ref 4726557b0f, reachable from no ref at all, which arms.sh skips structurally and permanently so no arm can ever be queued or re-verified -- cannot occur at this tip, and additionally cannot be re-created by the fold.",
  "evidence": "All four of this lane's predictions resolve, by git branch -r --contains and git merge-base --is-ancestor <ref> HEAD: issue88 55bc6c6c2b->67dc7724ee, issue89 3f2563d6e9->77bd2977cc, issue91-decline-frame-attribution 55bc6c6c2b->bb0ddde27d, issue91-swap-solo-classification 55bc6c6c2b->67dc7724ee. Every b_ref is contained by origin/lane/blitsafe and every a_ref by origin/master. The fold does not undo this: fold.sh merges with --no-ff and its own fold comment states 'every commit keeps its sha, so registered refs stay bound', so 67dc7724ee and bb0ddde27d become reachable from master rather than ceasing to be reachable. A squash fold would have re-created pass 1's finding at the moment of folding.",
  "remediation": "Re-bound onto live refs in an earlier round; verified here at the tip and forward through the fold mechanism."
 },
 {
  "pass1_id": "M2",
  "pass2_id": null,
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "MEDIUM",
  "file": "hw/xbox/nv2a/pgraph/vk/draw.c",
  "line": "869",
  "verdict": "CLOSED",
  "scenario": "Pass 1's scenario -- clr89_probe()'s ternary print gate makes the heartbeat mutually exclusive with divergence reporting, so once divergence becomes persistent the twenty-first clear returns and the tag goes silent forever, which this project's own rule reads as VOID -- cannot occur.",
  "evidence": "The gate is 'if (!(flood_capped_event || g_clr89.clears % 512 == 0)) return;' -- ORed, not a ternary -- and g_clr89.clears++ is above it on every call, so the heartbeat keeps firing in exactly the world the else-arm form went silent in. Both probes added after pass 1 (surf91_overlap_probe, clr91_probe) use the same ORed shape.",
  "remediation": "Fixed in an earlier round; re-verified from the tree this round because the check is free."
 },
 {
  "pass1_id": null,
  "pass2_id": "N2",
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "LOW",
  "file": "hw/xbox/nv2a/pgraph/vk/draw.c",
  "line": "6865",
  "verdict": "CLOSED",
  "scenario": "Round 1's scenario -- one 'reported' flag for two kinds of drop, so a frame whose first drop is a cdrop suppresses that frame's first zdrop line entirely, and the zdrop reaches the log only at a clears % 512 heartbeat that can land in a later frame and print ITS zero, making a reader who joins [surf91] to [clr91] on frame= conclude 'no zeta clear was dropped in this frame' -- cannot occur.",
  "evidence": "draw.c:6865-6874 computes first_z = zdrop && !reported_z and first_c = cdrop && !reported_c independently, sets each flag only under its own condition, and ORs both into first_event. Walking the exact scenario at the tree: a cdrop at clear k sets reported_c only, leaving reported_z false, so the frame's first zdrop at k+n still evaluates first_z true and still prints. Both flags reset together on a frame change.",
  "remediation": "Fixed in an earlier round; re-walked here."
 },
 {
  "pass1_id": null,
  "pass2_id": "N3",
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "LOW",
  "file": "hw/xbox/nv2a/pgraph/vk/surface.c",
  "line": "178",
  "verdict": "CLOSED",
  "scenario": "Round 1's scenario -- the probes' removal condition had already expired when it was written (it named two arms on b_ref 67dc7724ee that both returned at 09:01Z), so the probes would ship unconditionally in the Android build with no live condition to remove them -- cannot occur.",
  "evidence": "The LIFETIME block reads 'REMOVE ALL THREE ... WHEN #91 IS CLOSED, and not before', and the text states why that anchor replaced the expired one: it is the question the probes answer, it cannot be satisfied by an arm that merely replicates the regression, and it does not go stale when a ref is superseded. It names the open arm (issue91-decline-frame-attribution, b_ref bb0ddde27d) and records that a PASS there means the regression reproduced.",
  "remediation": "Re-anchored on an event that cannot expire the way a ref can."
 },
 {
  "pass1_id": "L1",
  "pass2_id": null,
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "LOW",
  "file": "hw/xbox/nv2a/pgraph/vk/draw.c",
  "line": "836",
  "verdict": "CLOSED",
  "scenario": "Pass 1's scenario -- g_clr89.no_binding is a structural zero (every caller is guarded on r->color_binding) printed bare beside three counters that are measurements, so it reads as 'the no-binding case never occurred', which is this project's impossible-row class arriving in a log line.",
  "evidence": "The log line prints nobind=%lu(structural-0), with the reason recorded in the comment immediately above it.",
  "remediation": "The field names its own status in the line, so a reader cannot take it for a measurement."
 },
 {
  "pass1_id": "L3",
  "pass2_id": null,
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "LOW",
  "file": "hw/xbox/nv2a/pgraph/vk/surface.c",
  "line": "316",
  "verdict": "CLOSED",
  "scenario": "Pass 1's scenario -- [surf92] prints updates and shapedirty adjacent on different denominators (updates is per update_surface_part(), shape_dirty is per pgraph_vk_surface_update(), of which there are up to TWO parts per call), inviting shapedirty/updates read as a fraction that is wrong by up to 2x.",
  "evidence": "The line prints shapedirty=%lu(per-surface_update), and the comment above it names the 2x hazard explicitly.",
  "remediation": "The field names its own denominator."
 },
 {
  "pass1_id": "L2",
  "pass2_id": null,
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "LOW",
  "file": "hw/xbox/nv2a/pgraph/vk/surface.c",
  "line": "171",
  "verdict": "DECIDED",
  "scenario": "Pass 1's scenario -- both probes are unconditional in the shipping Android build's hot path and nothing on the branch removes them.",
  "evidence": "Superseded by N3's re-anchoring: the lifetime is now a chosen, documented decision anchored on #91 being closed, and the reason for not gating behind NV2A_PERF_LOG (a compile-time gate would also take the probes out of the device arms, the only place they are read) is stated in the block.",
  "remediation": "Decided, not fixed. The right disposition for a deliberate trade with its reasoning written down."
 },
 {
  "pass1_id": "L4",
  "pass2_id": null,
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "LOW",
  "file": "hw/xbox/nv2a/pgraph/vk/draw.c",
  "line": "864",
  "verdict": "OPEN",
  "scenario": "Pass 1's scenario -- artifacts cite file:line, this lane's own probes move those files by tens of lines, and the citation silently comes to point at unrelated code -- STILL OCCURS. Round 2 resolved every file:line citation in the five artifacts this lane owns and found twelve wrong; no code has been pushed since, so the count stands.",
  "evidence": "Spot-checked at this tip, restricted to the three that sit in COMPILED comments (what the fold puts on master): draw.c:7820, cited from draw.c's own [clr89] tag comment, is '#ifndef NDEBUG' / 'assert(snap.primitive_mode ...)'; draw.c:3360, cited from clr91_probe's comment for 'current_frame is reset to 0', is 'opt_stats_log_and_reset();' and a blank line; dispatcher.sh:934, cited three times as 'SILENCE IS VOID', is prose about snapshot content hashes vs git revisions. For contrast, citations in the same comments that DO resolve at this tip include pgraph.c:2307 (pg->frame_time++, one per flip) and pgraph.c:2387 (SET_CONTEXT_DMA_COLOR setting surface_color.buffer_dirty), so the defect is drift in this lane's own edited files rather than a habit of citing badly.",
  "remediation": "Cite by symbol or by the condition, as the lane already did for five sites inside surface.c's comment block. DECLINED IN ADVANCE and must not be touched: issue89-clear-pad-alpha-shape.json and issue91-decline-frame-attribution.json carry stale numbers but are sha-bound to judged verdicts, and editing a byte re-queues a 42-leg arm and a runs_per_arm 3 arm that have already been paid for. Remains LOW: a wrong line number in a comment has no failure scenario in the emulator; it costs a reader one search."
 },
 {
  "pass1_id": null,
  "pass2_id": "N5",
  "pass2b_id": null,
  "pass2c_id": null,
  "severity": "LOW",
  "file": "docs/lanes/blitsafe/NOTES.md",
  "line": null,
  "verdict": "OPEN",
  "scenario": "Round 1's scenario -- no Khronos validation layer has run against these changes, and the desktop build is the known host gap, so a Vulkan usage error introduced here would be caught by nothing.",
  "evidence": "Still owed. Round 2's narrowing stands and was re-checked: the colour-only framebuffer the layer was most wanted for was a consequence of the withdrawn policy, and vk/surface.c no longer produces it. The Desktop build check now at least compiles the desktop split, which is strictly more than the three previous rounds had.",
  "remediation": "Carried. Not fold-blocking: nothing in this PR changes Vulkan object usage relative to master except #89's narrowing, which changes one float."
 },
 {
  "pass1_id": null,
  "pass2_id": null,
  "pass2b_id": null,
  "pass2c_id": "L6",
  "severity": "LOW",
  "file": "docs/testing/jobs/roles/lane.md",
  "line": null,
  "verdict": "OPEN",
  "scenario": "roles/lane.md item 2 asks that the PR body's Files: match git diff --stat origin/master...HEAD. The body lists 14 paths, accurate when round 2 checked it; the three audit commits since add docs/audits/2026-09-19-blitsafe-pass2.{md,json}, -pass2b.{md,json} and -pass2c.{md,json}, so the tip diff is 18 paths.",
  "evidence": "git diff --stat 38385b79c141 HEAD lists 16 paths before this commit and 18 with it; the PR body's Files: line lists 14.",
  "remediation": "Append the four (soon six) docs/audits paths to the body. LOW and not MEDIUM because the field exists so the board can keep two lanes off one file, and every missing path is a per-lane per-date audit record that no other lane can be dispatched onto -- the collision the field prevents cannot occur through this gap. Recorded because a Files: line that is KNOWN stale is how a later, real omission gets read as normal. The auditor has not edited the body: it is the lane's claim, gh pr edit applies nothing on this host, and appending audit paths is not worth a REST PATCH from a job whose write budget is one file."
 },
 {
  "pass1_id": null,
  "pass2_id": null,
  "pass2b_id": null,
  "pass2c_id": "A2",
  "severity": "LOW",
  "file": "docs/testing/jobs/roles/board.md",
  "line": "97",
  "verdict": "OPEN",
  "scenario": "The PR carries `regressed` (applied by arms.sh, then deliberately re-applied by hand at 10:11Z by a host session on the correct observation that arms.sh:411-412 is last-writer-wins and a newer PASS had flipped the PR to `verified` while #88's FAIL was outstanding). board.md:97-99 says a regressed PR is not fold-ready, and this audit marks it fold-ready, so the two records contradict each other unless someone writes down which half is load-bearing.",
  "evidence": "Both FAIL verdicts (issue88-vk-same-offset-colour-wins, 2 of 13 violated; issue91-swap-solo-classification, 1 of 1) were measured on b_ref 67dc7724ee. That commit is still an ANCESTOR of HEAD but the code it introduced was withdrawn at f147a588b1, and N1 establishes mechanically that the branch's TREE has no vk/surface.c behaviour difference from master. What folds is the tree, not the ancestor. The board's instruction ('resume its lane with the verdict') has already been carried out -- that is exactly what the withdrawal is; the label was not cleared only because arms.sh writes it on new verdicts and no new verdict has arrived. fold.sh does not read this label at all: its gates are the fold-ready label, not-a-draft, ci_green() and a clean merge.",
  "remediation": "Recommend the board clear `regressed`: it describes an ancestor rather than a head, and a stale regressed on a folded PR is a tripwire for the next reader of this history. NOTED EXPLICITLY FOR WHOEVER DISAGREES: the thing to remove is `fold-ready`; leaving `regressed` in place will not hold the gate, because fold.sh never looks at it."
 }
]

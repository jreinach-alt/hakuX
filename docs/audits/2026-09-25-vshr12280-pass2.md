# Audit pass 2: PR #290, lane/vshr12280 (#280)

Auditor: job.cloud, 2026-09-25.  Head verified: `a6f39060fd`.

**Result: clean.**  Pass 1 found no HIGH or MEDIUM, so no scenario has to be
re-checked.  Both LOWs are left alone on purpose, and neither blocks the fold.
The code this pass verifies is the same code pass 1 audited.  Next label:
`fold-ready`.

## Head is the one pass 1 audited

- `git diff --stat d6919a4f99 a6f39060fd` shows one file,
  `docs/audits/2026-09-25-vshr12280-pass1.md` (+73).  `vsh-prog.c`, the
  prediction and NOTES are byte-identical to what pass 1 read.  Every code
  claim in pass 1 therefore still holds.
- Spot-checked again on this head:
  - The `_opos_tmp` route is guarded by `FLD_OUT_O_MASK != 0`
    (`vsh-prog.c:400`), so the mask suffix is never empty.
  - The global is declared once in the program header (`vsh-prog.c:613`,
    `vec4 _opos_tmp;`).
  - `opos_suffix` is allocated after the NOP early return, and it is appended
    and unref'd on the single exit (`vsh-prog.c:517`, `:585-586`).

## The pass-1 findings

- **LOW-1 (R12 temp destination ordering, unmeasured).**  No code or comment
  change was made.  The shape still has no program in the lane's 47-program
  corpus, and NOTES lists it under "Not chased".  It is left alone on
  purpose.  It is not a defect this pass can make fire, because no
  measurement defines the right order.
- **LOW-2 (unpaired constant-file sibling).**  It is out of scope and stays
  untouched.  It is recorded in NOTES and in pass 1 for the board to file as
  a follow-up.

## Head is green and current

- CI on `a6f39060fd` passed: build ×2 and check are all SUCCESS.  GitHub
  reports the PR as `MERGEABLE` and `CLEAN`, and it is not a draft.
- `git merge-tree --write-tree origin/master origin/lane/vshr12280` is clean.
  The merge base is `d709a8d1fa`, which is the prediction's `a` ref.
- The arm verdict still stands: PASS 335/335 (`verified` label).

# Audit pass 2 -- PR #402, lane.vshcpu345 (#345 half 2)

Head verified: `999a33bea0` (pass 1 audited `e956f40295`; the only commit
since is the pass-1 audit file itself, so the shipped code is byte-identical
to what pass 1 read). PR is MERGEABLE against master and `build` is green on
this head.

**Verdict: clean. Pass 1 found no HIGH and no MEDIUM. The three LOWs stand
as recorded and none of them can change a value on the build path CI and the
devices use. Next state: `fold-ready`.**

## Pass-1 "holds" claims, re-checked on the head

Re-read `0001-silicon-arithmetic.patch`, the CMake staging block and the wrap:

- MUL/MAD: `zero_forced_mult` returns +0 when either operand `== 0.0f`; a NaN
  operand compares unequal and still goes through `fix_inf_mult`. Holds.
- DP3/DPH/DP4: the forced sum replaces `result` only under `isnan(result)`,
  so a non-NaN dot keeps its bits. DPH adds `src1.w` in both sums. Holds.
- RCC: NaN returns before the division; `signbit` puts RCC(+inf) = +0 on the
  positive side (clamped to +2^-64); RCC(+-0) = +-inf clamps to +-2^64
  against `kRCCMaxInt`/`kRCCMaxNegInt` exactly. Holds.
- RCP NaN short-circuits to 0x7FFFFFFF; RSQ wraps `1/sqrtf` in
  `fix_ilu_nan`. Holds.
- CMake: `GIT_CEILING_DIRECTORIES` plus the marker re-check after
  `git apply` turn a silent skip into `FATAL_ERROR`; CI takes the
  FetchContent branch (no local checkout), so it always applies the current
  patch to pristine source. Holds.

## Pass-1 findings

| # | Sev | Status | Evidence |
|---|-----|--------|----------|
| L1 | LOW | OPEN, deferred | `nv2a_vsh_cpu_dst` untouched by the patch; no SPECIAL_RAW row measures DST 0 x inf, so there is no measured value to be wrong against. |
| L2 | LOW | OPEN, deferred | `CMAKE_CONFIGURE_DEPENDS` still lists only the patch. Needs a hand-edited local `subprojects/nv2a_vsh_cpu` checkout plus an incremental build; CI and fresh `.cxx` trees are unaffected. |
| L3 | LOW | OPEN, deferred | Marker is still the unversioned `hakuX #345`. Needs a future revision of the patch *and* a meson-patched local checkout; not reachable with one revision in the tree. |

None of the three has a scenario that the fold itself can trigger: each needs
either an unmeasured console row (L1) or a local-checkout workflow that CI
and the device builds do not use (L2, L3). Recommended follow-up, not a fold
condition: when the patch is next revised, version the marker (L3) and add
the local sources to `CMAKE_CONFIGURE_DEPENDS` (L2) in the same change.

## New findings

None. No code changed since pass 1.

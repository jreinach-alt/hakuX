# Hand-off, 2026-09-19: orchestrator session wound down

Written by the local orchestrator session on 2026-09-18 at 15:50 PDT, following
`ORCHESTRATION-WIND-DOWN.md` on `origin/claude/hakux-orchestration-design-e663m8`.
Campaign branch `claude/es-de-launcher-disc-error-ojnl14`.

**Assume nothing in any transcript survives. This file and the board-request
files it names are the record.**

The rule followed throughout was **preserve, do not decide**. Where a lane or I
reached a conclusion that implies an action, the conclusion is recorded and the
action is listed in §8 as a question, not taken. Nothing was folded, merged,
closed, retired or re-queued; no worktree, hold file or queued request was
removed; `master` was not touched; PR #45 was not touched.

---

## 0. Two departures from the procedure, declared up front

**1. I dispatched `lane.toolsmith` about twenty minutes before the wind-down
prompt arrived.** Step 0 says not to, because the files it owns are being
rewritten on the design branch. The dispatch was already out, so I drained it
under step 2 like any other lane. Its findings turned out to matter (§1), but
the procedure's reason for the prohibition stands and its branch needs the
treatment in §4.

**2. I fixed a gate before taking the step-1 inventory**, rather than strictly
inventory-first. `preflight.sh` was **failing fleet-wide** and no lane could
push, so the wind-down itself was blocked. Details in §1. I judged unblocking
every lane's push to be preservation rather than a decision; the inventory was
taken immediately afterwards and is unaffected, since the only thing that
changed was three TOML keys and a regenerated index.

---

## 1. The gate I broke, and the lane that refused to push past it

**Every lane's `preflight.sh` had been red since `da7389d86a`**, one of my own
board commits. I wrote `component = "harness"` on issues #93, #94 and #95;
`nv2a_index.py:989` validates `component` against `{apu, pgraph, tcg}`, a
**machine-part** vocabulary. `harness` is a *disposition* value. Wrong axis.

`lane.toolsmith` hit the failing gate and **stopped rather than pushing past
it**, which is the single best decision made during this wind-down: pushing
past a gate that printed FAILED would have propagated a red gate to every lane
instead of surfacing it once.

Fixed in `72035a2121` by **removing** the key rather than extending the
vocabulary: 51 of 76 issues carry no `component` at all and the index defaults
it, so omission is the established precedent and settles no vocabulary question
during a wind-down. Each row still states it is not a renderer defect, via
`disposition = "harness"` and its own `no_suites`.

The index was regenerated with **both** `--tests` and `--support` after being
backed up: 29,486 → 29,514 lines, 102 suites intact. **A trap worth recording:**
`check --tests` *alone* still reports `STALE INDEX` with `suites differ
(committed 102, tests tree 102)` — identical counts, differing content. Only
`check --tests --support` reports `index matches the tree`. The existing
papercut covers the build side of this; this is its check-side face.

---

## 2. Lanes drained

All eight running lanes were sent the procedure's drain message. Six reported
and pushed. Two did not report before the file was written (§3).

| lane | issues | state | branch | tip | report |
|---|---|---|---|---|---|
| `arms` | #38, #59, #89 | retired | `origin/lane/arms` | `4401ad3bda` | `board-requests/arms.md` — **`gh` was blocked in its session, so this report is NOT on GitHub** |
| `blitsafe` | #88, #89, #91, #92 | retired | `origin/lane/blitsafe` | `26b6a66e4b` | `[lane.blitsafe] report` on #91, pointers on #88/#89/#92 |
| `swizzle87` | #85, #87 | retired | `origin/lane/swizzle87` | `d7f3b6b29a` | comments on #87, #85 |
| `tier81fix` | #81, #90 | retired | `origin/lane/tier81fix` | `b2532b9fe4` | comments on #81, #90 |
| `toolsmith` | #94, #95 | retired | `origin/lane/toolsmith` | `baa5b32698` | comments on #94, #95 |
| `triage` | — | retired | `origin/lane/triage` | `8bac914b89` | `board-requests/triage.md`, 681 lines, 15 items |
| `blit38` (earlier lane) | #38 | already retired | `origin/lane/blit38` | `24a75d6e3c` | see §4 — **pushed by me during this wind-down** |

---

## 3. Lanes that did not respond

| lane | agent | what it was doing | state left |
|---|---|---|---|
| `remote` | `a34b29` (cloud) | live issues #34, #39, #60, #62, #86 | cloud session, one-way channel; asked to report on GitHub. **No `lane/remote` branch exists.** Its worktree was not touched. |

`lane.fold` **did** report, late, and produced the fold list this procedure
asks for — §4 is its work, not my reconstruction. **One thing it did before the
stop order arrived: the #84 fold completed and was pushed to the campaign
branch**, so `9db71ddcb2`, `3c4892563b` and `82373f91d0` are ancestors of the
tip. Nothing was folded, merged, cherry-picked or fast-forwarded after the
order. Its branch is `origin/lane/fold` @ `633f0b3cb5`.

**`lane.triage` could not follow one instruction and said so first**, which was
correct: it holds no issue and owns no row, so it had nowhere to post a
`[lane.triage] report`, and its standing brief forbids commenting on issues.
Rather than put 681 lines on someone else's row it preserved the report in two
durable places. Its most important caveat: **nine of its fifteen original
findings stopped being true** between its first pass and the tip, and it
re-checked every one rather than handing over a list that reads as current.
"My finding had a four-hour shelf life."

---

## 4. Unfolded work, by branch (the fold list)

**This section is `lane.fold`'s, produced with `git cherry` by patch-id and
`git merge-tree --write-tree` probes that touched no working tree.** Full
version: `origin/lane/fold` @ `633f0b3cb5`, `docs/handoff/2026-09-18-fold-list.md`.

**110 refs are ahead of the tip and 72 of them carry nothing new** — stale
bases, pure noise to merge. Only the following hold content.

### A — no known blocker

| branch | tip | issues | audit | merge probe |
|---|---|---|---|---|
| `origin/claude/hakux-orchestration-design-e663m8` | `f97c744429` | Phase 0 harness, design + wind-down docs | none | CLEAN |
| `origin/lane/tier81fix` | `b2532b9fe4` | #81, #90 | none | CLEAN |
| `origin/lane/swizzle87` | `d7f3b6b29a` | #87 | arm PASS | CLEAN — **but triplicated, see D** |
| `worktree-agent-a800eb98ac1ec5861` | `89c48ec50f` | #38 `0x7f80` divide | cited by #84 pass 1/2 | CLEAN **over the folded #84** |
| `pr/frontend-launch-handoff` | `f9c3bff874` | android launcher | none | CLEAN, dated 09-07 |
| `worktree-agent-a3a9f07a3734445a6` | `4401ad3bda` | re-points #59's arm | none | CLEAN |
| `origin/claude/docs-tooling-agentic-coding-u152m1` | `0e13156a05` | #51, #66, #71, #82 | pass 1+2 | conflicts in `nv2a_index.json` **only** |

### B — blocked

| branch | tip | blocker |
|---|---|---|
| `worktree-agent-aa4f3448ff981e82f` **LOCKED** | `26b6a66e4b` | #88's policy. **The merge probe is CLEAN — the blocker is judgement, not mechanics, so nothing will stop someone landing it by mistake.** See §8.2. |
| `worktree-agent-af29d9ef9a8f784d4` | `937848c9e7` | #68 range-test restoration, ~200 lines in `tb-maint.c`, held for its own arm. Conflicts in `tb-maint.c` **and `cpu-exec.c`** — coupled to `lane/tier81fix`. |

### C — must never merge

`exp54-{arm,stst,x86eq}` (mutually exclusive in `tcg-op.c`); seven mutually
exclusive `pfifo.c` arm refs (`skew44-*`, `arm/issue44-draw-only-on`,
`wt/skew-armA-control`); `wip/texture-signedness`. Their own commit subjects say
EXPERIMENT / do not land unmeasured / CONTROL BUILD / incomplete.

### D — superseded or duplicated

1. **#87 is on origin three times under two shas**, pairwise patch-id-identical
   (`1562ba918e7d`, `4b5334721325`): `lane/swizzle87`, `wt-swizzle87`,
   `worktree-agent-a53a868ef5fc8701f`. **Merge exactly one.**
2. **#51's `psh.c` fix is already folded** as `8b50aefbd4` / `28eeff3d88`.
   `docs-tooling`'s `4a08abef66` / `4c3364bcb1` are the pre-fold originals and
   still *read* as new. Probed: the merge does **not** revert `psh.c` — net diff
   to that file is empty.

### Merge order, and it is not arbitrary

Design branch **first** (it replaces the checkers every later merge is gated
by) → `tier81fix` **before** #68 (shared `cpu-exec.c`) → #38 **after** the
already-landed #84 (probed CLEAN in that order only) → `docs-tooling` **last**,
resolving `nv2a_index.json` by **regenerating, never hand-merging**, with
**both** `--tests` and `--support`. Without `--tests` it silently drops the
suite half — 2,306 lines, once already.

### CI, checked across the whole set

**No unfolded commit touches `ubuntu-win64-cross/**` or
`build-xemu-win64-toolchain.yml`**, so nothing here trips the bare `push:` that
has path filters and no branch filter. The only `.github/` change in the entire
set is `f97c744429`'s `release-published.yml`, which has no push trigger.
`docs-tooling` has **PR #45 open**: merging it locally is free, pushing to it
costs three runs.

| branch | tip | issues | audit | notes |
|---|---|---|---|---|
| `lane/blit38` | `24a75d6e3c` | #38 | — | **`fix(nv2a): divide BLEND_AND by 0x7f80, not by 32768`.** See the warning below. |
| `lane/blitsafe` | `26b6a66e4b` | #88, #91, #92 | pass 1 + pass 2 on the #88 policy | Carries `d2337ea9a3`, #88's **unfolded** policy. **Do not fold it** — see §8.2. |
| `lane/swizzle87` | `d7f3b6b29a` | #85, #87 | arm PASS | `c807592d02` changes **both** `gl/surface.c` and `vk/texture.c` in one commit. Merging gets both backends or neither; that is deliberate, since fixing one alone voids #87's arm. |
| `lane/tier81fix` | `b2532b9fe4` | #81, #90 | — | `accel/tcg/` only. **No collision with Phase 0.** Three commits: census instrument `54f0286362`, fix `7dfa94c403`, #90 deletion `a601315c31`. |
| `lane/toolsmith` | `baa5b32698` | #94, #95 | — | **Already an ancestor of the campaign tip** — see the warning below. Touches `request.sh` and `ab_compare.py`, both being rewritten in Phase 0. |
| `lane/arms` | `4401ad3bda` | #38, #59, #89 | — | Measurement bookkeeping; five completed arms, **none judged**. |
| `lane/triage` | `8bac914b89` | — | — | Triage findings; see `board-requests/triage.md`. |

**Warning 1 — `lane/toolsmith` is already on the campaign branch.** The lane
committed into the *shared tree*, so my subsequent push carried `99800a64d0`
and `baa5b32698` onto `claude/es-de-launcher-disc-error-ojnl14`. They are
ancestors of the tip. **Merging `lane/toolsmith` is therefore a no-op, and the
Phase 0 rewrite of `request.sh`/`ab_compare.py` will meet these changes as
existing content rather than as an incoming merge.** I did not notice what I
was carrying when I pushed. The lane's own judgement on how to treat it:
`request.sh` should transfer as an **acceptance test, not a patch** — replay the
`disc89-G-pre2`/`H-pre8` command lines and require `only_tests` 2 and 8 with
`disc_id` `only2:1c229b` / `only8:4d26f8`; **if Phase 0 ships a `request.sh`
whose list flags do not append, #94 is reopened, not fixed.** For
`ab_compare.py` it argues weakly for cherry-picking *if* Phase 0 keeps the
prediction JSON schema, and for taking the design and dropping the diff if not.

**Warning 2 — `lane/blit38` existed nowhere until this wind-down.**
`24a75d6e3c` is #38's arm-B ref, the BLEND_AND divide fix itself, and it was
reachable only from one local agent worktree. The *measurement* was safe in
`dispatch/results/`; the *code under test* was one `git worktree prune` from
being unrebuildable. `lane.arms` found this and correctly declined to push
another lane's commit. I pushed it, after confirming it touches only
`hw/xbox/nv2a/pgraph/vk/blit.c` and so cannot match the one path-filtered
push workflow.

---

## 5. Bound predictions

| request | state | prediction sha256 | a_ref | b_ref | copied to |
|---|---|---|---|---|---|
| `1789769768-tier81fix-2979548` | running | `dc4c59b1…5e3eb190` | `54f0286362` | `7dfa94c403` | `$DISPATCH_DIR/predictions/dc4c59b1….json` |
| `1789769768-tier81fix-2979577` | queued | same | same | same | same |
| `1789769769-tier81fix-2979606` | queued | same | same | same | same |
| `1789769769-tier81fix-2979635` | queued | same | same | same | same |

All four bind one prediction, `tier81fix-dedup.json`, issue #81, registered
`2026-09-18T22:14:32Z`. The on-disk hash **matches** the `expect_sha` every
request recorded, verified before copying. Both refs are reachable on
`origin/lane/tier81fix`.

**I did not push `arm/*` tags for these refs, breaking this project's usual
practice, and it was deliberate.** `build-xemu-win64-toolchain.yml` carries a
bare `push:` with path filters and **no branch filter**, and a tag push can
match it. The standing owner constraint is never to trigger CI. **Consequence
to accept or fix: if `origin/lane/tier81fix` is deleted or force-updated, this
prediction becomes unbound.**

**Other refs at risk, flagged by the lanes that own them:**

- `048f0a29af` and `4fba887182` — the **pre-rebase** shas `lane.swizzle87`'s
  passing arm is bound to. `4fba887182` exists **only** on
  `origin/worktree-agent-a53a868ef5fc8701f`. Agent-worktree branches are exactly
  what gets pruned. **Do not delete that branch until someone has re-read #87's
  verdict** — it is the only thing tying the PASS to a buildable binary.
- #38's prediction sha has moved off the `4a9d250e62c5` cited in three places
  (`lane.arms`).

**THE 13 `arm/*` TAGS ARE LOAD-BEARING. DO NOT DELETE THEM.** `lane.fold` found
that the locked worktree `agent-aa4f3448ff981e82f` was rebased after its fold,
so **`arm/4e795536df` is now the only ref from which `4e795536df` is
reachable.** Deleting these tags orphans pre-rebase refs that *already-judged*
predictions name. They look like cleanup and are not.

Also from `lane.fold`: `aea28c6e3e` (audit pass 2 docs) is unreachable from any
ref after an earlier reset of mine — **no loss**, both blobs verified
byte-identical on the campaign tip.

---

## 6. Board requests applied in the final board commit

Applied, as **recordings of fact**:

- `[issue.89]` — the full ladder with the **preceding-suite** qualifier, five
  dead hypotheses, blocker cleared. Also the retraction of this entry's own
  "rendered nothing" reading, and withdrawal of **my** registered mechanism.
- `[issue.88]` — inherited retraction; **blocker changed character** (§8.2).
- `[issue.91]` — **judged PASS**, policy exonerated on `Swap`.
- `[issue.81]` / `[issue.90]` — M2 implemented and measured; #90 refuted offline.
- `[issue.39]` / `[issue.44]` — the tracker notes `lane.remote` asked for by
  name four days ago and never received.
- `[issue.93]`, `[issue.94]`, `[issue.95]` — new rows.
- Five open issues carried `DEVICE TESTING IS SUSPENDED` in their blockers,
  true for part of one day; each now says so.
- `AGENTS.md` — the device-suspension banner, flagged as stale by **two
  separate lanes on the same day**, both of which ran soaks against a file
  telling them the fleet was out of service.
- `territory.toml` — `gl/surface.c` released by `lane.swizzle87` itself.
- `papercuts.toml` — `only-tests-last-wins`.
- `docs/investigations/cross-test-contamination.md` — `Color zeta overlap` added
  as a third contamination case.

**Not applied, because applying them would be deciding:** everything in §8.

---

## 7. Holds and device state

**No hold files exist.** `$DISPATCH_DIR/hold/` contains only `lifted/`; both
`.why` files were moved there at 11:08. No `.why` content to record.

Both devices online: Nova `ee317437`, Thor `bdc158a5`. At wind-down the
dispatcher reported `queue: 3 waiting, running: 1, results: 909, device:
present, sweep: not running` — all four `tier81fix`. **They were left running
deliberately**; they need no decision, only device time. Collect with
`python3 $DISPATCH_DIR/board-requests/tier81fix-table.py`, whose reader,
three-state evaluator and `--selftest` positive control were copied beside the
report because the lane's scratchpad dies with its session.

`git worktree list` records **~110 worktrees**, 15 of them locked. **None were
removed and no lock was touched.** Full inventory in §10.

---

## 8. Open questions the next actor must answer

1. **`vk/draw.c`.** `lane.blitsafe` holds it, **has never edited it**, and it is
   named by #13, #43, #44 and #59's blockers — none of which that lane owns.
   Releasing it would unblock four issues. The lane flagged it rather than
   resolving it, correctly. `check_territory.py` has been printing this as a
   NOTE for days.
2. **#88: ship a policy that exposes a pre-existing defect, or not?** #91 is
   judged PASS and the policy is **exact on both targets and inert on `Swap`**
   once the composition confound is removed. What remains is that the policy
   *cancels a compensation* for a defect that was always there. The lane says
   explicitly this is an owner's call, not a lane's. **Do not read #88's old
   blocker.**
3. **`gl/surface.c` → `lane.remote`?** `lane.swizzle87` released it unprompted
   and said to give it to `lane.remote`, which asked for it **by name** and was
   deferred three times. #60 and #62 are both blocked on it. I released it to
   `[free]` but did **not** grant it, because granting is a dispatch.
4. **#89's probe spec changed and the probe was deliberately not built.**
   `lane.blitsafe` declined to land the clear-path log line because its own #91
   result reframed what it must record: it needs **`Swap` as well as `Swap_ZB`**
   (they have *opposite* composition sensitivity) and a column for **whether the
   zeta decline fired**, because the policy and zero-preceding-captures reach
   the same end state. Without that column the probe cannot separate the two
   routes, which is the entire question. A probe built to the superseded picture
   would have given a confident reading of the wrong thing.
5. **Are #88, #89 and #91 one defect?** `Swap` and `Swap_ZB` — same test, same
   two surfaces — respond to composition in **opposite directions**
   (`Swap_ZB`: 0→0, 1→0, 2→141,125; `Swap`: 0→304,750, 1→165,447). Plus a defect
   the policy reaches independently. That is an argument for one aliasing defect
   with three faces.
6. **#92's motivation may rest on a composition artefact.** It is motivated by
   `Swap` agreeing at 165,447 across GL and Vulkan — but 165,447 is only the
   *1-preceding-capture* value. The agreement is evidence of a shared defect
   **only if both were measured at the same composition, which nobody checked.**
   Settle that before paying `SurfaceShape`'s blast radius.
7. **Closable, per `lane.triage`, not closed here:** #43 and #79 (the latter as a
   duplicate of #44, waiting four days). **Needing an owner decision:** #44 and
   #34. See `board-requests/triage.md` items 1–3 and 9.
8. **`tb_lookup_cmp()`'s comment in `accel/tcg/cpu-exec.c` still asserts
   `tier1_consume_request()` "never fires"** — measured false 11/11.
   `lane.tier81fix` holds the file and declined to change it, because correcting
   it is a claim about **#73's** reasoning. Grant it to whoever owns #73.
9. **`xemu_get_tier1_stats()` has zero callers** and
   `g_tier1_promotions_dropped` is unreadable on device — which is why promotion
   pressure is a *bound* and not a value in both #81 arms. One
   `__android_log_print` fixes it; needs `android/app/src/main/cpp/xemu_android.cpp`.
10. **`TIER1_BUDGET_INTERVAL_MS` is dead** (`cpu-exec.c:63`). The real limit is
    8 per 100,000 TB executions. Left alone deliberately: not #81's fix.
11. **`ab_compare --register` has no `--must-not-regress` flag.** Seven of 107
    prediction files carry the field, all hand-edited after registration.
12. **A queue-time `b_ref` ancestry gate** is still only wished for in
    `AGENTS.md`. `lane.arms` counts **four occurrences** now.
13. **`libcurl4-openssl-dev` is not installed**, so no lane can build desktop —
    which is where the Khronos validation layer lives, the check #88 most wants.
    Owner action: `sudo apt install libcurl4-openssl-dev`.
14. **PR #45's open question is unanswered by design.** `lane.remote` asked for
    two shas; the procedure says the design session answers it by merging the
    branch. PR #45 stays a draft and unmerged.
15. **`lane.arms`'s five completed arms are unjudged**, including **#38, which is
    judgeable for the first time** — the prior pair died on
    `progress_log_proof false`, this one is 5/5 on both arms against
    `MIN_RUNS = 5`, one device, one day, distinct `apk_sha` per arm.
16. **`audit2-blitsafe` holds a territory row with no fleet record at all** —
    an untracked lane. It holds no files. Left in place.
17. **`check_cited_commits.py` FAILs on 5 entries and is NOT wired into
    `preflight.sh`**, so the failure is invisible unless run by hand.
    `lane.triage` split the five by cause rather than relaying them: *landed
    under another sha, re-cite and keep the status* — #51 `4a08abef66`→`8b50aefbd4`,
    #51 `4c3364bcb1`→`28eeff3d88`, #66 `5b707602`→`a9d06195fd`; *genuinely
    unfolded, so `fixed-unlanded` is the checker's own honest value* — #38
    `24a75d6e3c` and #68 `937848c9e7`.
18. **#62 should be narrowed to finding 2 alone.** The tracker carries
    `lane.triage`'s earlier finding verbatim and HEAD has overtaken it: findings
    1, 3, 4, 5 and 6 are all fixed, and the note still says finding 4 "is NOT at
    HEAD" when `gl/surface.c:2724-2725` now reads `DO_CMP(shape.color_format)`.
    Only finding 2 is live, at `gl/surface.c:635`, and it is **#59's semantics,
    not #62's**.
19. **#81's `blocked_on` still describes a cleared blocker** — it asks for
    `hakuX-tier1:D` in `dispatcher.sh`'s `LOGCAT_SPEC`, which is at
    `dispatcher.sh:1042`, while the entry's own `status_note` says the
    measurement happened. `1c9935f102` fixed the file half, not the tag clause.
20. **#34's `blocked_on` is literally `None`** while two comments ask the board
    to decide, and the Adreno half is not un-run but **currently impossible**:
    `grep -rn` over `android/` for the validation layer returns zero hits, and
    `vk/instance.c:111` can only match a packaged layer.
21. **#43's last comment is still a bare unexpanded `@/tmp/…/done.md`.** The
    rescue landed in the tracker, not in the issue log — so if #43 is closed
    before the content is posted, **the evidence closes with it**.
22. **FBSTP still has no row**, despite #74's own `blocker_tested` saying to
    file it separately. Site `helper_fbst_ST0`, `fpu_helper.c:1461`; `fbstp`
    count is 0 in the corpus, so **no arm on this fleet can ever guard it**.
23. **#88 still carries the word "verified" on the strength of a check that was
    never run** — the Khronos validation layer is desktop-only and is not
    installed (`sudo apt install vulkan-validationlayers`, and see item 13).
    `lane.fold` flagged this debt and it is still outstanding.

---

## 9. Released

```
docs/testing/backlog-gate.sh release   ->  orchestrator released   (exit 0)
docs/testing/goal.sh clear             ->  goal cleared            (exit 0)
```

**`hold_device.sh release` was NOT run, deliberately.** This session placed no
device hold and `$DISPATCH_DIR/hold/` contains no hold files — only `lifted/`.
Running it would have released something this session did not place, which is
the opposite of preserving.

**Monitors:** the idle watchdog and `watch_remote_lane.sh` were this session's
and stop with it; the backlog gate is released above, so the Stop hook is inert.
The three systemd **user** timers are *not* this session's and were left alone
on purpose — `hakux-nightly` (00:30), `hakux-dx` (09:23) and `hakux-comments`
(hourly at :17), all `Persistent=true` because WSL2 shuts down. Phase 0 replaces
them with `hakux-board.timer`; **until it does, they will keep firing against a
session that no longer exists.** Whoever installs the Phase 0 units should
expect that and disable these three rather than be surprised by them.

---

## 10. Inventory at wind-down

Verbatim outputs are in the commit that adds this file, under
`docs/investigations/handoff-2026-09-19-inventory.txt`.

---

## 11. One thing I would tell the next actor before anything else

The recurring failure in this session was not a wrong answer. It was **a record
left standing after the thing it recorded had ended**, and it happened at every
scale on the same day: a suspension banner two lanes ran soaks against; five
issue blockers citing a device outage that was over; a territory row for a lane
whose agent was never dispatched; a prediction bound to a ref nobody pushed; and
a `component` key that was the wrong axis and turned every lane's push gate red
for hours without anyone noticing, because a gate that fails silently and a gate
that passes look identical from the outside until someone tries to push.

Phase 0 replaces an always-on session with jobs. The thing worth carrying into
that design is that **almost none of these were caught by the actor who created
them.** Every one was caught by a lane that hit it from the outside — and in the
most valuable case, by a lane that stopped and refused to push rather than
working around it.

# #31 on silicon, fourth run: `ClipF` at `clip_left` 300, `clip_top` 8, and `flatTop` is refuted

**Measured 2026-09-25 on the project console (GPU rev 163 / MCP rev 212).**
Registered beforehand in
[`docs/lanes/xbox/wbuf31-clipf300-prediction.md`](../lanes/xbox/wbuf31-clipf300-prediction.md)
(`8502546e21`). The host routed this capture on #112 (item C); #31 was
`blocked_on` it. Earlier runs: PRs #218 (35), #221 (4) and #226 (8/12/16/64).

## The answer: the first triangle anchors at 10 (the 4-grid), not 8

| triangle | px (pred) | hw offset interval | anchor | quad snap | 4-grid | master's `sel` |
|---|---:|---|---:|---:|---:|---:|
| t0 | 24,952 (24,952) | [98713.0111, 98713.0198] | **10.000** | 8 | 10 | 8 (**wrong**) |
| t1 | 126,348 (126,348) | [73585.9574, 73585.9619] | 34.000 | 34 | 34 | 34 |

At `clip_left` 300, `clip_top` 8, t0's first covered span starts at the clip.
There, `span_starts_at_clip` (grid, 10) and master's `flatTop` clause (quad, 8)
disagree, and silicon took the grid.

- **The `flatTop` clause of #222's rule is refuted at this geometry.** Master's
  `sel` now reproduces 53 of 78 anchors. It misses this one and the 24 `TriV`
  anchors that every rule misses.
- **Size of the miss on this capture.** The tool's float32 simulation of master's
  rule gets 108,412 pixels exact, 17,936 within ±1, and 24,952 wrong. The
  wrong pixels are all of t0. This variant is not on the golden disc, so no
  scored golden moves; it is a misprediction on silicon.
- **The selector search** (`--selectors`, over 54 anchors, 35 informative) leaves
  57 four-literal fits. These are exactly the 57 that voted grid at
  registration. The 36 that voted quad, master's among them, are gone.
  - 36 of the survivors rest on ClipF-150-008/016/032/064's t0.
  - 21 rest on FloorQuad/t1, RoofQuad/t0, WallQuad/t0 and the ClipW anchors.
  - They all agree at every `ClipF` `clip_top` at `clip_left` 150.
- **The t0 run's "multiples of 8" observation does not hold.** This `clip_top`
  is 8-aligned and the triangle is flat-topped, yet it took the grid. What
  separates this t0 from `ClipF-150-008`'s t0 (quad, 8) is where the first
  span starts: at the clip here, at the diagonal (x ≈ 207) there.

## Every registered leg

| leg | registered | measured | |
|---|---|---|---|
| T0 | 8 (quad) or 10 (grid); anything else is neither | 10.000 | grid |
| T1 | 34 | 34.000 | held |
| C1 | 22 control files bit-identical to goldens or earlier silicon | 22 / 22 identical; the new capture is present and differs from ClipF-150-008 | held |
| C2 | t0 24,952 px, t1 126,348 px | 24,952 and 126,348 | held |
| C3 | 0 plane mismatches on the floor | FloorQuad 0 / 221,970 | held |
| dry run | the Thor completes and writes 12 captures | `1790346648-xbox-wbuf31c300-dryrun-2923513`: "Testing completed normally", 24 PNGs | held |

## What ran

- The XBE: nxdk_pgraph_tests `hakux/wbuf31-clipf300` `d504bad`, which is
  `da77078` plus one variant. The patch is
  [`wbuf31_clipf300.patch`](../lanes/xbox/wbuf31_clipf300.patch). XBE sha256
  `cd2956a882b3…`.
- The console run: `tools/xbox/pgraph_run.py`, app `PgraphWbuf31d`, output
  `e:/wbuf31_clipf300`.
- Settings: shutdown-on-completion off, network off, progress log on, and no
  register writes.
- Outcome: the run took 57 s, handed back to the dashboard, and returned 25
  files.

Captures and the full tool output (`scored-anchors.txt`) stay on the host
under `~/hakux-work/hardware/runs/2026-09-25-wbuf31-c300/`. To score:

```
python3 docs/lanes/xbox/score_clipf300.py \
  --goldens <run>/console-run/console --goldens /home/justin/goldens/results \
  --goldens <clipf35>/console-run/console --goldens <clipf04>/console-run/console \
  --goldens <t0>/console-run/console
```

Which of the 57 survivors to encode is `lane.wbuf31fix`'s call.

{
 "registered_utc": "2026-09-14T00:52:00Z",
 "who": "remote-lane-agent",
 "issue": "60",
 "a_ref": "082bc0ac",
 "b_ref": "082bc0ac + f75a4aad's one line",
 "prediction": "THE ORDERING CLAIM, TESTED DIRECTLY. territory.toml's lane.remote note says '#71 before #60 -- #60's stale-format bug is HIDING #71 ... and #60's fix regresses four captures by 31,948 px that are entirely thirty s2t decisions flipping false->true'. That regression has now been reproduced on the campaign branch: 082bc0ac against 1a879e56 moves exactly three captures, all worse, +31,436 px, two runs per arm byte-stable. The campaign branch does NOT carry #71's fix -- f75a4aad is not an ancestor of it and `min_filter = 0xFFFFFFFF` appears zero times in its gl/surface.c -- so it is carrying #60 in exactly the configuration the ordering note warns about. THE ARM: apply f75a4aad's single added line on top of 082bc0ac -- `texture->min_filter = 0xFFFFFFFF;` after the forced GL_LINEAR at gl/surface.c:1407, which drops the cached filter so a REUSED binding has the guest's filter reapplied instead of keeping the one forced to make a single-level texture complete. Nothing else changes. PREDICTED: all three regressions SHRINK. Blend_surface::ARGB8_Add_SrcA_1-SrcA from 22,910, ARGB8_Add_SrcA_DstA from 21,402, DstAlpha_XA_O1A7RGB8 from 90,112, each moving back toward its 082bc0ac-parent value of 9,547 / 11,521 / 81,920. NOT PREDICTED, and the distinction is the point: that they return exactly to those values. The investigation established the damage is entirely those thirty decisions taking the fast path; f75a4aad repaired ONE reason that path was wrong. If a second reason remains, the captures improve without closing, and that is a result rather than a failure -- it would say the ordering note is right about the direction and incomplete about the size. WHAT WOULD REFUTE ME: no movement at all, or any of the three getting worse. Either would mean the regression is not the s2t path and the ordering note's mechanism is wrong. CAVEAT ON COVERAGE, stated before the result: #66's assert is live on this branch, so every run stops at 44 of 236 captures with QEMU_EXIT=134. All three movers are inside those 44, so the legs are testable, but nothing outside them is. METHOD: two runs of each arm, compared as BYTES with sweep_agreement.py before any number is quoted.",
 "must_not_move": [],
 "expect": {},
 "expect_counts": {"better": 3, "worse": 0}
}

{
  "a_ref": "11ddd94a66",
  "b_ref": "2ffd961764",
  "expect": {},
  "expect_counts": {},
  "issue": "164",
  "must_not_move": [
    "Blend_surface/*",
    "Surface_format/*",
    "Clear/SCF_X8R8G8B8_O8R8G8B8",
    "Clear/SCF_X1R5G5B5_O1R5G5B5",
    "Clear/SCF_X1A7R8G8B8_Z1A7R8G8B8",
    "Clear/SCF_X1A7R8G8B8_O1A7R8G8B8",
    "Clear/SCF_R5G6B5",
    "Clear/SFC_A8R8G8B8",
    "Clear/C0*"
  ],
  "must_not_regress": [
    "Clear/*"
  ],
  "prediction": "#164: the CLEAR half of #59's surface pad-bit write side, ported to GL. gl/draw.c's clear site called the shared pgraph_get_clear_color() raw; its alpha switch has no case for the four pad formats and falls through to default *a = 1.0f. Since #158 gave GL the raster stamp, a _Z surface has held alpha 1 where CLEAR_SURFACE wrote and alpha 0 where the raster drew. Hardware gives 0 for both.\n\nTHE TWO MOVERS ARE Clear/SCF_X8R8G8B8_Z8R8G8B8 AND Clear/SFC_X1R5G5B5_Z1R5G5B5, AND THEY ARE BOUND IN PROSE RATHER THAN BY VALUE, DELIBERATELY. This lane cannot reach the dispatch directory from its sandbox, so it could not measure a pre-fix baseline, and the only absolutes available for these captures are wrong for this arm: the 98,342 and 49,274 quoted on issue #164 are GOLDEN-VERSUS-GOLDEN differences (the _O golden against the _Z golden, from the table headed \"In the goldens\" in docs/investigations/surface-pad-bits-are-written-not-read.md), not scored rows, and no arm can move them. The real scored rows are device- and commit-dependent: 49,152 and 81,840 on thor at 23be8223f5, 98,304 and 49,152 on adreno at baab7ad5dc. Inventing an expect value from either would have failed this arm on arithmetic that has nothing to do with the change. So the machine-read half of this prediction is the must_not_move set, which needs no baseline, and the claim about the movers is stated here to be judged by reading the per-capture table.\n\nWHAT THE MOVERS MUST DO: both must IMPROVE, and substantially. Clear::TestSurfaceFmt clears a whole 128x128 pad-format surface, draws a 4x4 centre mark, and samples the entire surface through LU_IMAGE_A8B8G8R8, so the cleared-and-never-drawn area is essentially the whole capture and its alpha comes from the clear. NV097_CLEAR_SURFACE_COLOR is 0xF0 and therefore includes the alpha bit (1 << 7), so glColorMask admits the write.\n\nGL SHOULD MOVE WHERE VULKAN DID NOT, and this is the sharpest thing the arm can test. The same change on Vulkan scored better 0 worse 0 same 217: 49,104 px moved on SFC_X1R5G5B5_Z1R5G5B5, green going 49,152 to 48, while the score stood still because #48's readback swizzle overwrote the alpha half. One defect replaced by another on the same pixels. GL has NO such swizzle -- sampled_pad_alpha exists only in vk/constants.h and vk/texture.c, and gl/renderer.h:361 states GL has no host_fmt at all -- so on GL the sampler returns the stored alpha and both the green and the alpha halves should correct. If these two captures move green but not the score, the GL-has-no-readback-override premise is wrong.\n\nTHE UNCOMFORTABLE OUTCOMES, NAMED IN ADVANCE. (1) If the two movers do not move AT ALL, the most likely cause is not that the model is wrong but that the arm ran GLES: this change is inside #ifndef __ANDROID__ and gated on pgraph_glsl_dual_src_pad_supported(), whose setter gl/renderer.c keeps inside the same guard, so on an Android build it is a no-op BY CONSTRUCTION and would read exactly like a refuted prediction. THIS ARM MUST RUN DESKTOP OPENGL, which is where #158's own 236-capture arm ran. Establish which renderer the arm used before reading a zero as a refutation. (2) If either _O twin moves, the ZERO and ONE cases are transposed: pgraph_get_clear_color() already returns alpha 1.0, so PSH_PAD_ALPHA_ONE is a deliberate no-op and its twin moving means the mapping is inverted. (3) If either X1A7R8G8B8 capture moves, the change reached a format pgraph_glsl_surface_pad_alpha_mode() classifies as NONE, and the model is wrong even if the headline improved.\n\nWHY Blend_surface AND Surface_format ARE PINNED BIT-IDENTICAL RATHER THAN ASSUMED: both were checked in the test sources rather than taken from the brief. Surface_format prepares its pad-format scratch surface with a CPU memset (surface_format_tests.cpp:216) and issues its only PrepareDraw at :145, before the pad format is bound at :153. Blend_surface likewise calls PrepareDraw at :287 and :494, before RenderToSurfaceStart binds the pad surface at :299 and :500. So no CLEAR_SURFACE reaches a pad-format surface in either suite, and #158's nine recovered captures must read exactly what they read now.\n\nNO expect_counts IS REGISTERED, AND THAT IS A CHOICE. expect_counts is a global tally over the whole disc, and Surface_pitch::Swizzle moves on its own -- it was the single mover in a compile-only no-op arm that was otherwise byte-identical on 235 of 236 (the #71 race). A worse=0 leg would fail on it while saying nothing about this change, which is the #67 failure verbatim.\n\nNO disc BLOCK IS RECORDED, AND THAT IS ALSO A CHOICE. The field exists to stop an ABSOLUTE registered on one composition being judged against an arm run on another (#89, #95), and this prediction registers no absolute at all -- expect is empty by the reasoning above. The two arms sharing a disc is enforced separately by check_comparable(). This lane cannot reach the dispatch directory from its sandbox and so does not choose the disc the arms job will use; declaring a composition it does not control could only manufacture a refusal that means nothing about the change.",
  "registered_utc": "2026-09-19T22:51:44Z",
  "who": "lane.clrpad164"
}

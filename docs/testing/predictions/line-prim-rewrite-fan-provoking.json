{
  "registered_utc": "2026-09-20T19:42:57Z",
  "who": "lane.primpv13",
  "issue": "13",
  "prediction": "MECHANISM.  #13's geom.c half landed the derived edge-priority order -- for a\ntriangle (a, b, c) paint the edge opposite a, then opposite b, then opposite c\n-- and took nine of eleven candidate classes to 100.00% of their decisive\npixels.  TFan and QStrip/TFan did not, because rewrite_triangle_fan() calls\nemit_tri_pv(), which rotates the guest's provoking vertex to index 0: a fan\ntriangle reaches geom.c already rotated by one relative to the TRIANGLES draw\nbeside it, and GeomState::primitive_mode is the REWRITTEN mode, so geom.c\ncannot tell them apart and cannot compensate.\n\nTHIS ARM removes that rotation in exactly the state where nothing reads index\n0.  Both renderers rasterise first-vertex-provoking (gl/draw.c sets\nGL_FIRST_VERTEX_CONVENTION; nothing in vk/ enables VK_EXT_provoking_vertex)\nand geom.c spells provoking_index as the literal \"0\" only when the shade mode\nis FLAT -- needs_rewrite() already encodes that for PRIM_TYPE_TRIANGLES, which\nit declines to rewrite at all unless the draw is flat AND last-provoking.  The\nnew predicate is `flat_shading || polygon_mode != POLY_MODE_LINE`, so the fan\nkeeps its rotation under FILL, under POINT, and under flat shading everywhere.\n#13's trade is NARROWED to its one real corner -- a flat-shaded wireframe,\nwhere the colour must win over the paint order -- not abolished.\n\nWHY IT IS FREE ON THE WIREFRAME PATH.  geom.c splits a triangle (A, B, C) into\nemit_line(B,C), emit_line(C,A), emit_line(A,B).  Rotating the triple is\ntherefore a pure rotation of the EDGE LIST: the multiset of ORDERED (i0, i1)\npairs is identical and only the paint order changes.  That is checked, not\nargued -- see the A/B below, where the old code emits 2 0 1 / 3 0 2 / ... and\nthe new emits 0 1 2 / 0 2 3 / ..., the same ordered pairs in a rotated order.\nSo vtxFogSpecial, which glsl/common.c qualifies `flat` in EVERY shade mode but\nwhich each emit_line() takes from that edge's OWN first endpoint rather than\nfrom the triangle's index 0, is unchanged per edge.\n\nTHE SCOPE IS MEASURED, NOT ASSERTED.  Both revisions of prim_rewrite.c were\ncompiled side by side and their output diffed over every\n(primitive_mode x polygon_mode x last_provoking x flat_shading x count)\ncombination, 1508 states: exactly 20 differ, all of them TRIANGLE_FAN under\nPOLY_MODE_LINE with smooth shading, at both provoking conventions, with an\nIDENTICAL index count on every one.  A reorder emits the same number of lines,\nso no counter can tell the two arms apart and only the captures can.\n\nWHICH FOOTPRINT, AND WHY THIS REPLACES AN EARLIER REGISTRATION OF THE SAME\nARM.  line_priority's decisive set is built from a model of OUR OWN footprint,\nbecause the question is which edge OUR capture's colour names, and an edge our\nrender covers but the model excludes reads as `unm` rather than as agreement.\nThis prediction was first registered (2026-09-20T19:36:56Z) against the\nPERPENDICULAR footprint, which is line_priority_arms.py's default -- and that\ndefault, with the docstring justifying it, dates from 2026-09-13 and says the\nwider rule is \"what we do not yet implement\".  That is no longer true:\n80c23dcabe landed the derived extent in geom.c's widen_lines path the same\nday, and 7ce57a799b / e0c0a9974b refined its cap on 2026-09-19.  Our renderer\ndraws the WIDER footprint on the device today, so `--extent-rule` is now the\naccurate model of our own coverage and the default is the stale one.  No\ndevice run had touched the earlier file; this is a footprint model corrected\nBEFORE any measurement, not a prediction widened after one, and the superseded\nvalues are restated below so the difference is auditable rather than quiet.\n\nTHE MODEL IS CALIBRATED THREE WAYS, NONE OF THEM FITTED.\n  1. `line_priority.py --rules --extent-rule --min-width 8 --max-width 63.875`\n     gives 225,558 decisive pixels and scores the derived rule `opp_abc` at\n     100.00% in each of eleven classes separately.  That is exactly the figure\n     and the phrasing prim_rewrite.c's own file comment carries from #13's\n     original derivation -- same instrument, same window, same answer.\n  2. The same tool WITHOUT --extent-rule gives 198,880 px and reproduces the\n     landed geom.c arm's measured arm-B table on all eleven classes AND the\n     aggregate -- Tri 45,760 100.00%, QStrip 35,560 100.00%, LLoop 31,548\n     99.53%, QStrip/TFan 22,897 77.17%, Quad 22,761 100.00%, Poly 15,235\n     100.00%, Poly/TFan 13,853 100.00%, TFan 9,731 70.36%, LLoop/Tri 1,305\n     100.00%, Poly/Tri 222 100.00%, LLoop/TFan 8 100.00%, ALL 95.85%.  Eleven\n     ns and twelve percentages against a real device arm.\n  3. `patched_order(block)` comes out IDENTICAL to `order_for(block,\n     \"opp_abc\")` for every block, and identical to `ours` for every block but\n     TFan.  The patch does not approximate the derived rule, it becomes it.\nThe tool needed fixing first to get any of this: its hand-listed `ours` table\npredated BOTH halves of #13's edge-order work and still described the pre-fold\norder for Tri, QStrip, Poly and Quad, and order_for()'s index arithmetic was\ntacitly keyed to that same layout, so `opp_abc` -- the derived rule itself --\nwas scored against the wrong slots.  Both are now derived from prim_rewrite()\ncomposed with geom.c's three emit_line() calls.\n\nA CORRECTION TO A NUMBER THIS WORK HAS BEEN CARRYING.  #13's comments, and\nthis lane's brief, call the residue \"32,628 decisive pixels\".  That is the\nPOPULATION of the two classes under the perpendicular model (9,731 + 22,897),\nnot the pixels wrong inside it.  Under the footprint we actually draw the two\nclasses hold 11,636 + 24,009 px and the pixels naming the WRONG edge are 3,119\n+ 5,801 = 8,920.  Registering 32,628 as the expected movement would have been\nbetween 3x and 4x out either way.\n\nTHE JUDGED COMMAND is\n  python3 docs/testing/line_priority_arms.py --a <armA> --b <armB> \\\n      --extent-rule --min-width 8 --max-width 63.875\nand legs 1-4 and 7 are its numbers.  The perpendicular-default values are\ngiven alongside each leg so the prediction is still judgeable, against a\nregistered number, if the default is run instead.\n\nLEG 1 -- TFan: 11,636 decisive pixels go from 73.20% naming silicon's edge to\n100.00%.  (Perpendicular default: 9,731 px, 70.36% -> 100.00%.)  The world in\nwhich it fails: unchanged at 73.20% (the patch did not reach the device, or\nthe predicate never fires); 47.57%-like values, which would mean the\ncomposition is being undone rather than completed; or any other value, which\nrefutes the model that a fan triangle is pre-rotated by exactly one.\n\nLEG 2 -- QStrip/TFan: 24,009 decisive pixels go from 75.84% to 100.00%.\n(Perpendicular default: 22,897 px, 77.17% -> 100.00%.)  This is the harder\nhalf and it is a MIXED class: the pixel is decided by a fan edge's rank\nagainst a QUAD_STRIP edge emitted by a code path this patch does not touch.\nIt can fail while leg 1 passes, and it fails at 75.84% (unchanged), at\n100.00%-minus-anything, or if QStrip itself moves.\n\nLEG 3, scope -- the nine other classes are UNCHANGED and `moved` is 0 on each\none.  Under --extent-rule all nine read 100.00% before and after: Tri 52,411,\nQStrip 40,139, LLoop 35,571, Quad 26,549, Poly 19,355, Poly/TFan 13,764,\nLLoop/Tri 1,724, Poly/Tri 272, LLoop/TFan 128.  (Perpendicular default: the\nsame nine, with LLoop at 99.53% rather than 100.00%.)  Poly/TFan and\nLLoop/TFan are the ones that can fail: they CONTAIN fan edges whose rank this\npatch changes, and the claim is that the WINNER does not change even though\nthe rank does.  A leg naming only the fan-free classes would be forced true.\n\nLEG 4, did it execute -- `moved` (decisive pixels where arm A and arm B name\nDIFFERENT edges) is at least 3,119 on TFan and at least 5,801 on QStrip/TFan.\n(Perpendicular default: at least 2,884 and at least 5,228.)  A patch that\nnever reached the device gives moved = 0 on every class and is otherwise\nindistinguishable, in any pixel total, from one that ran and returned a\ndifferent wrong answer.  Arm A against itself gives 0 by construction, so this\nleg has a world in which it fails.\n\nLEG 5, THE TRADE'S OWN GUARD -- every `Shade_model/*_TriFan_Flat_*` capture is\nBYTE-IDENTICAL (14 captures: seven shader prefixes x First/Last).  Under flat\nshading pv_placement_observable() returns true and rewrite_triangle_fan() is\nuntouched, so these must not move at all.  This is the leg that would catch\nthe predicate being inverted or the flat_shading term being dropped, and its\nfailure would be large rather than subtle: a flat-shaded fan whose provoking\nvertex moved changes the colour of whole triangles, not a few edge pixels.\nIt does not depend on the footprint question above.\n\nLEG 6, primitive scope on the same suite -- `Shade_model/*_Tri_*`,\n`*_TriStrip_*`, `*_Quad_*`, `*_QuadStrip_*` and `*_Poly_*` are all\nBYTE-IDENTICAL.  The patch reaches PRIM_TYPE_TRIANGLE_FAN and nothing else;\nTRIANGLE_STRIP in particular is deliberately left alone, because its odd-i\ncase is a reflection rather than a rotation and the Line_width corpus that\npins these orders draws no TRIANGLE_STRIP under POLY_MODE_LINE at all.\nLegs 5 and 6 together cover 154 of Shade_model's 168 captures -- all but the\n14 `*_TriFan_Smooth_*` that may legitimately move.\n\nLEG 7, advisory and NOT counted -- ALL rises from 96.05% to 100.00% over\n225,558 decisive pixels.  (Perpendicular default: 95.85% -> 99.93% over\n198,880, the 149 px left being LLoop's known 99.53%.)  Advisory because it is\nlegs 1-3 re-added and the pooled figure hides which class moved.\n\nTHE DISC carries Blend_tests, which draws no wide line and no wireframe at\nall, for the same reason lane.linecap13 put it there: it is the tripwire for\nthis change being broader than modelled.\n\nEXPECTED TO MOVE, direction unpredicted, named so a mover is not read as a\nsurprise: `Shade_model/*_TriFan_Smooth_*` (14 captures -- these are the ones\nthat CAN reach the patch), every per-capture differing-pixel total in\n`Line width`, and any `Edge flag` / `Front face` / `W param` capture that\ndraws a TRIANGLE_FAN under POLY_MODE_LINE.  Direction is unpredicted for the\nreason #13 recorded: the decisive set is ~0.1% of the ink and the priority\nclass overlaps the extent class, so removing a priority error need not reduce\nany total.  ONE SECOND-ORDER ITEM, disclosed rather than buried: geom.c's\nPOLY_MODE_LINE body takes its depth slope from calc_triz(0, 1, 2), which\ncomputes m and b relative to vertex 0, so a fan triangle's dz is now evaluated\non the same basis a list triangle's already was.  The plane's gradient is\nrotation-invariant, so this can move dz only by rounding, and it feeds triMZ\n(depth) and not coverage; `Line width` disables the depth test, so it cannot\nshow there at all.\n\nNOT A FALSIFIER, recorded as an assertion about my own output: `2D_Lines/*`\nbyte-identical.  PRIMITIVE_LINES never reaches rewrite_triangle_fan(), so my\nown patch forces it true and it tests nothing about silicon.\n",
  "a_ref": "4955050b31",
  "b_ref": "40ca2bcb22",
  "must_not_move": [
    "Shade_model/*_TriFan_Flat_*",
    "Shade_model/*_Tri_Flat_*",
    "Shade_model/*_Tri_Smooth_*",
    "Shade_model/*_TriStrip_*",
    "Shade_model/*_Quad_Flat_*",
    "Shade_model/*_Quad_Smooth_*",
    "Shade_model/*_QuadStrip_*",
    "Shade_model/*_Poly_*",
    "2D_Lines/*"
  ],
  "expect": {},
  "expect_counts": {},
  "must_not_regress": [],
  "disc": {
    "suites": [
      "2D_Lines",
      "Blend_tests",
      "Edge_flag",
      "Front_face",
      "Line_width",
      "Shade_model",
      "W_param"
    ],
    "skip_tests": [],
    "only_tests": [],
    "source": "stated on the --register command line"
  }
}

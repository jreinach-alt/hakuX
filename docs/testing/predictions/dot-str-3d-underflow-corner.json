{
  "registered_utc": "2026-09-13T04:07:13Z",
  "who": "desktop-lane-agent",
  "issue": "51",
  "prediction": "MECHANISM, and this is what to judge -- not a pixel total, because the six Texture_cubemap/DotSTR3D_* captures sit near 49,000 px each and will not go exact even if the rule is exactly right. THE DOT TRIPLE UNDERFLOWS AND THE SIGNS SURVIVE. The three dots handed to the cube lookup are rows of the INVERSE composite matrix, which carries the 640x480 viewport and a 0x00FFFFFF depth scale, so |dot1|,|dot2| <= 0.00299 and |dot3| <= 9.37e-7. An 8-bit fixed-point fraction has a half-quantum of 0.0039, above all three bounds, so on any dot path of eight or fewer fractional bits the whole triple arrives at the addressing hardware as EXACTLY ZERO. (The bound is the right instrument here and only here: it is an upper bound and the claim needs only that every value fall under one threshold. It said nothing about the mid/max ratio, and an earlier prediction of mine that used it for that was wrong -- a bound is not a value.) In sign-magnitude the magnitude underflows while the sign bit survives, so the lookup receives a direction with no magnitude and three signs. Silicon resolves a zero-magnitude cube direction to a POSITIVE face at a saturated face coordinate -- measured independently from the other end by #40 (f6609964 reverted, re-landed as vec3(1.0, 1-2^-13, 1-2^-13), exactly -70 px on every one of 20 captures). The face coordinates then saturate and (s,t) = (-z/x, -y/x) lands on the corner those signs name. THE MEASURED QUANTITIES ARE THE SELECTED FACE AND THE POSITION WITHIN IT, recovered per pixel from colour. (1) FACE, as a sign group: GenerateMaxContrastNoisePattern holds only EIGHT colours across all six faces, and all three positive faces carry the same four corner colours permuted while all three negatives carry the complementary set -- so colour fixes the SIGN GROUP and the CORNER but CANNOT identify which positive face, and this prediction deliberately does not claim one. Every differing pixel must land on a corner of a POSITIVE face: output in {#FF0000,#0000FF,#FFFFFF,#00FF00} and never #FF00FF, #FFFF00 or #00FFFF. ONE negative-face corner colour anywhere falsifies the mechanism. (2) POSITION: no interior texel at all. One face holds 8 distinct colours over an 8x8 patch and the pattern holds 8 in total, yet each golden spends ~57,000 px on 4 of them, all corners. Any interior colour falsifies it. (3) WHICH corners, and this is the sharpest leg: the three UNSIGNED dotmaps (0to1, HiLo_1, HiLoHemi) must reach EXACTLY TWO corners forming an EDGE pair -- two corners sharing one coordinate extreme -- and the three signed ones (-1to1, -1to1D3D, -1to1GL) must reach all four. A DIAGONAL pair on the unsigned captures falsifies the PRODUCT rule specifically while leaving the corner claim standing, and that distinction matters: both (s,t) carry sign(x), so pinning sign(x) alone flips s and t together and yields the diagonal. Observing the edge pair is what says the selectors are the two PRODUCTS sign(z)*sign(x) and sign(y)*sign(x) rather than two of the three signs taken alone. Reaching THREE corners falsifies the whole mechanism, since two bits give four or two and never three. (4) CORNER COUNTS, B/R/G/W, and THESE MUST BE SCORED OVER y >= 45: 0to1 28114/0/28795/0, HiLo_1 28385/0/28524/0, HiLoHemi 28958/0/27951/0, -1to1 14446/14562/13723/14178, -1to1D3D 13093/13722/14832/15262, -1to1GL 14527/14457/13849/14076. The W column recorded before today was WRONG on all six -- it counted each capture's own white title text, which sits in a 16px banner at the top of the frame and is not a cube texel. On the unsigned captures the entire old W value was glyphs (1042/1170/1376 -> 0). A SCORER THAT COUNTS THE BANNER WILL MIS-JUDGE THIS PREDICTION. R, B and G were never contaminated, since white text on a black banner cannot add to them. NOT CLAIMED, and stated so it is not read in: the bit width is not measured -- the bound sits under an 8-bit half-quantum and over a 10-bit one, so '<= 8 fractional bits' is what the evidence is consistent with, not a width read off silicon; WHICH positive face is not determined by these captures, so a fix that picks the wrong positive face scores IDENTICALLY here and this corpus cannot catch it; and whether the underflow hits the direction or the face coordinate is not separated, both routes ending at the same texel. FREE PARAMETER, flagged rather than fitted: kDotSTR3dQuantum. Every pixel of every affected capture is below any plausible threshold, so 1/256 and 1/64 score identically -- this corpus confirms the rule and CANNOT calibrate the threshold. Calibrating it needs a capture whose dot magnitudes straddle it and none exists in the corpus. expect_counts better=6 worse=0 is the same claim in the scorer's units and is SECONDARY to the face and position above; a capture whose count improves while its colours are not positive-face corners has NOT confirmed the mechanism. b_ref is empty because glsl/psh.c is held by the #52 stream and this lane did not commit into it; the orchestrator points it at the arm when the diff lands, as was done for #40.",
  "a_ref": "2f14403e",
  "b_ref": "",
  "must_not_move": [
    "Texture_2D_as_cubemap/*",
    "Texture_cubemap/Cubemap_q*",
    "Texture_cubemap/DotReflect*",
    "Texture_cubemap/DotSTRCube_*",
    "Texgen/*"
  ],
  "expect": {},
  "expect_counts": {
    "better": 6,
    "worse": 0
  }
}

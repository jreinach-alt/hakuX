{
  "registered_utc": "2026-09-13T02:20:22Z",
  "who": "cubemap-face-agent",
  "issue": "40",
  "prediction": "SECOND ATTEMPT at #40, after arm de0c222852 -> f66099643c was judged against the first and reverted (ff9dec40cb). REGISTERED AS A DELTA, not as absolute counts, because the first round's expect-values were derived from a different binary's captures. Two things are now measured that were inferred before, and the revert's own conclusion is contradicted by its own arm.\n\nWHAT THE FIRST ARM ACTUALLY SHOWED. Read back from its captures at /home/justin/hakux-work/dispatch/results/1789264650-cube40-{base,fix}: the guard fired on 70 of 70 silhouette pixels in EVERY affected capture, not on one. B differs from A on all 70 in DotReflectSpec_-1to1, _0to1, DotSTRCube_-1to1, _0to1, DotReflectSpecConst_-1to1, DotReflectDiffuse_-1to1, _0to1 and DotReflectConst_0_0to1, _1_0to1, _0_-1to1. The counts stayed flat because a wrong answer was replaced by a different wrong answer: A was +Z at intensity 0xBF (or 0x06 on DotSTRCube, where the direction is a clean zero rather than a 0/0), B was +Z at 0xFB, and on the checkerboard captures A was 0xFFFF5555 and B was 0xFF222222. So \"the guard did not fire\" and \"at most one of the 70 is exactly zero\" are both wrong; the three -1 px deltas were a coincidence of the substituted texel matching the golden on one pixel, not the guard's reach.\n\nTHE PREMISE IS MEASURED, ON THE DEVICE, ON ALL 70 PIXELS. The #51 probe build (ref ec13c70e60, captures at dispatch/results/0-probe51) encodes |dot_1| as a bucket in the blue channel, with 0 reserved for EXACTLY ZERO. At the 70 silhouette pixels it reads EXACTLY ZERO on 70/70 for DotSTR3D_0to1 and DotSTR3D_-1to1, and >= 0.001 or \"tiny but nonzero\" on 70/70 for _-1to1D3D, _-1to1GL, _HiLo_1 and _HiLoHemi. That is precisely the partition predicted by sign3(0) == 0 and identity(0) == 0 against sign1(0) == -1.008 and sign2(0) == +0.0039. One pixel to the right of each, the same probe reads \"tiny but nonzero\" and mixed dot_2 signs, so the zero is a property of that column and not of the draw.\n\nTHE ERROR BEING FIXED. vec3(1.0) was taken from the Vulkan/GL rule that X wins a major-axis tie. That contradicted a measurement already in hand -- the zero vector resolves to +Z on this stack, so it breaks ties the other way -- and the substitution was itself a three-way tie, resolved to +Z at (s,t) = (+1,-1), i.e. texel (63,0): 0xFB on the radial gradient, and square (15,0) = 0xFF222222 on the checkerboard. Both predicted by that reading, both observed. The fix skews y and z below x by 2^-13, which makes |x| strictly greatest under either tie rule while keeping (s,t) inside texel (0,0) for any face up to 16384 wide, since PshState does not carry the face size.\n\nTHE 71st PIXEL, AND WHAT ACTUALLY PRODUCED THE -1 DELTAS. The first arm changed 71 pixels per capture, not 70, in all 20 captures and in no other capture. The 71st is (x=414, y=232) -- on the RIGHT cube, not the left silhouette. Our arithmetic degenerates there too (the guard reached it), but silicon's evidently does not: the gradient golden there is (136,0,136), a two-channel magenta naming -X, while the checkerboard golden there is 0xFF222222, which is the dark square of a POSITIVE face and cannot be -X at all. Two textures that agree on every other pixel disagree on this one, so it is a coincidental zero in our dot products rather than a shared degeneracy, and no substituted constant can be right for both it and the column. It was already wrong in arm A, so it cannot regress; the claim is therefore a delta of -70 out of 71 changed, with the 71st expected to stay wrong. And that pixel is exactly what the revert read as evidence: on the three captures whose golden there happens to be 0xFF222222, the substituted +Z texel (63,0) matched it by accident, which is the entire -3. The one pixel that moved the count is the one pixel that is not part of the mechanism.\n\nFALSIFIER, AS A MEASUREMENT OF FACE AND TEXEL, WITH THREE DISTINGUISHABLE OUTCOMES. The claim is a per-capture delta of exactly -70 on the 20 captures named in the previous registration, and on those 70 pixels the rendered value must be the +X face's (0,0) texel: exactly (191,0,0) on the 14 radial-gradient captures (DotReflectSpec*, DotReflectSpecConst*, DotSTRCube*) and exactly (255,119,119) on the 6 checkerboard captures (DotReflectDiffuse_-1to1, _0to1, DotReflectConst_*). (1) If the colour is a one-channel RED at 191 (and 0xFF7777FF on the checkerboard), the face rule and the saturated coordinate are both confirmed and the counts fall by 70 each. (2) If the colour is a one-channel RED at some other intensity, THE FACE RULE IS CONFIRMED AND ONLY THE TEXEL IS WRONG -- that is a real partial result, the substituted (s,t) is off, and it is worth another constant rather than a revert; the intensity itself names the texel via the gradient. (3) If the colour is BLUE again, +Z, then my model of this stack's tie resolution is wrong even with the skew, nothing here is salvageable, and #40 should close as a measured negative: silicon's resolution of a zero cube direction would then not be reachable by any substituted direction and would need the face selection done in the shader against six sampler2D views, which is out of proportion to 1,400 pixels.\n\nWHAT IS STILL INFERRED, AND SHOULD BE READ AS SUCH. The FACE is measured: +X, from two cube textures whose per-face palettes are disjoint by construction (kColorMasks and kCheckboxColors, one or two channels per face). The TEXEL is inferred: 191 occurs at 21 texels of the 64x64 gradient, and requiring the checkerboard's golden to be square-parity-even as well leaves 11 of them. (0,0) is the only one of the 11 with a mechanism behind it -- a divide by zero saturating both face coordinates negative -- so it is the one substituted, but outcome (2) above is its live alternative and is not a falsification of the face.\n\nMUST NOT MOVE, unchanged and all still mechanistically impossible rather than merely hoped: Texture_cubemap/DotReflectDiffuse_-1to1GL and every other _-1to1D3D, _-1to1GL, _HiLo_1 and _HiLoHemi capture, because the probe measured their dot_1 as nonzero on all 70 pixels, so dot(d,d) > 0.0 holds and the guard cannot reach them; Texture_cubemap/Cubemap_q*, six exact captures on a mode this does not touch; Texture_cubemap/DotSTR3D_*, #51's lane, whose stage resolves to sampler2D here; and all of Texture_2D_as_cubemap, which calls SetCubemapEnable(false) on stages 2 and 3 so the guarded form is never emitted -- its DotSTRCube_Bad2D shows the same 70-pixel column and stays unfixed on purpose, the non-cube remapCubeTo2D fallback's behaviour on a degenerate direction being unmeasured. Texgen is carried as a control for the preflight text growing on every shader.\n\nLEAST CERTAIN POINT. These stages sample with MAG_BOX_LOD0, which vk/constants.h maps to VK_FILTER_NEAREST, so any (s,t) inside texel (0,0) gives that texel exactly. Under a LINEAR-filtered cube stage the skewed corner would tap across a face seam, and nothing here measures that; a cubemap-flagged stage with TENT filtering and an exactly zero dot triple is the case this fix is not calibrated for.",
  "a_ref": "a494d1f594",
  "b_ref": "8705465ce6",
  "must_not_move": [
    "Texture_2D_as_cubemap/*",
    "Texture_cubemap/Cubemap_q*",
    "Texture_cubemap/DotSTR3D_*",
    "Texture_cubemap/*_-1to1D3D",
    "Texture_cubemap/*_-1to1GL",
    "Texture_cubemap/*_HiLo_1",
    "Texture_cubemap/*_HiLoHemi",
    "Texgen/*"
  ],
  "expect": {},
  "expect_counts": {
    "better": 20,
    "worse": 0
  }
}

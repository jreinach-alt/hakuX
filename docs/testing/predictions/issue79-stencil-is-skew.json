{
  "registered_utc": "2026-09-14T01:44:02Z",
  "who": "stencil",
  "issue": "79",
  "prediction": "#79 IS #44's GUEST<->PGRAPH SKEW, ON VERTEX DATA. Arm A is XEMU_OPT_FIFO_SKEW_BOUND = 0, arm B is the same tree with that one constant at 1 and nothing else -- `git diff --stat d97d506514 ec7f50e859` is one line in hw/xbox/nv2a/pfifo.c, verified in this worktree, and both APKs are already in the dispatcher's build cache so this arm costs no build. Stencil disc, --runs 4, --device thor, one requester, device_label read out of each result.json.\n\nTHE DIAGNOSIS BEING TESTED, derived with no device time from captures already in dispatch/results, the goldens and stencil_tests.cpp; written up in docs/investigations/stencil-is-the-guest-pgraph-skew.md. Every Stencil draw is one DefineBiTri -- SIX vertices, [ul,ll,lr] then [ul,lr,ur] -- into a buffer the guest rewrites for the next draw with no fence. The wrongly-drawn regions in the corpus are TRIANGLES: `REPLACE_ST` green is inner tri1 alone at IoU 1.0000, `REPLACE_ST_DT` green is OUTER tri2 alone at 0.9980, `ZERO_DT` shows D2's BLUE vertex colour on inner tri2 alone at 0.9960 on a draw whose COLOR_MASK is zero, and `REPLACE_DT` red is tri1 intact plus tri2 whose shared `ul` has x = 270 (the 100x100 quad) and y = 140 (the 200x200 quad) at 0.9943 -- one vertex caught with its x rewritten and its y not, the two copies of `ul` in the six-vertex array disagreeing. No stencil state is true for one half of a six-vertex draw and false for the other, and no state reaches inside a single vertex. This is #44's byte-level-mixture-inside-one-texel row, one level up.\n\nJUDGED ON WRONG (RUN, CAPTURE) OBSERVATIONS by docs/testing/stencil_outliers.py, NEVER on differing pixels and NEVER on a median. The whole content of #79 is that the MAJORITY image is golden-exact and the outliers are wrong, so a median over four runs reads 0 for a capture wrong in one of them -- which is exactly what ab_compare compares. The floor this is judged against was produced by that script on the fourteen Stencil-only-disc runs already on disk, before this arm was queued: 25 wrong of 224 observations, 11.2%, 8 of 14 runs carrying at least one, across five refs including the campaign tip.\n\nV0 (VALIDITY GATE, arm A ALONE, and the pair is VOID rather than passed if it fails): arm A shows at least 2 wrong observations in its 4 x 16 = 64. At 11.2% the expectation is 7.2 and P(fewer than 2) is about 0.5%. A control that does not flake cannot test a fix for a flake, and a zero from arm A would be an instrument result rather than a mechanism result. This gate exists because AGENTS.md records a falsifier whose power silently eroded when the baseline rate moved under it.\n\nV1 (THE BAR): arm B shows 0 wrong observations in 64. Under the null that the bound does nothing, P = 0.888^64 = 0.0005. FAILS on any non-exact Stencil capture in any of arm B's four runs.\n\nV2 (DID IT EXECUTE, on the direct counter and not a proxy): the fifoskew line in arm B's logcat shows held(n)/kicks >= 0.90, and arm A shows held(n) = 0. A flat count cannot separate a change that never ran from one that ran and returned a different wrong answer, and AGENTS.md records three separate legs on this project where a proxy over-read a defect that the direct counter said was zero. SILENCE IS VOID: if no fifoskew line is captured, V2 is void and V1 stands alone with its impossibility argument, it does not pass by default.\n\nV1 AND V2 ARE IN TENSION ON PURPOSE. Turning the bound off passes neither. A bound that is genuinely in force but irrelevant to Stencil passes V2 and FAILS V1 -- and that is the outcome that refutes this arm's document while leaving #44 untouched, which is the world this leg names. Nothing in the patch forces V1 true: the patch changes when the PFIFO thread is allowed to lag the guest and touches no stencil, clear, surface or draw code at all.\n\nTHE `expect` BLOCK BELOW IS THE WEAK HALF AND IS LABELLED AS SUCH. It registers all sixteen Stencil captures at 0 differing pixels in arm B. ab_compare compares MEDIANS, and every one of these captures already has median 0 in arm A, so arm A passes it too -- it is not inert (a badly broken arm B fails it) but it is not the bar. V1 is the bar. This is recorded rather than dressed up, because AGENTS.md's rule is that a leg your own baseline guarantees is a description and not a test.\n\nNO `expect_counts`, AND NO must_not_move ON Stencil/*. `worse = 0` is unsatisfiable on any disc carrying Stencil while #79 is open -- it took a 121-of-122 result to FAIL an hour before this was written -- and Stencil/* as a must-not-move control is precisely what stencil-is-intermittently-wrong.md forbids. The Stencil disc has no other suite to use as a control, and inventing one would change the disc composition and therefore what is being measured.\n\nWHAT A V1 FAILURE MEANS, stated before the run: not that the skew model is wrong -- #44 is settled texel-exact -- but that Stencil's outliers have a second cause the bound does not reach. The named next place to look is sync_vertex_ram_buffer's OPT_SYNC_RANGE_SKIP early exit, which declines to re-copy a range on a dirty-bitmap read rather than on the draw's own ordering, and which no amount of PFIFO pacing constrains.\n\nSCOPE, disclosed rather than discovered afterwards: both refs sit 292 commits behind the campaign tip, EQUALLY, which is what makes them a pair rather than a cross-ref comparison. The flake reproduces at every ref in the corpus including the tip (z-6762a54c82-059, 3 of 16 wrong), so the base is not doing the work -- but this arm cannot certify the tip, only the mechanism. If V1 passes, the follow-up is the same constant on a tip-based pair, and the decision in front of the board is the COST that guest-pgraph-skew.md already priced (gfps p90 29 -> 13 on Galleon), not the mechanism.",
  "a_ref": "d97d506514",
  "b_ref": "ec7f50e859",
  "must_not_move": [],
  "expect": {
    "Stencil/Stencil_REPLACE": 0,
    "Stencil/Stencil_REPLACE_DT": 0,
    "Stencil/Stencil_REPLACE_DT_ZB": 0,
    "Stencil/Stencil_REPLACE_ST": 0,
    "Stencil/Stencil_REPLACE_ST_DT": 0,
    "Stencil/Stencil_REPLACE_ST_DT_ZB": 0,
    "Stencil/Stencil_REPLACE_ST_ZB": 0,
    "Stencil/Stencil_REPLACE_ZB": 0,
    "Stencil/Stencil_ZERO": 0,
    "Stencil/Stencil_ZERO_DT": 0,
    "Stencil/Stencil_ZERO_DT_ZB": 0,
    "Stencil/Stencil_ZERO_ST": 0,
    "Stencil/Stencil_ZERO_ST_DT": 0,
    "Stencil/Stencil_ZERO_ST_DT_ZB": 0,
    "Stencil/Stencil_ZERO_ST_ZB": 0,
    "Stencil/Stencil_ZERO_ZB": 0
  },
  "expect_counts": {}
}

{
  "registered_utc": "2026-09-25T07:17:59Z",
  "who": "lane.wbuf31fix",
  "issue": "31",
  "prediction": "#31: ClipF's residual. The change under test is one emitted GLSL statement in wbufSlopeStep (glsl/psh.c): the row anchor takes the absolute 4-grid (4*floor(r/4)+2) when the clip did not cut the triangle, as before, AND NOW ALSO when the window clip's own top edge cut it (clip.y > 0 and the top vertex above it), unless that edge is a multiple of 8 and the triangle is flat-topped. Everything else keeps the 2x2 snap. The column is untouched.\n\nWHERE THE RULE COMES FROM. Silicon, GPU rev 163, 2026-09-25 (PRs #218, #221, #226): ClipF at clip_top 4, 8, 12, 16, 32, 35, 64. The second triangle anchors at 4*floor(ct/4)+2 at all seven. The first, flat-topped, triangle anchors at ct where ct is a multiple of 8 (8, 16, 32, 64) and at 4*floor(ct/4)+2 at 4 and 12 (35 does not separate the rules). wbuf_anchor_recover.py --selectors over the goldens plus the three console runs: 52 anchors, 34 informative, nothing of three or fewer literals fits, and 93 four-literal selectors fit all 52. All 93 give the same anchor on every existing capture, so this arm cannot tell them apart and does not try to. The one chosen here keeps the shipped `!cut` term and adds a clause about the clip edge's 8-row alignment. docs/lanes/wbuf31fix/NOTES.md explains the choice and names the silicon capture that would separate it from its rivals.\n\nMUST MOVE, BETTER, ALL THREE, BOUND IN PROSE AND NOT BY VALUE. The only captures on the disc whose emitted anchor changes are the three ClipF depth captures, and on each of them only the SECOND triangle's anchor changes: row ct becomes ct+2.\n  W_buffering/WBuf24D_ClipF-150-032_V1_ZB0_ZS1_ZB  arm A about 206,009 (16,520 from t0's +-1, 189,489 from t1 wrong)  -> predicted about 44,050\n  W_buffering/WBuf24D_ClipF-150-128_V1_ZB0_ZS1_ZB  arm A about 159,250 (all t1)                                   -> predicted about 24,500\n  W_buffering/WBuf24D_ClipF-150-224_V1_ZB0_ZS1_ZB  arm A about 112,210 (all t1)                                   -> predicted about 14,700\nThe arm A figures are the 2026-09-13 Nova arm, and wbuf_anchor_recover.py --simulate reproduces them to the pixel. The predicted figures come from the same float32 emulation of the new rule. What remains in each is off-by-one, so it depends on which way the driver rounds the last ULP of the offset. That is why these are prose bands and not expect values (the #31 TriH precedent: registering a band as a value makes a correct fix fail). The machine-checked part is must_not_regress on all three, and the claim is that each capture improves by at least half. If one improves by less than half while its t1 offset lands on target (leg M below), the pixel model is wrong, not the anchor.\n\nLEG M, THE MECHANISM LEG, AND THE ONE THAT DECIDES THIS. On arm B's own capture, bound each triangle's offset with `wbuf_anchor_recover.py --ours <arm B result>`: off in [max(z-w), min(z+1-w)] over the triangle's pixels. Hardware's intervals are: ClipF-032 t1 [73585.9574, 73585.9619], ClipF-128 t1 [30771.1983, 30771.2110], ClipF-224 t1 [16800.8127, 16800.8212]. The model at the arm A anchor gives 75290.17, 31229.44 and 16985.21. PREDICTED: arm B's t1 offsets land within 1.0 of hardware's intervals (the model is exact to under a unit everywhere else). The must-stand-still half: ClipF-032 t0 is correct today (hardware [75289.2175, 75289.2351]; arm A and arm B both anchor it at row 32) and must stay within 1.0 of that. THE WORLDS IN WHICH IT FAILS: if the selector reads false on t1, all three t1 offsets stay at 75290 / 31229 / 16985. If the flat-top clause misfires on t0, ClipF-032 t0 jumps to about 73585, and its 16,801 px go wrong. If the grid phase is wrong, t1 lands a full rung away (about 1,700 units at ct 32), not within 1.0.\n\nMUST NOT MOVE. Each leg names the patch change that would move it.\n  W_buffering/WBuf*_FloorQuad_*, W_buffering/WBuf*_RoofQuad_*: y-dominant triangles cut at clip_top 0 whose top vertex is above row 0 (Floor at y = -34.31). They move if topCut loses its `clip.y > 0.0` guard, because Floor's ytop < 0 = clip.y would then read as a window-clip top cut and take the 4-grid.\n  W_buffering/WBuf*_TriH_*: uncut, so already on the 4-grid. They move if the `!cut ||` term is lost or inverted.\n  W_buffering/WBuf*_TriV_*, W_buffering/WBuf*_WallQuad_*, W_buffering/WBuf*_ClipW-*: x-dominant with pb exactly 0, so no row anchor can reach them. They move only if the new grid choice leaks into the COLUMN, which this patch deliberately does not do.\n  W_buffering/WBuf*_ZS0*, Depth_Clamp/*: W-buffered with no slope factor, so wbufSlopeStep is emitted but its result is multiplied by zero. They move if the new statements break the emitted shader. This lane could not run glslc in its sandbox, so a GLSL compile error in the z_perspective branch is a live risk, and it would reach every W-buffered capture, these included.\n\nDROPPED, WITH THE REASON EACH DISCHARGES NOTHING. W_buffering/ZBuf*: z_perspective is false and the change sits wholly inside the z_perspective block, so no edit to these statements reaches them. W_buffering/WBuf*_LargeZ_*: its offset is 136 at every anchor, so no anchor rule moves it. W_buffering/WBuf*_LineStrip_*: clip_top is 0 in every variant, so the only change that could move it is the one the Floor/Roof leg already guards, and this lane has not checked whether any line's synthetic triangle starts above row 0. The rest of psh.c's blast radius (Bump map, Texture format and so on, from nv2a_index.py blast): their shaders have z_perspective false, the emitted GLSL is byte-identical, and nothing in this diff can reach them.\n\nDELIBERATELY UNCONSTRAINED. The three ClipF COLOUR captures (WBuf24D_ClipF-*_ZS1 with no _ZB) print the depth they read back into the frame. A correct depth change must change their glyphs, and the 09-13 prediction treats such label captures as void.",
  "a_ref": "1883f0fd52",
  "b_ref": "63a24f9834",
  "must_not_move": [
    "W_buffering/WBuf*_FloorQuad_*",
    "W_buffering/WBuf*_RoofQuad_*",
    "W_buffering/WBuf*_TriH_*",
    "W_buffering/WBuf*_TriV_*",
    "W_buffering/WBuf*_WallQuad_*",
    "W_buffering/WBuf*_ClipW-*",
    "W_buffering/WBuf*_ZS0*",
    "Depth_Clamp/*"
  ],
  "expect": {},
  "expect_counts": {},
  "must_not_regress": [
    "W_buffering/WBuf24D_ClipF-*_ZB"
  ],
  "notes": [
    "The machine-read half is guards: eight must_not_move globs, each tied to a named patch change, plus must_not_regress on the three ClipF depth captures that must move. The movers' sizes and leg M are prose. Judge them from the per-capture table and from `wbuf_anchor_recover.py --ours <arm B result>`.",
    "a_ref 1883f0fd52 is this branch's merge of origin/master at 82e460e863, and psh.c there is master's. b_ref 63a24f9834 is the one-commit change to wbufSlopeStep. Both are on lane/wbuf31fix."
  ]
}

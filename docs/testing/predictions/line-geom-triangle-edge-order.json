{
  "registered_utc": "2026-09-13T12:37:43Z",
  "who": "line-geom-tri-agent",
  "issue": "13",
  "prediction": "MECHANISM: a TRIANGLES draw under POLY_MODE_LINE keeps PRIM_TYPE_TRIANGLES through pgraph_prim_rewrite_get_output_mode(), so its three edges are emitted by OUR geometry shader (pgraph_glsl_gen_geom), not by Turnip. #13 derived silicon's order exactly on 225,558 of 225,558 decisive pixels -- for a triangle (a,b,c), paint the edge opposite a, then opposite b, then opposite c, i.e. (b,c),(c,a),(a,b) -- and landed it only for the blocks prim_rewrite.c rewrites to LINES. This arm rotates geom.c's three emit_line calls from (0,1),(1,2),(2,0) to (1,2),(2,0),(0,1).\n\nTHE FALSIFIER IS A LABEL, NOT A COUNT, and it is run by `python3 docs/testing/line_priority_arms.py --a <armA> --b <armB> --min-width 8 --max-width 63.875`. A reorder emits the same number of lines, so any counter passes identically on both arms and proves nothing; and the priority class overlaps the extent class, which is not implemented, so a per-capture pixel total can move either way without bearing on the rule. For each pixel whose golden colour names exactly one covering edge, the tool asks which edge OUR capture's colour names.\n\nTHE MODEL IS CALIBRATED ON ARM A'S OWN NUMBERS, WHICH IS WHY THESE ARE PREDICTIONS. Two different traces of prim_rewrite.c's provoking-vertex rotation reproduce the measured Tri figure and only one reproduces the measured TFan figure, so the composition was pinned from the landed arm at e1c21dd5cf instead of from reading the code: Tri's actual per-triangle edge order is [0,1,2] (the only order scoring 58.24%) and TFan's is [2,0,1] (the only order scoring 47.57%). A rotation of geom.c's emit_line calls rotates BOTH by the same amount, because GeomState::primitive_mode is the REWRITTEN mode and geom.c cannot tell a fan triangle from a list triangle. That model reproduces arm A on all eleven candidate classes to within 0.02 percentage points. My own first hand trace of the same code predicted the OPPOSITE for TFan -- 100.00% falling to 47.57% -- and the arm refuted it; that is recorded here because it is the reason the legs below are stated against the calibrated model and not against the trace.\n\nLEG 1, the Tri class -- 45,760 decisive pixels go from 58.22% naming silicon's edge to 100.00%. The world in which it fails: geom.c is not the path that orders these edges (arm B reads 58.22%, unchanged), or the rotation is the wrong way round (17.70%), or the rule as derived does not describe a bare triangle (anything else). Constrained by the goldens: 100.00% is `opp_abc`'s own score on this class, derived before this change existed.\n\nLEG 2, the TFan class -- 9,731 decisive pixels go from 47.57% to 70.36%, BETTER but NOT to 100.00%. The world in which it fails: TFan reads 100.00%, which would mean the fan's triangle is not pre-rotated and one geom.c order fixes both blocks; or it reads 47.57% (did not execute); or 17.70%/anything else, which refutes the composition model. This leg is the one that says the remaining TFan residue is rewrite_triangle_fan()'s rotation and therefore not reachable from my territory, so it must be able to fail.\n\nLEG 3, scope -- QStrip 100.00%, Quad 100.00%, Poly 100.00%, Poly/TFan 100.00%, LLoop/Tri 100.00%, Poly/Tri 100.00%, LLoop/TFan 100.00%, LLoop 99.53% and QStrip/TFan 77.17% are ALL UNCHANGED from arm A. The world in which it fails: the reorder reaches the LINES branch of the generated shader (LLoop, QStrip, Quad, Poly move), or the composition model is wrong about which edge wins in a mixed class (QStrip/TFan and Poly/TFan are the sensitive ones -- they contain one fan edge whose rank against the other block's edges decides the pixel, and the model says the winner does not change while the fan edge's rank does).\n\nLEG 4, did it execute -- `moved` on the Tri class, the count of decisive pixels where arm A and arm B name DIFFERENT edges, must be at least 19,000. |100.00 - 58.22|% of 45,760 is 19,113, so the labels must change on at least that many. This is the guard against reading a flat total as inertness: a patch that does not reach the device gives moved = 0 on every class, which is indistinguishable from a patch that reaches it and returns a different wrong answer in any pixel total. Arm A against itself gives 0 here by construction, so this leg has a world in which it fails and that world is the one that has cost this project three retractions.\n\nLEG 5, advisory and NOT counted as a falsifier -- ALL rises from 85.12% to 95.85% over 198,880 decisive pixels. Advisory because it is leg 1 plus leg 2 plus leg 3 re-added, and because the pooled figure hides which class moved.\n\nEXPECTED TO MOVE, direction unpredicted, named so a mover is not read as a surprise: every per-capture differing-pixel total in `Line width`, and every capture of `Edge flag`, `Shade model`, `Front face` and `W param` that draws a triangle-type primitive under POLY_MODE_LINE -- those are the only other four suites in the corpus that set NV097_SET_FRONT_POLYGON_MODE_V_LINE on TRIANGLES, TRIANGLE_STRIP or TRIANGLE_FAN, and they therefore reach the changed code. The direction is unpredicted for the reason #13 recorded: the decisive set is 0.1% of the ink, silicon's extent rule is not implemented, and the two residual classes overlap, so removing the priority error need not reduce any total. `Line_0064.0`-`.7` and `Line_FFFFFFFF` are VOID for the models -- their goldens' ink masks are byte-identical to `Line_0001.0`'s because the width register holds nine bits of eighths and 64.0 does not fit -- but they are NOT void for us, because this emulator reproduces the nine-bit overflow, so they should move by a couple of pixels rather than catastrophically. One statement about the goldens, applied to exactly the set whose register value overflows nine bits.\n\nNOT A FALSIFIER, recorded as an assertion about my own output rather than about silicon: `2D_Lines/*` must be byte-identical. PRIMITIVE_LINES takes the LINES branch of the generated geometry shader, which this change does not touch, so my own patch forces this true. It is worth asserting and it is not evidence.\n\nWHAT THIS DOES NOT CLAIM: nothing here bears on silicon's EXTENT rule, which is the larger half of #13 and needs plumbing across five files with an undetermined floor/ceil phase; nothing bears on the 29.64% of TFan and 22.83% of QStrip/TFan that rewrite_triangle_fan()'s rotation owns; and 45,760 + 9,731 decisive pixels is 0.1% of Line width's ink, so a 100.00% on a class is not a claim about the suite's pixel score.",
  "a_ref": "5e898fb191",
  "b_ref": "ba31c26ee0",
  "must_not_move": [
    "2D_Lines/*"
  ],
  "expect": {},
  "expect_counts": {}
}

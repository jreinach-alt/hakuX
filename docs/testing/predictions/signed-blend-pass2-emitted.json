{
  "registered_utc": "2026-09-13T22:22:04Z",
  "who": "signfold",
  "issue": "43",
  "prediction": "#43 THE SIGN FOLD, WITH PASS 2 ACTUALLY EMITTING.\n\nRoot cause of the first arm's failure, now fixed: the inline_buffer branch\nclears `inline_buffer_populated` on every attribute it consumes, and\npgraph_vk_bind_vertex_attributes_inline() builds the active attribute list from\nexactly that flag -- so pass 2 bound no vertex data and drew nothing. The fold\nhas been emitting ONE pass since it landed. The flags are now saved once and\nrestored before each pass.\n\nGrounds for registering EXACTNESS rather than an improvement, which is the\nhalf of this that can fail: the arithmetic is exhaustive on all 65,536 (S,D)\ncells and on 7,526,820 golden-recovered channels; the LOW half is already\nconfirmed exact ON DEVICE through this very shader (source < 128: 0 of 856,098\nwrong in the failed arm, so the mask and its placement after the alpha test are\nright); and the failure mode is now understood to the channel -- arm B's two\ncaptures were identical on 125,360 of 125,360 high channels and equal to the\nDESTINATION on 100.00% of them, which is pass 1 alone.\n\nF1 txt_A8R8G8B8_SADD     186,724 -> 0 differing pixels (BIT-EXACT)\nF2 txt_A8R8G8B8_SREVSUB   64,475 -> 0 differing pixels (BIT-EXACT)\n   FAILS IF pass 2 still does not emit, if the restored flags are insufficient\n   (some other consumed state), or if the per-pass blend op is wrong.\n\nF3 CHANNEL LEVEL, the direct instrument, via signed_blend_source_halves.py:\n   source >= 128 wrong 365,491 -> 0 for SADD, and 125,704 -> 0 for SREVSUB;\n   source < 128 stays 0 of 856,098 for both. This is the leg that cannot be\n   satisfied by accident -- note SREVSUB's arm-A-side 125,704 is the D = 127\n   subset only, because the D = 255 high channels were ACCIDENTALLY right\n   (a bare destination saturates to 255, and so does the correct answer).\n\nF4 Both statuses label-differs -> ok. The label carries the same defect: 7,192\n   of the high channels sit in rows 0-63, and SREVSUB's status already flipped\n   to ok when its D = 255 channels went accidentally right. So this is a\n   renderer property, not a different build of nxdk_pgraph_tests, and these\n   captures are symptomatic rather than void.\n\nF5 THE MECHANISM COUNTER, read from logcat tag `hakuX` at INFO, lines matching\n   `[signfold]`: emitted == 2*folds and empty == 0.\n   TWO instrument faults were fixed to make this leg readable at all, and both\n   are the reason it is registered rather than assumed. It counted the\n   inline/draw-array LENGTHS, which survive both passes untouched, so it would\n   have read emitted == 2*folds on a run where pass 2 drew nothing; it now\n   counts `populated`, the state the bind actually consumes. And it logged to\n   tag hakuX-signfold, which the dispatcher's capture spec silences via `*:S`\n   -- the 0b8956e7c3 arm printed it and the logcat holds not one line, which is\n   indistinguishable from the mechanism never firing.\n   FAILS IF no [signfold] line appears at all, which would mean the counter is\n   still unreachable and this leg is VOID rather than passed.\n\nF6 BOUNDED, still a bound and still says so: the 15 #spot_*_SADD fall from\n   96,869 and the 15 #spot_*_SREVSUB from 100,980 to a value in\n   [7,350, 19,140]. 19,140 is the cardinality of the UNION of the 75 unsigned\n   seam masks and the individual unsigned residuals run 6,154..19,140, so no\n   exact value is derivable; 7,350 is a hard floor because those pixels are\n   seam-only, carry no source >= 128 channel, and the fold cannot touch them.\n   Factor-independence must SURVIVE: one distinct value across all 15 of each.\n\nGUARDS, 124 of 156, leaving unguarded exactly the 32 the fix targets, verified\nby fnmatch against the arm's own capture list:\n  Blend_surface/* (32); Texture_signed_component_tests/A8R8G8B8_* (16);\n  txt_A8R8G8B8_ADD (1, the bit-exact unsigned control on the same path);\n  Blend_tests/#spot_*_{ADD,SUB,REVSUB,MIN,MAX} (75).\n  These fail in the world this realistically breaks: the fold leaking into\n  unsigned draws, or the restored attribute flags perturbing an unsigned draw's\n  vertex binding. The second is new with this change and is why the 75 unsigned\n  #spot captures matter more on this arm than on the last.\n\nKNOWN GAPS, unchanged and not claimed: a draw whose own primitives overlap, and\ndepth/stencil re-application on pass 2. Neither is reachable in this corpus --\nevery quad in blend_tests.cpp is its own Begin/End and merging is refused.",
  "a_ref": "322adc3a01",
  "b_ref": "8d2bf50075",
  "must_not_move": [
    "Blend_surface/*",
    "Texture_signed_component_tests/A8R8G8B8_*",
    "Texture_signed_component_tests/txt_A8R8G8B8_ADD",
    "Blend_tests/#spot_*_ADD",
    "Blend_tests/#spot_*_SUB",
    "Blend_tests/#spot_*_REVSUB",
    "Blend_tests/#spot_*_MIN",
    "Blend_tests/#spot_*_MAX"
  ],
  "expect": {
    "Texture_signed_component_tests/txt_A8R8G8B8_SADD": 0,
    "Texture_signed_component_tests/txt_A8R8G8B8_SREVSUB": 0
  },
  "expect_counts": {}
}

{
  "registered_utc": "2026-09-25T14:48:44Z",
  "who": "lane.wbuf31sel",
  "issue": "31",
  "prediction": "#31: replace the refuted flatTop clause. The change under test is one emitted GLSL statement in wbufSlopeStep (glsl/psh.c) plus hoisting the existing local `first` out of its loop: in the window-clip top-cut exception, 'the triangle is flat-topped' becomes 'the first covered span does not start at the clip's left edge' (first != clip.x). Every other term of #222's rule is unchanged.\n\nWHY. Silicon, ClipF at clip_left 300 / clip_top 8 (PR #243, 2026-09-25): t0 anchors at 10.000 (the 4-grid); flatTop said 8. wbuf_anchor_recover.py --selectors over the goldens plus all four 2026-09-25 console runs leaves 57 four-literal fits; this one is `!cut | (cut_top_by_clip & (span_starts_at_clip | !ct%8==0))`, the only survivor that keeps every other term of the shipped rule. Its new literal reads `first`, which the shader already computes from the three vertices and the clip rect; it does not read the anchor column. docs/lanes/wbuf31sel/NOTES.md has the 57-fit computability table.\n\nMUST NOT MOVE: EVERY CAPTURE IN BOTH SUITES. ClipF-300-008 is not on the golden disc. Everywhere the disc reaches, the new literal agrees with flatTop: at clip_top 0 (Floor, Roof, Wall, ClipW, TriH, TriV, LargeZ, LineStrip) topCut is false and the clause is never read. On the three ClipF depth goldens (clip_left 150, clip_top 32/128/224, all 8-aligned) t0 is flat-topped with its span at the diagonal (x about 294), and t1 is not flat-topped with its span at the clip (x 150), so both literals give quad for t0 and grid for t1. The --simulate split is identical to sel's on every golden capture.\n\nTHE WORLDS IN WHICH IT FAILS. (1) If first != clip.x wrongly reads TRUE on t1 (for example if `first` kept its initial 0 because the hoist broke), ClipF-150-032/128/224 t1 fall back to the 2x2 snap. Their depth captures then regress to arm-A-of-#222 levels (about 189,489 / 159,250 / 112,210 px wrong), and the ClipF colour captures' glyphs change. (2) If it wrongly reads FALSE on ClipF-150-032 t0, t0 jumps to row 34 and its 16,801 px go wrong. (3) A GLSL compile error in the z_perspective branch moves every W-buffered capture, ZS0 and Depth_Clamp included. Any golden moving means the chosen selector disagrees with flatTop somewhere the old evidence pinned: report which capture, and do not refit.\n\nTHE POSITIVE EVIDENCE IS OFFLINE and no golden can show it: wbuf_anchor_recover.py --simulate over the console capture of ClipF-300-008 predicts 117,722 exact / 33,578 +-1 / 0 wrong under this rule, against 108,412 / 17,936 / 24,952 under master's (all of t0 wrong).",
  "a_ref": "21946df29b",
  "b_ref": "f32946d5ea",
  "must_not_move": [
    "W_buffering/*",
    "Depth_Clamp/*"
  ],
  "expect": {},
  "expect_counts": {},
  "must_not_regress": []
}

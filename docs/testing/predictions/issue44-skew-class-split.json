{
  "registered_utc": "2026-09-13T15:35:00Z",
  "who": "skew-bound",
  "issue": "44",
  "a_ref": "d97d506514",
  "b_ref": "ec7f50e859",
  "judge": "docs/testing/border_swatch_classes.py --a A.json --b B.json (fed from border_swatch_origin.py --json)",
  "prediction": "AN ADDITIONAL, MECHANISM-LEVEL EXPECTATION for the same two arms as issue44-fifo-skew-bound.json, registered as a SEPARATE file rather than by editing that one, because it was derived after that prediction was registered -- from captures already on disk, with no device run, and before either arm was claimed. Nothing in the original file is changed or reinterpreted; this adds a reading the original's S1 cannot give. WHY IT IS NEEDED: `stale_px` is a SUM over swatches that fail for structurally different reasons, and #44 is now worked by two lanes with two mechanisms -- this lane's pushbuffer skew, and the texture lane's decoded-length hash gate that declines to re-upload at all (`blind=2` measured directly, on pass-2 4x4 and pass-2 8x8). AGENTS.md's rule applies exactly: a class count is a count of pixels a mechanism TOUCHES, not of pixels it is solely responsible for, and subtracting one from a differing total assumes an additivity these classes do not have. Judging a two-cause capture on one total is how a correct fix gets reverted. THE SPLIT, computed on the published ten-run noise floor (dispatch 1789255594-exp54-stst-correct-3660183, reproduced on this tree to the pixel: stale_px 146/0/2352/0/0/2430/0/1847/5640/181, races_lost 1/0/2/0/0/3/0/2/3/1, unexplained_px 0 on every run): of 12,596 wrong pixels in 12 failing swatches, TORN is 8 swatches and 3,852 px (30.6%), COMPLETE is 3 swatches and 8,064 px (64.0%), NO-SUCCESSOR is 1 swatch and 680 px (5.4%). TORN means a finite `cut` with `prefix_ok` -- the stale set is a row-major prefix of the successor's write, so the read landed mid-write; the offset differs every run (116, 175, 207, 229, 405, 432, 506) and nothing but a read during a write in flight produces a frontier at an arbitrary offset. COMPLETE means no `cut` and the successor explains every wrong pixel: the successor's write had finished when the read happened, so a skew of one whole iteration rather than a partial one. NO-SUCCESSOR means `successor == 0` with pixels still wrong; on this floor it is pass-2 4x8, the LAST swatch of the test, which has no successor at all. T1 (THE MECHANISM, and it is what this lane's bound is actually responsible for): in arm B, TORN px == 0 and COMPLETE px == 0, on every run. Both are skews and the bound removes the skew, so both must go. FAILS if either survives, and then the bound is not doing what its impossibility argument says -- which is a different and much more useful failure than a non-zero total. T2 -- WITHDRAWN before any arm was claimed, see `retraction`; it rested on an unchecked reading of the tool's `successor` field. T2b REPLACES IT: UNMODELLED-SUCCESSOR px == 0 in arm B as well, so all three classes go to zero and S1 and T1 agree. The reasoning: `border_swatch_origin.py:255` sets `succ = None` for the test's final swatch because its successor is the NEXT TEST's first write, which the tool does not model -- the 680 px are matched by the `1x1` candidate, consistent with exactly that. A cross-test successor is still a skew, and the bound holds the guest at EVERY submission including the next test's, so it should close this class too. FAILS if the class survives in arm B while torn and complete go to zero, and then the bound reaches within-test submissions but not across a test boundary, which would point at the disc runner or at a path that advances DMA_PUT without a kick rather than at the bound. T3 (THE DIRECTIONAL ARGUMENT THAT SEPARATES THE TWO LANES' MECHANISMS, and it can fail): COMPLETE px must be explained by the SUCCESSOR and not by a predecessor. A texture that was never re-uploaded holds content from an EARLIER upload and therefore cannot show the successor's surface, which is written after the draw that is missing it -- so a missed re-upload does not reach the COMPLETE class, and the texture lane's hash-gate mechanism should leave it untouched. FAILS if any COMPLETE swatch in either arm is better matched by a predecessor than by its successor, and then the directional argument is wrong and the two mechanisms are not separable this way. Noted against it, from #44 itself: on 2 of 11 losses the successor is not UNIQUELY identified -- pass-1 8x2's +1/+2/+3 all match 2,048 of 2,048 -- so this leg is about predecessor-versus-successor and not about which successor. T4 -- WITHDRAWN before any arm was claimed, see `retraction`. It asserted #44's 100% headline was really 94.6%; the headline is about the eleven swatches with a modelled successor and it stands.",
  "must_not_move": [
    "arm A's per-class split stays within the noise floor's shape: TORN present on most failing runs, COMPLETE on the pass-1 8x2 and 4x8 swatches",
    "unexplained_px == 0 on every run of both arms"
  ],
  "expect": {
    "T1/armB_torn_px": 0,
    "T1/armB_complete_px": 0,
    "T2b/armB_unmodelled_successor_px": 0,
    "T3/armB_complete_predecessor_better_matches": 0,
    "noise_floor_torn_px": 3852,
    "noise_floor_complete_px": 8064,
    "noise_floor_unmodelled_successor_px": 680
  },
  "expect_counts": {},
  "note_refs": "Same arms as issue44-fifo-skew-bound.json -- a_ref d97d506514, b_ref ec7f50e859, one line apart. Derived entirely from captures already on disk, committed before either arm was claimed, and deliberately a NEW file: editing the registered prediction after finding this would be the widening the content hash exists to catch, even though the finding predates the measurement. The two are judged together -- S1 is the bar #44 set and T1 is the mechanism; they can disagree, and T2 says in advance which way.",
  "retracted_legs": [
    "T2",
    "T4"
  ],
  "retraction": "T2 and T4 are WITHDRAWN, before either arm was claimed and for the same single cause: both rested on a reading of border_swatch_origin.py's `successor` field that had never been checked against the code producing it. `border_swatch_origin.py:255` sets `succ = None` for the FINAL swatch of the test -- its own comment says 'the last swatch's successor is a write by the NEXT TEST, whose content this tool does not model' -- so `successor == 0` there is DEFINITIONAL, not measured. T4 claimed #44's headline 'the immediate successor explains 100% of wrong pixels' was really 94.6% (11,916 of 12,596). That is wrong: the headline is a claim about the eleven swatches with a MODELLED successor and it stands. The arithmetic was valid inside a model nobody had measured, which is the failure AGENTS.md records as 'an inference can be valid and still wrong'. T2 claimed the 680 px class was not this bound's to close, on the reasoning that the final swatch has no successor so no run-ahead could produce it. Also wrong, and it made the prediction WEAKER than it should be: the final swatch's successor is the next test's first write, a cross-test successor is still a skew, and the bound holds the guest at EVERY submission including that one. Replaced by T2b below, which predicts the class goes to zero. T1 and T3 are unaffected: both key on `cut`, whose semantics the tool's docstring states ('index into the successor's row-major source write ... None for a write already complete'), and that is the difference between the part of this split that survives the retraction and the part that does not. The class shares are unchanged -- torn 8 swatches / 3,852 px, complete 3 / 8,064, unmodelled-successor 1 / 680."
}

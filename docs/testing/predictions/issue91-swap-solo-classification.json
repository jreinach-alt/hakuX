{
  "registered_utc": "2026-09-18T21:02:42Z",
  "who": "lane.blitsafe",
  "issue": "91",
  "prediction": "DIAGNOSIS ARM, NOT A FIX ARM, and it is registered that way on purpose: nothing in vk/surface.c changes between this pair and the #88 pair. a_ref is #88's arm A and b_ref is #88's arm B, unchanged; the only thing that differs is the DISC, which is narrowed to Color_zeta_overlap::Swap alone. #88's arm measured Swap 165,447 -> 304,750 on the three-suite disc 3-suites:e0a8f913, deterministic 5/5 in both arms, on a registered must_not_move. This arm asks ONE question: is that regression intrinsic to Swap, or is it an earlier test's state leaking forward? That is make_test_iso.py's solo/pair classification -- correct alone means contamination, still wrong alone means a state distinction we do not implement -- and the two answers need opposite fixes, which is why it is worth an arm before any code. WHAT IS ALREADY ESTABLISHED WITHOUT A DEVICE, and it corrects [issue.91]'s own attribution. The issue attributes the regression to the missing depth attachment: zeta declines, so there is no zeta binding, pDepthStencilState is NULL, and the quad draws with no depth test. THE CAPTURES REFUTE THAT. The three colour populations are partitioned IDENTICALLY in both arms -- 165,447 quad, 139,303 background, 2,450 text -- and a change to depth testing moves the boundary between populations. Nothing moved. Only the background's VALUE changed, and its byte signature names the mechanism exactly: golden and arm A hold #242424 alpha FE, which in memory is the bytes 24 24 24 FE, the word 0xFE242424, and that is the test's own clear colour from PrepareDraw(0xFE242424, 0); arm B holds #000024 alpha 00, which is the bytes 24 00 00 00, the word 0x00000024 -- the same clear colour with BITS 8-31 ZEROED AND BITS 0-7 PRESERVED. That is precisely a Z24S8 depth-only clear of 0 written over it: the depth field is bits 8-31, the stencil byte is bits 0-7 and is left alone, and the surviving 0x24 is the clear colour's own low byte. So in arm B the DEPTH CLEAR REACHED THE COLOUR SURFACE. A clear landing on the wrong attachment, not a test being skipped -- which points at pgraph_vk_clear_surface() rather than at the pipeline state, and those are different files and different fixes. Noted while reading it: the INLINE clear path in vk/draw.c guards correctly, `if (write_zeta && r->zeta_binding)`, so the fall-through pipeline clear is where to look. WHAT IS NOT ESTABLISHED, and why this is a diagnosis rather than a fix. Reading update_surface_part() against TestSwap() does not reproduce the decline firing inside Swap at all. SET_CONTEXT_DMA_COLOR sets surface_color.buffer_dirty (pgraph.c:2386), so colour rebinds to the zeta address FIRST; zeta then asks for the colour address and finds an object that is no longer r->color_binding, so `surface == other` is false and the decline does not fire. Also found while checking it, and worth its own note: SurfaceShape carries NO ADDRESS, so framebuffer_dirty() cannot see a DMA-context swap. I could not close the gap between \"the decline does not fire in Swap\" and \"Swap measurably moved\", and I am not going to fit a fix to the half I could not establish. ColorIntoZeta and ColorIntoZeta_ZB run BEFORE Swap in the suite and the decline changes the pg-level state they leave behind -- it clears surface_zeta.buffer_dirty and leaves zeta_binding absent -- which is the contamination candidate this arm tests. THE LEG, and it is one leg because one leg answers the question. Color_zeta_overlap/Swap must_not_move between the two arms. On a disc containing only that test, if the regression is contamination the two arms come back BIT-IDENTICAL and the leg passes; if it is intrinsic to Swap the leg fails and the control-flow reading above is wrong. Genuine in both directions and neither outcome is forced by anything I have done: no code differs between these refs beyond what #88's pair already differed by, and the narrowing cannot itself create or remove a difference BETWEEN arms because both arms run the same disc. NO ABSOLUTE IS REGISTERED, DELIBERATELY, and #89 is the reason. #89 measured Color_zeta_overlap/Swap_ZB at 0 on the nine-capture disc and 141,125 on the 236-capture iso_surf1 -- same device, same renderer, same binary -- so a narrowed disc can move a capture in this very suite by 141,125 px. Any absolute I carried over from the three-suite arm, 165,447 or 304,750, could therefore be violated by composition alone and would read as a refutation of the mechanism. Arm A's own solo value is the only sound baseline for a solo disc, and this arm produces it; it is an output of the measurement, not an input to it. For the same reason NO FIGURE FROM THIS ARM may be compared with the three-suite figures or with iso_surf1's in either direction. NOT A FALSIFIER, registered so it is not totted up: that the two arms differ in vk/surface.c alone is a property of the refs, already verified by diff, and not a prediction about this run. NOT PREDICTED: the absolute value of Swap on a one-test disc, on either arm; whether Swap_ZB, which is 0 in both arms of the three-suite pair, stays 0 -- it is not on this disc; and anything about #89, which needs the full iso_surf1 composition and cannot be reached from here. NOT MEASURABLE ON THIS ARM AT ALL, said so it is not silently assumed: the Khronos validation layer, which is what a change to framebuffer attachments most wants and which is desktop-only while the desktop build is a known named gap on this host. A depth clear reaching a colour attachment is exactly the class it would catch, and no capture can stand in for it because a validation error need not move a pixel.",
  "a_ref": "f343580647",
  "b_ref": "4726557b0f",
  "must_not_move": [
    "Color_zeta_overlap/Swap"
  ],
  "expect": {},
  "expect_counts": {},
  "must_not_regress": []
}

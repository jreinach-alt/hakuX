{
  "registered_utc": "2026-09-18T22:14:32Z",
  "who": "tier81fix",
  "issue": "81",
  "prediction": "#81 TIER-1 PROMOTION DEDUP + exec_count RESET, and #90 RULED OUT OFFLINE.\n\nARM A0 = 54f0286362 (census instrument only, NO behaviour change).\nARM B  = 7dfa94c403 (A0 + dedup on (pc,cs_base,flags) + reset of the source\nexec_count, with the budget charge moved to the caller so a dedup hit is not\ncharged). Five separate 240 s Crimson Skies soaks per arm, thor bdc158a5,\nqueued under one --who. The replicate is the RUN, not the window.\n\nTHERE IS NO CAPTURE ORACLE FOR THIS AT ALL. [issue.81] and [issue.90] both\ncarry no_suites: this is guest-CPU core, no golden suite scores it, and\nnxdk_pgraph_tests is nearly all static content so it could not exercise the\npromotion path even if one did. Every leg below is a LOGCAT COUNTER leg, read\nby the lane off tag hakuX-tier1 at priority D. That tag is confirmed live in\nthe RUNNING dispatcher snapshot (dispatch/bin/dispatcher.sh:1042) and, more to\nthe point, empirically: six Crimson Skies soaks from 2026-09-18 11:10-11:31\ncarry promote/consume/requests-FULL lines. That is the positive control, and\nit is stronger than reading the spec, because the tag was once silenced by\n*:S before anyone asked the question and every logcat then read zero.\n\nEXACT vs BOUNDED, because mixing them is how a bound becomes a value. The\nthrottle on promote/dedup is (count < 10 || count % 10000 == 0), so indices\n0-9 are EXACT and anything above is a BOUND. consume is exact at these rates\n(1-4 per run observed). The census line is exact and one-shot. NO PROMOTION\nTOTAL IS REPORTABLE IN EITHER ARM and no leg below asks for one.\n\nWHAT ARM A0 IS FOR. #81's central claim -- 64 slots filling with DUPLICATES of\na few pcs -- has never been measured; it is inferred from 8 of the first 10\npromote lines naming pc=0x402fff inside one millisecond, plus the FULL line.\nA0 measures it. If A0 comes back with distinct_pc near 64, THE ISSUE'S CENTRAL\nCLAIM IS REFUTED and arm B's dedup is pointless; that outcome is registered as\nL1 failing and must be reported as a refutation, not smoothed over.\n\nLEGS.\n\nL0a A0-census-line-present-runs = 5. The census line appears in all five A0\nruns. INSTRUMENT CONTROL. If absent from a run that shows requests FULL, the\ninstrument did not compile in and that run is VOID -- absence here is not\nevidence of an empty table.\n\nL0b A0-census-valid-equals = 64. valid MUST read 64/64: we reach the census\nonly because the free-slot scan found nothing. Any other value means the\ncensus is wrong, not the table. This is the impossible-row control.\n\nL1 A0-distinct-pc-at-most = 16. In at least 4 of 5 A0 runs, distinct_pc <= 16.\nTHE MEASUREMENT, and not forced by anything this lane wrote: the guest decides\nhow many distinct pcs cross the threshold, and the value can legally be\nanything in 1..64.\n\nL2 B-dedup-line-present-runs = 5. \"dedup #0\" present in all five B runs.\nPATCH-LIVE CONTROL ONLY, and near-tautological given duplicates exist -- it\nis listed as a control, not as evidence. If absent, arm B is VOID.\n\nL3a B-distinct-key-equals-valid-runs = 5. distinct_key == valid in all five B\nruns. FORCED BY THE PATCH and therefore NOT EVIDENCE -- a dedup on\n(pc,cs_base,flags) makes this true by construction. Registered so it is on\nrecord as the patch's own description rather than smuggled in as a result.\n\nL3b B-distinct-pc-at-least = 32. In at least 4 of 5 B runs, distinct_pc >= 32.\nThe NON-forced half: dedup pins distinct_key, not distinct_pc, because two\ndistinct keys may share a pc.\n\nL4 B-runs-with-consume-pc-outside-known-set = 3. In at least 3 of 5 B runs, at\nleast one consume line names a pc OUTSIDE {0x32e338, 0x212834} -- the only two\npcs ever consumed across all six A-side soaks. THIS IS THE LEG THAT MATTERS\nAND THE LANE EXPECTS IT TO FAIL. Freeing 61 slots only helps if something is\never retranslated for those pcs, and per #73 the masked lookup means a request\nis consumed only for a block the guest OVERWRITES. If L4 fails while L2/L3b\npass, the diagnosis is that M2 is PALLIATIVE and the binding constraint is the\nmasked CF_INVALID comparison, not the duplication -- a result about the model,\nnot a reason to revert the patch. Nothing in this patch touches the lookup, so\nthe leg is not forced in either direction.\n\nL7 B-promote-reaches-index-at-least = 9. promote #9 present in all five B runs:\nthe promotion path is still live. Pairs with L3a against the trivial cheat --\nthe easy way to get \"no duplicates in the table\" is to stop promoting, which\nfails L7 and also erases the census L3a is read from. No inert change and no\ndisabled feature passes L2, L3b and L7 together.\n\nL8 B-gfps-median-within-A0-band = 1. hakuX-perf gfps median in arm B inside\nthe A0 run-to-run band. The dedup adds an O(64) scan; the reset is what bounds\nit to once per threshold executions rather than once per execution, so this is\nthe leg that catches the reset failing to do its job.\n\nMUST-NOT-REGRESS GUARDS: consume count >= 1 in every B run (the mechanism is\nnot switched off); the two refs differ by exactly one commit touching exactly\none file; device_label identical across all ten results.\n\nWHAT THIS INSTRUMENT CANNOT SEE, stated before any zero is allowed to count.\n(1) SLOT OCCUPANCY OVER TIME -- the census is ONE sample at the FIRST drop. If\nthe table cycled full/empty, this instrument would show nothing; it prints\nonce per process. (2) tb_flush -- no log site exists. The translate-all.c\nbuffer-full site never fired in 6 of 6 A-side runs, so no overflow-driven\nflush; a flush from any other cause is invisible, and would erase the table\nwithout the census noticing. (3) BUDGET-DENIED PROMOTIONS --\ng_tier1_promotions_dropped has no Android reader (only a qemu_printf that\ncannot reach logcat; xemu_get_tier1_stats() has zero callers tree-wide), so\npromotion PRESSURE stays a loose lower bound in both arms. This patch narrows\nwhat that counter counts and does NOT make it readable; xemu_android.cpp is\nnot in this lane's territory. (4) HOW MANY TIMES A BLOCK RE-ASKED -- dedup\ncollapses that into a throttled dedup counter, a bound. (5) ONE TITLE, ONE\nDEVICE -- Crimson Skies on the thor. Nothing here speaks to other titles.\n(6) NO CAPTURE MOVES -- a soak writes no captures, so ab_compare computes no\nverdict and the synthetic leg keys above BIND TO ZERO ROWS by construction.\nThe prediction's value is its sha, recorded at queue time, which makes a leg\nquietly widened after the numbers arrive detectable. PRE-REGISTERED here means\nBOUND, NOT MACHINE-CHECKED, and the count of legs that bound to rows is ZERO.",
  "a_ref": "54f0286362",
  "b_ref": "7dfa94c403",
  "must_not_move": [
    "consume count >= 1 in every arm-B run (mechanism not switched off)",
    "the two refs differ by exactly one commit touching exactly one file",
    "device_label identical across all ten results"
  ],
  "expect": {
    "L0a-A0-census-line-present-runs": 5,
    "L0b-A0-census-valid-equals": 64,
    "L1-A0-distinct-pc-at-most": 16,
    "L2-B-dedup-line-present-runs": 5,
    "L3a-B-distinct-key-equals-valid-runs": 5,
    "L3b-B-distinct-pc-at-least": 32,
    "L4-B-runs-with-consume-pc-outside-known-set": 3,
    "L7-B-promote-reaches-index-at-least": 9,
    "L8-B-gfps-median-within-A0-band": 1
  },
  "expect_counts": {},
  "must_not_regress": []
}

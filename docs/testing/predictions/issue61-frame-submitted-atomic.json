{
  "registered_utc": "2026-09-13T06:51:00Z",
  "who": "vk-surface-agent",
  "issue": "61",
  "prediction": "MECHANISM (judge this, not a pixel total): PGRAPHVkState.frame_submitted[] is cross-thread. It is written with qatomic_set from the submit worker (vk/submit_worker.c:61) and the render thread (vk/render_thread.c:153), and read with qatomic_read at six sites. Two read it PLAINLY, and both gate something irreversible: surface_in_flight() (vk/surface.c) answers get_any_compatible_invalid_surface(), which hands an invalidated surface's VkImage to a new draw as its render target, and prune_invalid_surfaces(), which destroys it; and draw.c's any_in_flight loop rewinds descriptor_set_index and push_ubo_set_index, which is #34's finding 1. Both sit inside QTAILQ_FOREACH_SAFE / for loops around the read, which is where a hoisted load costs most. This change makes exactly those two reads qatomic_read and leaves the two wait_idle-guarded plain reads (draw.c:815 in the finalize path, draw.c:2436 in pgraph_vk_flush_all_frames) alone, because pgraph_vk_render_thread_wait_idle(r) immediately precedes each and quiesces both writers.\n\nTHE MEASURED QUANTITY IS WHETHER THE RACE WINDOW IS EVER HIT ON THIS DEVICE, not a pixel delta, and the two outcomes are both informative:\n  - If NO capture moves: the window was not being hit in these 255 captures on this handheld. The change is then a correctness guard against a compiler transformation and an ARM ordering exposure that this run did not exercise, and must be reported as a code-quality fix rather than as a measured one. This is the expected outcome and the one I am predicting.\n  - If ANY capture moves: the reads were NOT previously benign. That is a positive finding about #61's reachability -- get_any_compatible_invalid_surface() was recycling an image the GPU still held, or the descriptor ring was being rewound under a live command buffer -- and it makes #61 a live defect rather than an inspection finding. It does not make this change wrong; it makes it measurable.\n\nFALSIFIED IF a capture moves in a suite that has no surface invalidation or shelving to churn. The model says the only reachable exposure on this disc is the surface-lifetime one, so a move must land in the suites that invalidate or shelve surfaces at one address -- Surface_clip (which is #29's own suite, where a shelved surface's freed view was dereferenced on every run), Surface_format, Texture_render_target, Image_blit, Texture_Framebuffer_Blit, Null_surface. A move in Blend_surface or Depth_buffer_fixed_function, which render every swatch into one stable surface and do not exercise the invalid list, would refute the model: it would mean the atomic load changed something other than the visibility of a foreign thread's write, and the change should then be re-derived rather than kept.\n\nSEPARATING \"IT DID NOT EXECUTE\" FROM \"IT EXECUTED AND THE MODEL IS WRONG\": a pixel count cannot do this for an atomicity change, because a fix whose window is never hit and a fix that does nothing produce the same zero. So the two are separated OUTSIDE the pixel count, by two different instruments. (a) Whether the change is in the binary at all is a property of the diff and of the compiled ref, not of the captures: qatomic_read appears at vk/surface.c surface_in_flight() and at draw.c's any_in_flight loop, the two wait_idle-guarded plain reads are byte-unchanged, and the dispatcher records the APK sha of arm B. I assert this as a statement about my own output and DO NOT count it as a falsifier, per orchestration.md's rule on legs a patch forces true. (b) Whether the window is hit is answered by the must-not-move table itself, in the two directions set out above. There is deliberately no leg of the form \"the differing count falls\", because nothing in this change alters a rendering rule and any such leg would be either vacuous or fitted.\n\nMUST NOT MOVE, and why these eight suites: all eight are on the disc (disc_id 8-suites:ce60c492), so every glob below matches captures in both arms -- the previous #50 arm registered nine globs that matched nothing and was scored FAIL on the guard never having been applied, which is a process failure and not a measurement. The eight are the surface- and texture-path suites: they create, invalidate, shelve and recycle surfaces and read them back, so they are where surface_in_flight()'s verdict is consumed. Surface_clip is the most load-bearing of them because #29's use-after-free was found there. The 79 captures that are currently bit-exact are the most sensitive rows in the table: a correctness change that costs one of those has taken something that was right. Six are named individually below alongside three high-residual Surface_clip captures, so the guard is pinned to rows and not only to globs.\n\nNOT falsified by: the run-to-run band. ab_compare measures the band with --runs and classes a delta inside it as NOISE; a single-run flip on a capture known to be bimodal on one binary (see 2D_BorderTex_SZ, eleven runs, five bit-exact and five not) is not a move. If a capture moves, it must be shown to move outside the band before it counts either way.",
  "a_ref": "91255df20f",
  "b_ref": "b62ee8907e",
  "must_not_move": [
    "Blend_surface/*",
    "Depth_buffer_fixed_function/*",
    "Image_blit/*",
    "Null_surface/*",
    "Surface_clip/*",
    "Surface_format/*",
    "Texture_Framebuffer_Blit/*",
    "Texture_render_target/*",
    "Surface_clip/DebugTextShouldClip",
    "Surface_clip/XemuBug420",
    "Surface_clip/rt_x0y0_w512h384",
    "Surface_clip/rt_x0y0_w640h480",
    "Surface_clip/rt_x0y240_w640h240",
    "Blend_surface/1-DstAlpha_ARGB8",
    "Blend_surface/DstAlpha_X_O1RGB5",
    "Null_surface/*"
  ],
  "expect": {},
  "expect_counts": {}
}

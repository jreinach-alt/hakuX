{
  "registered_utc": "2026-09-13T13:08:04Z",
  "who": "desktop-lane-agent",
  "issue": "60",
  "prediction": "MEASURED, before the change was written, with a probe that records the requested guest format on every binding and reports every consumer read where the binding's held format differs from it. Full OpenGL run of iso_surf1. #60's mechanism was already established (check_surface_compatibility compares the GL view -- gl_attachment, gl_internal_format, pitch -- and never shape.color_format, and seven of ten guest colour formats share a GL row); what was NOT established, and what the issue gets wrong, is that it is latent. IT IS NOT. 178 reuses on this disc carry a stale format field, and 82 of them are READ by pgraph_gl_check_surface_to_texture_compatibility(), which is not Android-gated and decides whether a rendered surface may be sampled directly as a texture. Split, all at addr 026eb000 against texture format 0x12 (LU_IMAGE_A8R8G8B8): held=7 asked=6 x48, held=4 asked=8 x30, held=5 asked=4 x2, held=2 asked=1 x2. Evaluating the function's switch by hand for each pair, three of the four are false under both the held and the asked format and so decide nothing; the fourth, held=4 (X8R8G8B8_Z8R8G8B8) against asked=8 (A8R8G8B8), is false today and TRUE on the true current format, because case A8R8G8B8 lists LU_IMAGE_A8R8G8B8 and case X8R8G8B8_Z8R8G8B8 does not. So 30 surface-to-texture decisions per run are currently made on a format the surface does not have, and the correct answer is the opposite of the one we give. Note the Vulkan consumer audit in b6239ccb concluded its own shape refresh would be pixel-inert because every vk-side reader of shape's format is diagnostics; that does not carry over -- GL has twelve readers, eight of them the Android pack/unpack conversions and two the surface-to-texture tests above, and those two are live on desktop. THE CHANGE: on the is_compatible reuse path, refresh only the format field belonging to the role being updated, and take it from pg->surface_shape rather than from entry. Both restrictions are from the code, not taste: entry->shape is the COLOUR BINDING'S shape entire on a zeta update with a colour binding present, so refreshing color_format from entry on the zeta path would copy a possibly-stale value forward, and 10 of the 178 reuses are exactly that shape (Z-role, held c=8 asked c=3); and shape's other six fields are geometry, which belongs to the image and not to the request, since a non-strict match may reuse a surface larger than asked for. FALSIFIERS. (1) No capture regresses against d662ba2b over all 236. Genuine and the whole risk: taking the surface-to-texture path where we previously refused it samples the live surface instead of re-reading guest memory, and those differ whenever the surface has not been downloaded. (2) Vulkan byte-identical over 236 -- this touches gl/ only. (3) The 82 stale consumer reads go to zero when the probe is re-run against the fixed build. Weak, and registered as weak: it is close to forced by the edit, though not entirely, since a refresh on the colour path does not touch bindings reached only through the zeta path. NOT A FALSIFIER: 'the reuse path now assigns shape.color_format' is the edit. NOT PREDICTED, and this is the interesting one: whether anything IMPROVES. I do not know which of the two paths produces the right pixels for those 30 decisions. The refusal we give today falls back to a guest-memory read, which is the conservative answer and may well already be correct; if so this change is inert on pixels and is a correctness fix only, exactly as the Vulkan version turned out to be. expect_counts worse=0 is the claim; better is deliberately unbounded and unclaimed.",
  "a_ref": "d662ba2b",
  "b_ref": "",
  "must_not_move": [
    "Vulkan/*"
  ],
  "expect": {},
  "expect_counts": {
    "worse": 0
  }
}

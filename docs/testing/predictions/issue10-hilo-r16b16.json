{
  "registered_utc": "2026-09-13T04:40:00Z",
  "who": "hilo-r16b16-agent",
  "issue": "10",
  "prediction": "#10, the HILO dot mapping for R16B16. TWO MECHANISMS, both derived offline from the goldens before anything was built, with a new oracle (docs/testing/bump16_oracle.py) that models the whole Test16bit path from literals in bump_map_tests.cpp:211. The falsifiers below are MAPPED VALUES AT NAMED TEXELS AND NAMED PIXELS, not pixel totals; the totals are reported as a band and are not what decides this.\n\nTHE ORACLE'S OWN VALIDATION GATES, both met before any rival was believed. (1) The integer blend reproduces the only two colours these goldens hold inside the quads, (254,0,0,255) and (33,32,32,191). (2) Modelling psh.c's OWN shader -- eight-bit halves, sign flags ignored, CLAMP on the dependent lookup -- reproduces our captures at ce9c4eecf8 to 2,616 of 111,496 px on BumpMap_R16B16 and 1,616 on _B, i.e. 97.7% and 98.6%, with the residual sitting exactly on #38's checkerboard-boundary rows, which the oracle does not model. An oracle that could not reproduce our own renderer would not be allowed to arbitrate hardware's.\n\nMECHANISM 1: the two 16-bit fields must be read whole. SZ_R16B16's view swizzle is {G,R,R,G} in both vk/constants.h and gl/constants.h, so col.r is the entire R16 field (bytes 2-3, the texel's HIGH half, therefore hi) and col.g the entire B16 field (bytes 0-1, the low half, therefore lo). psh.c rebuilt each half from two eight-bit channels, which is correct for a four-byte texel and discards the low byte of both fields here.\n\nEXACT VALUES AT NAMED TEXELS (bump texture 256x256, the quad covers texels 1..3 in both axes, tent filter). Texel (1,1) of BumpMap_R16B16 holds R16=0x0202, B16=0x0101, so hi = 514/65535 = 0.00784314, lo = 257/65535 = 0.00392157, third = 1.0 -- and the old reading gives the SAME, because the test's converter byte-replicates. That is why all four flat corners of every quad were already exact under both readings and why this is not a decode or swizzle error. The error is the ramp. At the tent midpoint between texel (1,x) and (2,x) in y, hi must be (514+65535)/2/65535 = 0.50392157; the old reading truncated the filtered eight-bit view to 128 and returned 32896/65535 = 0.50190000, an error of 0.00202157, which the DOT_PRODUCT stage's 10x multiplier turns into 0.0202 in s, i.e. 0.65 of a checkerboard cell, 3.4 px. At fx = 0.25 across the B16 ramp lo must be 385.5/65535 = 0.00588235 against the old 257/65535 = 0.00392157; at 15x that is 0.94 of a cell, 4.9 px. Those two shifts are the whole 6,525 and 7,316 px per quad.\n\nMECHANISM 2: a TEXFILTER sign flag signs the FILTER, not the value. The four quads of each capture differ in nothing but ASIGNED and GSIGNED. Read off the goldens: they are byte-identical everywhere the input texels are flat -- INCLUDING rows 126..167 of BumpMap_R16B16, where the flagged field holds 0xffff, which read as a signed 16-bit value would be -1/32767 and turn a clamped solid band into an ordinary checkerboard -- and they differ on ~80 px per row across exactly rows 42..125, the hi field's tent ramp, and nowhere else (6,834 px, 84 rows, 0 rows outside). On _B the same thing transposed: exactly columns 42..125, the lo field's ramp, 6,561 px. So each texel is sign-extended BEFORE interpolation and the filtered result is read back as an unsigned 16-bit value; sign extension is a no-op modulo 2^16, which is precisely why a flat texel cannot see it. ASIGNED carries the high byte of the texel's high half and GSIGNED the high byte of its low half, which is what bump_map_tests.cpp:298 labels the axes 'G' and 'A' for.\n\nFALSIFIER A -- NAMED PIXELS, EXACT COLOURS, MECHANISM 1. On BumpMap_R16B16, in every one of the four quads (quad origin (146,66) + (180*xsigned, 180*ysigned)), at quad-local pixel (115,7) hi = 0.0078431, lo = 0.0107843, s = 0.765931, t = 0.206408, texel (196,52), so the pixel is RED, exactly (254,0,0,255); at (73,16) texel (132,51), RED; at (73,22) texel (132,60), GREY, exactly (33,32,32,191). The old reading gives the opposite colour at all three. On BumpMap_R16B16_B, quads with xsigned=1, at (73,4) texel (132,35) RED, at (115,19) texel (196,35) RED, at (94,22) texel (164,51) RED, all three GREY under the old reading. All 24 such named pixels (3 per quad per capture, each at least 2 px from a cell boundary in both s and t so #38's one-row floor cannot reach them) agree with the goldens today. IF ANY OF THESE 24 PIXELS COMES BACK THE OTHER COLOUR, mechanism 1 is wrong -- not the constant, the reading -- and dotmap_hilo_1_16 comes out.\n\nFALSIFIER B -- NAMED PIXELS, MECHANISM 2, WHERE THE SIGN MODEL ALONE DECIDES. BumpMap_R16B16, quad ysigned=1 xsigned=0 (ASIGNED set, so hi is the flagged field): quad-local (58,46) -> hi = 0.0074222, lo = 0.0054622, s = 0.422436, t = 0.358718, texel (108,91), RED; (25,71) -> hi = 0.0050833, s = 0.202619, t = 0.484419, texel (51,124), GREY; (157,109) -> hi = 0.0015284, s = 0.952784, t = 0.828256, texel (243,212), RED. Every one of those is the OPPOSITE colour if the field is filtered unsigned, and the opposite again under 'value is signed over 32767' at (58,46) and (157,109). BumpMap_R16B16_B, quad ysigned=0 xsigned=1: (47,111) -> lo = 0.0109934, s = 0.296277, t = 0.828591, texel (75,212), GREY, where both the unsigned reading and the signed-value reading give RED. 15 and 8 such sign-decisive pixels exist under that filter and all 23 agree with the goldens today. IF THESE COME BACK AS THE UNSIGNED READING, mechanism 2 did not reach the shader -- check that the sticky DOT_RGBMAPPING gate is not suppressing it -- and if they come back as neither, the sign model is wrong and #10 closes on mechanism 1 alone at roughly half the pixels.\n\nWHY EACH LEG CAN FAIL IN A WORLD I DO NOT CONTROL. Falsifier A's texels are fixed by the goldens' own flat corners plus the filter weights, neither of which my change touches; if the hardware quantised the filtered field anywhere between eight and sixteen bits, the ramp pixels would land elsewhere and A fails. Falsifier B is decided by which direction the interpolation crosses between 0x0202 and 0xffff, which is a property of silicon's filter unit; my change forces the downward crossing, but nothing in it forces the golden to agree, and the 'value is signed' and 'clamp negatives to zero' rivals are live alternatives that predict different colours at the named pixels above. The band below can fail on its own if the residual class I am attributing to #38 is not #38's.\n\nPIXEL BAND, secondary and explicitly not the criterion. Baseline at ce9c4eecf8 (z-sweep-010-Bump_map): BumpMap_R16B16 32,748 differing, BumpMap_R16B16_B 35,106, sum 67,854. The oracle's pixel-by-pixel delta -- pixels the old reading got wrong and the new one gets right, minus the reverse -- is -30,425 and -33,269, so predicted 2,323 and 1,837, against the 1,576 px floor that BumpMap_R16B16_L and BumpMap_R8B8 already sit at. Registered as: each of the two lands in [1,576, 3,500] and the sum falls by at least 60,000. Note the oracle also predicts 907 and 596 px that get WORSE -- pixels where the old reading agreed with the golden by coincidence -- so a small positive term is expected and is not a regression.\n\nBLAST RADIUS, MEASURED RATHER THAN ARGUED. Both mechanisms are gated on state->tex_hilo16[], set only for SZ_R16B16 and LU_IMAGE_R16B16, and the signed rebuild is gated again on the stage's mode actually applying a dot mapping. 8,192 shaders were generated through the baseline psh.c and this one -- every dot-mapping mode 0..7 crossed with all eight dot texture modes on two stages, cubemap on and off, four sign-flag masks, GL and Vulkan -- using docs/testing/psh_differ's carved build. The emitted text differs by exactly 73,728 lines, which is nine lines per shader: the two new preflight function definitions. NO EMITTED STATEMENT DIFFERS ANYWHERE. dotmap_hilo_1 keeps its eight-bit reconstruction deliberately, because what precision hardware's HILO unit sees of a FILTERED eight-bit channel is not measured and the Texture_cubemap HiLo_1 captures are scored against it. Every shader in the corpus does grow by those nine lines of unused GLSL, which is the one thing that can move a capture I have not predicted -- carried as the reason Texgen is on the must-not-move list.\n\nLEAST CERTAIN POINT, and the capture that exposes it. The dependent lookup is modelled as CLAMP rather than REPEAT, inferred from the goldens rather than read from a register: in BumpMap_R16B16's unflagged quads rows 148..167 are solid bands with no trace of the 5.25 px checker, which a wrapped s = 10.0 + U would keep, and the oracle scores clamp at 14,222 px against repeat's 44,017 on the same model. That reading is not something this diff changes -- both arms clamp -- but every named pixel above with s or t outside [0,1] depends on it, and the three _B pixels at t = 2.29, 3.35 and 5.99 (quad (0,0), (52,43), (58,43), (73,43)) are where it is load-bearing. If the address mode is really REPEAT and something else produces those solid bands, those three predictions are the ones that break and the rest still stand.\n\nNOT CALIBRATED FOR. A HILO mapping over a Y16 texture, which has one 16-bit field where the mapping wants two; #10's Y16 captures are untouched and stay where they are. And a 16-bit-field texture reaching a hemisphere mapping, which still quantises its halves to bytes -- no capture in the corpus does that.",
  "a_ref": "0499184e2d",
  "b_ref": "f3b888aa7c",
  "must_not_move": [
    "Bump_map/BumpMap_R16B16_L",
    "Bump_map/BumpMap_R8B8",
    "Bump_map/BumpMap_G8B8*",
    "Bump_map/BumpMap_A8R8G8B8*",
    "Bump_map/BumpMap_Y16*",
    "Bump_env_lum/*"
  ],
  "expect": {
    "Bump_map/BumpMap_R16B16_L": 1576,
    "Bump_map/BumpMap_R8B8": 1576
  },
  "expect_counts": {
    "better": 2,
    "worse": 0
  },
  "notes": [
    "must_not_move lists only suites certain to be in an arm for this issue, because ab_compare fails a pattern that matches no capture. The full list the change has to respect, and which should be added if the arm runs them: Texture_cubemap/*, Texture_2D_as_cubemap/* (they share dotmap_hilo_1 and dotmap_hilo_hemisphere and are #40's just-landed fix), Texture_format/TexFmt_R16B16 and _R16B16_L (0 differing px, the pinned rows in vk/constants.h), Texture_signed_component_tests/* (the other consumer of TEXFILTER's sign bits) and Texgen/* as the control for the preflight growing nine lines in every shader.",
    "expect_counts better=2 worse=0 is the whole-arm claim and stays valid whatever extra suites are run, since nothing outside the two R16B16 captures is predicted to move at all.",
    "b_ref is the last code-bearing commit, f3b888aa7c; this registration itself sits on top of it and the branch tip will be a documentation-only sha. Re-point a_ref/b_ref to the arms actually built, as ab_compare requires; nothing between those shas and the tip touches the emitter."
  ]
}

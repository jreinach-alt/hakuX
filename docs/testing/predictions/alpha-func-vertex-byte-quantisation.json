{
  "registered_utc": "2026-09-13T02:55:02Z",
  "who": "alpha57",
  "issue": "57",
  "prediction": "MECHANISM (judge this, not a pixel total): silicon quantises the vertex colour to its BYTE before the colour interpolator; we interpolate the float and quantise at the fragment, in glsl/psh.c's `int fragAlpha = int(round(fragColor.a * 255.0))`. The alpha test itself is correct and is NOT the defect: AlphaFuncNever_Enabled is bit-exact (the only capture that draws nothing), AlphaFuncAlways_Enabled is byte-identical to AlphaFuncAlways_Disabled, and the white rectangle -- drawn at diffuse alpha exactly 0x7F, the reference itself -- has zero structural error on all 20,480 px of all sixteen captures. Only interpolated alpha is wrong.\n\nTHE MEASURED QUANTITY IS THE COVERAGE MASK OF THE GREEN BAND, not a pixel count. Alpha_func's green band ramps 0.495f -> 0.505f, i.e. 126.225 -> 128.775 in byte units, a span of 2.55; quantising the endpoints to bytes gives 126 -> 129, a span of 3, an 18% slope change. AlphaFuncEqual_Enabled draws exactly the pixels whose round(255*alpha) == 127, so its coverage gives both crossings and bounds the slope with no model of the blend at all. Measured with docs/testing/alpha_func_ramp.py against the goldens and the z-sweep-002-Alpha_func captures (APK fb4dfafc6d38):\n  HARDWARE: a8 == 127 on x 148..317  => slope in (2.9942, 3.0296), left endpoint in [126.005, 126.011]. Contains 3.0000. Excludes 2.5369 by twenty times the bracket's own width, and excludes the 9-bit (126.5) and 10-bit (126.75) carriers via the intercept.\n  OURS NOW: a8 == 127 on x 120..321  => slope in (2.5222, 2.5473). Contains 2.5369, which is the slope after vsh.c colorPrecision() drops ten mantissa bits -- so the capture dates itself behaviourally as postdating 5c2b26db2f.\n  AFTER: ours must read a8 == 127 on x 148..317 exactly, bracket (2.9942, 3.0296), containing 3.0000 and excluding 2.5369. Equivalently AlphaFuncLessThan_Enabled's green coverage must run x 64..147 (84 px per row, now 56) and AlphaFuncGreaterThan_Enabled's x 318..575 (258 px per row, now 254). Run docs/testing/alpha_func_ramp.py CAPTUREDIR on the B arm; it is the falsifier and it prints the brackets.\n\nFALSIFIED IF: the green coverage moves off x 120..321 but does not land on x 148..317. That would mean the endpoints are being quantised to something other than the byte -- 126.5 or 126.75, which the intercept already excludes for hardware -- and this rule would be wrong rather than incomplete. A capture that improves without its coverage reaching the golden's has NOT confirmed the mechanism. Residual classes overlap and cannot be added or subtracted: the green band's structural pixels and its one-step pixels are not disjoint from each other's causes.\n\nNOT FALSIFIED BY the suite failing to become exact. 425,816 of the 435,032 differing pixels (97.9%) are one-step, classified one-step-sym by classify_residuals.py -- explicitly not a rounding rule -- and are the ~1 px phase offset between hardware's colour interpolator and ours, visible as hardware's red ramp starting 0.8 high and its blue ramp starting 0.8 low on the same geometry. That is #38 mechanism 2 and nothing in the vertex stage can reach it. Only 9,216 px are structural, 8,064 of them (87.5%) in the green band. Modelled offline over the green band, with the model first validated against our own capture (reproduces it to within one step on 3 of 512 px per row): 51,456 -> 2,176 differing and 8,192 -> 768 structural, green band only. Red and blue must NOT move at all -- their endpoints are 0.0 and 1.0, already exactly 0 and 255 under either rule -- and a flat count on them does not prove inertness, so diff the two arms' red/blue bands directly rather than comparing totals.\n\nTHE PATCH: vsh.c colorPrecision() becomes `floor(uintBitsToFloat(floatBitsToUint(c) & 0xFFFFFC00u) * 255.0 + 0.5) / 255.0`. Truncation FIRST and then the byte, because round(255 * 0.1f) is 26 and hardware says 25; floor(x + 0.5) rather than round() because 0.5f's tie at 127.5 must go up (#38 measured 128) while acc(7)'s 178.5 must go down, which it does only because the truncation has already pushed it below 178.5, and GLSL round() may break that tie either way. On a FLAT region this is identical to what we do now -- round(255*x)/255 through a UNORM8 attachment is round(255*x) -- so all nineteen of #38's probes still pass and vertex_colour_quantiser.py must still print 'OK: surviving rules = [mantissa13_half_up]'. The two rules differ only along a gradient, and flat regions were all #38 had.\n\nMUST NOT MOVE, and why these: colorPrecision() runs on all four colour outputs of every vertex shader, so the guard list is #38's own flat-region evidence, the set that proves the new rule did not break the old one. Point_size/* first: #38 names PointSmoothOff_16_FF as the capture that exposes a wrong precision choice before anything else, because it is the only one holding ten values from a single accumulating walk. Surface_clip/*, Null_surface/*, SetVertexData/*, Stencil_func/*, Antialiasing_tests/* are the captures #38 verified pixel-exact on flat submitted colours; if any of them moves, the byte quantisation has broken the flat rule and the patch is wrong whatever the green band does. Alpha_func/AlphaFuncNever_* must stay exact: Never_Enabled is the suite's one bit-exact capture and the proof that the alpha test plumbing is right.\n\nEXPECTED TO MOVE, named so a mover is not read as a surprise: every Gouraud gradient in the corpus. Lighting* (195 goldens), 3D_primitive, Attrib_carryover, Attrib_float, Attrib_setter, Shade_model. Direction unpredicted -- their endpoints move by at most 0.5 byte and their spans are tens of units, so the effect is under half a percent and could fall either way. They are the reason this is an A/B and not a landed patch.\n\nLEAST CERTAIN: one probe. Alpha_func's green band is the only gradient in the corpus whose endpoints sit far enough from their bytes relative to its span to discriminate; 3D_primitive's 0.25f->0.65f and 0.75f->1.0f ramps differ by under 0.4% between the two rules. The capture that would expose a wrong choice first is Alpha_func/AlphaFuncLessThan_Enabled: its green coverage is the low crossing alone, with no second boundary to average against and no clamping, and its modelled residual (148 px) is the smallest in the suite, so nothing can hide a miss there.",
  "a_ref": "9a79c9a8a9",
  "b_ref": "9a79c9a8a9",
  "must_not_move": [
    "Point_size/*",
    "Surface_clip/*",
    "Null_surface/*",
    "SetVertexData/*",
    "Stencil_func/*",
    "Antialiasing_tests/*",
    "Alpha_func/AlphaFuncNever_*"
  ],
  "expect": {
    "Alpha_func/AlphaFuncNever_Enabled": 0
  },
  "expect_counts": {}
}

{
  "registered_utc": "2026-09-26T10:31:56Z",
  "who": "lane.dpforce345",
  "issue": "345",
  "prediction": "#345 half 1: glsl/vsh-prog.c _DP3/_DPH/_DP4 force each 0 x inf and 0 x NaN product to 0 (the _MUL rule), matching silicon's (0,1,2).(inf,1,1) = 3 (PR #344, docs/testing/xbox-special-raw-2026-09-25.md). THE HUNK IS NaN-GATED: d = dot(); if isnan(d), d = sum of _MUL products. A zero factor can only change a dot's value through a 0 x inf or 0 x NaN term, and every such term makes dot() NaN, so for every dot whose plain result is not NaN the generated value is dot()'s, bit for bit. docs/lanes/dpforce345/dp_model.py reproduces all 5 silicon DP rows (3 moved from NaN, 2 unchanged). NO MUST_MOVE LEG: no golden in this disc is known to feed a DP a 0 x inf/NaN term. nxdk_vsh_tests CPU Shader Tests (vsh_score.py) is NOT an instrument for this hunk: its values come from the nv2a_vsh_cpu evaluator via LAUNCH_TRANSFORM_PROGRAM / constant writeback in pgraph.c, which never runs this GLSL (#281's prediction says the same of Exceptional Float). EVERY LEG IS MUST_NOT_MOVE, and what would move each: (1) any capture in any suite blanks or moves by its full geometry if the new helper fails to compile (it calls _MUL and isnan, both already used in this prelude); (2) Lighting_* (6 suites, a DP on every lit programmable path) and Vertex_shader_independence/rounding/swizzle move only if a DP in them is NaN today, i.e. one of their inputs is inf/NaN meeting a zero -- a move there toward the golden would be this defect showing, and is reported as an unpredicted finding, not claimed; (3) Attrib_float/* (the #281 NaN captures: MOV and MUL x NaN constant columns) moves only if its program has a DP over a NaN attribute; its NaN sign rule (_PosNaN) is untouched; (4) Fog_vsh, Fog_exceptional_value, Fog_inf_coord feed inf/NaN through vertex programs and move only if one of them reaches a DP with a zero factor. W_param is omitted: its prog_ captures run passthrough.vsh (mov only) and rcc_clamp_test.vsh (mul, rcc), no DP (docs/lanes/wparam223/NOTES.md). RE-REGISTERED 2026-09-26 on the same refs after arm 1 (Nova, 1 run/arm) FAILed only on Vertex_shader_rounding_tests/GeometrySuperscreen_*: 3 better, 3 worse, 294 same. Those nine captures are NOT guarded here, decided from runs without this hunk: in the 21 result dirs holding them, off-modal images occur only in the vshnobegin242 arms (Thor, base and fix) and in arm 1, whose BASE arm at master 2dc2b5c49a was itself off the modal image on 4 of 9 (0.5624, 0.5626, 0.9990 and 1.0000). The fix arm matched the modal image on 0.5624 and 0.9990 (docs/lanes/dpforce345/superscreen_hashes.py). The flip goes both ways, so the captures are unguarded, not must_not_regress. Every other rounding capture stays guarded (legs [!G]*, Geometry_* and GeometrySubscreen_*). A worse move on any remaining leg refutes the claim that the hunk is inert on finite dots.",
  "a_ref": "2dc2b5c49a",
  "b_ref": "74a238a614",
  "must_not_move": [
    "Lighting_Two_Sided/*",
    "Lighting_accumulation/*",
    "Lighting_control/*",
    "Lighting_normals/*",
    "Lighting_range/*",
    "Lighting_spotlight/*",
    "Vertex_shader_independence_tests/*",
    "Vertex_shader_rounding_tests/[!G]*",
    "Vertex_shader_rounding_tests/Geometry_*",
    "Vertex_shader_rounding_tests/GeometrySubscreen_*",
    "Vertex_shader_swizzle_tests/*",
    "Attrib_float/*",
    "Fog_vsh/*",
    "Fog_exceptional_value/*",
    "Fog_inf_coord/*"
  ],
  "expect": {},
  "expect_counts": {},
  "must_not_regress": [],
  "disc": {
    "suites": [
      "Attrib_float",
      "Fog_exceptional_value",
      "Fog_inf_coord",
      "Fog_vsh",
      "Lighting_Two_Sided",
      "Lighting_accumulation",
      "Lighting_control",
      "Lighting_normals",
      "Lighting_range",
      "Lighting_spotlight",
      "Vertex_shader_independence_tests",
      "Vertex_shader_rounding_tests",
      "Vertex_shader_swizzle_tests"
    ],
    "skip_tests": [],
    "only_tests": [],
    "source": "stated on the --register command line"
  }
}

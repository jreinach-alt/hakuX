{
  "registered_utc": "2026-09-19T04:12:36Z",
  "who": "lane.cloud-109",
  "issue": "109",
  "prediction": "WHAT LANDS IN GUEST VRAM FOR A SWIZZLED RENDER TARGET WHOSE SET_SURFACE_PITCH_COLOR IS SMALLER THAN width * bytes_per_pixel? Two models, both closed-form, both derived offline with no free parameters; derivation and generator in docs/investigations/undersized-surface-pitch-two-models.md. EVERY SOURCE CITATION HERE IS BY SYMBOL, NOT BY LINE -- vk/surface.c moved ~206 lines between this lane's base and master and all ten of this prediction's original by-line citations were wrong, one of them pointing at the contradiction of its own claim (3c2f6622c5, committed 67 minutes before this registration, is the same lesson). MODEL H (hardware, #109's undersized-pitch rule): pitch does not enter the swizzled address computation -- generate_swizzle_masks() interleaves the bits of x and y and takes no pitch -- so dest(x,y) = src(x,y) everywhere, and the surface occupies width*height*bpp bytes rather than pitch*height. MODEL E (this tree's CPU download path): surface->pitch is the row stride of the LINEAR INTERMEDIATE on both the write and the read (swizzle_rect's row_pitch), so for pitch < width*bpp only the first pitch bytes of each row survive and the read takes width pixels from y*pitch: dest(x,y) = src(x,y) for x < pitch/bpp, and dest(x,y) = src(x - pitch/bpp, y+1) for x >= pitch/bpp. COUNTED OVER DESTINATION PIXELS AS SOURCE COORDINATES, so content-independent: for a 128x128 A8R8G8B8 surface the two models diverge on 0 of 16,384 at pitch 512 (the exact pitch -- they agree by construction) and on 8,128 of 16,384 at pitch 256; for 64x64, 0 of 4,096 at pitch 256 and 2,016 of 4,096 at pitch 128. Diverging columns are exactly [pitch/bpp .. width-1]. SIGNATURE CHECKABLE WITHOUT A GOLDEN: under Model E, dest(x + pitch/bpp, y) == dest(x, y+1) at 8,128 of 8,128 positions for 128x128 pitch-256; under Model H it holds only where the drawn content happens to agree.\nTHIS ARM'S LEG, WHY IT IS GL vs VULKAN RATHER THAN A CODE CHANGE, AND WHICH DOWNLOAD PATH EACH ARM RUNS. There is nothing to fix yet -- #109 says state in this tree is absent and this lane did not touch hw/xbox/nv2a/ -- so the discriminator is which download path serves the surface, not which commit. THAT MAKES NAMING THE PATH THE WHOLE LEG. 'The GL CPU download path' is THREE paths and an earlier draft of this registration modelled the wrong one: the write it quoted, dst_row = linear_guest + y * surface->pitch, is android_surface_download_depth16_to_guest() -- #ifdef __ANDROID__, DEPTH16 ONLY, unreachable for the A8R8G8B8 colour target this prediction is about. surface_download_to_buffer() branches three ways for a swizzled download. PATH A, Android and android_surface_uses_rgba8_transfer() (true for LE_A8R8G8B8): MODEL E, via android_surface_rgba8_to_guest() writing dst_row = dst + y*dst_stride with dst_stride = surface->pitch, then swizzle_rect at the same pitch, into a g_malloc(surface->size) buffer that is in bounds. PATH B, otherwise with surface_scale_factor == 1: MODEL E, via glo_readpixels() setting GL_PACK_ROW_LENGTH = pitch/bpp, which is less than the read width, so GL packs overlapping rows; then the same swizzle_rect. PATH C, otherwise with surface_scale_factor != 1: assert(surface->pitch >= surface->width * surface->fmt.bytes_per_pixel) -- ABORT, on the undersized condition ITSELF, which is this prediction's entire subject. PATH C IS NOT HYPOTHETICAL: surface_download() passes downscale=true unconditionally for every surface->VRAM download and the only thing that clears it is downscale &= (surface_scale_factor != 1), and nothing in surface creation forbids pitch < width*bpp -- populate_surface_binding_entry_sized() asserts the DMA limit and pitch % bpp, never the width relation. SO BOTH REFS PIN [display] quality.surface_scale = 1 (the config_spec.yml default). WITHOUT THAT PIN THERE IS A THIRD OUTCOME -- a desktop GL arm at any other scale aborts on the Surface_pitch/Swizzle capture, expect_counts.better = 1 fails, and it fails for a reason that is neither model. An Android-device GL arm takes Path A, where the assert is unreachable because the RGBA8 branch returns first, so the pin is redundant there and harmless. VULKAN, EITHER PLATFORM: download_surface_to_buffer() takes use_compute_to_swizzle (swizzle && bpp == 4 && !depth_stencil, all true here) and memcpys width*height*bpp out of the compute swizzle with NO PITCH ANYWHERE -- Model H by construction. PREDICTED, if Model E is what our GL renderer does: Surface_pitch/Swizzle is NONZERO under OpenGL and 0 under Vulkan -- exactly one `better` in the whole arm, zero `worse`. PREDICTED, if Model H is what our GL renderer does (i.e. the pitch-as-stride round trip cancels for a reason not in the derivation): Surface_pitch/Swizzle is 0 in BOTH arms and classes `same`, and expect_counts better=1 FAILS. With the scale pinned those are the only two outcomes and either settles it. NOT PREDICTED: the GL value. The map above is per render target and the capture is a 640x480 framebuffer into which four textured quads were drawn, so no count over the surface converts into a count over the capture and none is registered.\nTHE FALSIFIER #109 ASKED FOR, RUN OFFLINE. IT RETURNS *UNMEASURED*, NOT *REFUTED* -- CORRECTED 2026-09-19 from an earlier draft that said the rule 'explains nothing about #87'. The difference is the instruction the board gets. #109 files this as a lead on #87 and says the rule earns that only if the two models diverge on #87's known-wrong quads and agree on the quads #87 already gets right. The evidence splits into two kinds that the first draft ran together. HARDWARE, AND IT HOLDS: Surface_pitch programs the SAME 128x128 surface twice, at pitch 512 and 256, and THE GOLDEN is identical across that pair -- recorded in the #87 fix's comment in pgraph_gl_check_surface_to_texture_compatibility(). Silicon ignores pitch at the one-half ratio for a power-of-two width, which CONFIRMS #109's hardware rule at the one point the disc reaches. OUR CAPTURE, AND IT DISCRIMINATES NOTHING: 'our capture is identical across that pair too' and '#87's residual was identical on BOTH 128-wide quads' are both observations about THE VULKAN CAPTURE, and Vulkan is the renderer this same prediction proves is Model H BY CONSTRUCTION. nv2a_issues.toml #87 is explicit -- at HEAD with no skew bound the VULKAN renderer reproduces the race-free digest unaided while GL gives 15,360 with a different digest every run -- and the 10,240 is the Vulkan column of gl-vs-vulkan-surf1-2026-09-13.tsv against GL's 15,360. On a Model-H path the two quads are identical BY CONSTRUCTION, which is exactly what was seen: THE ABSENCE OF THE ASYMMETRY IS ENTAILED BY THE PATH, NOT EVIDENCE AGAINST THE RULE. And the pitch-512 member could never have discriminated either, since the two models agree on it by construction. SO: #109's HARDWARE rule is confirmed at the one ratio the golden reaches; #87 stays closed on its own subject (a 100.0000% Morton fit against a 68.75% identity control and a device arm of 10,240 -> 0 on c807592d02); and #109's claim to be A LEAD ON #87 is UNSUPPORTED BECAUSE UNMEASURED rather than falsified. DO NOT CLOSE THAT LEAD ON THE STRENGTH OF THIS DOCUMENT. Model E is UNTESTED -- every clean number on this capture is Vulkan, where pitch never enters, and the only GL numbers (13,160 / 14,848 / 15,360) predate c807592d02 and are race-affected. The #87 fix is precisely what routes GL out of the fast path and into the pitch-using code, and the two renderers demonstrably DO differ on this capture, which is the opposite of reassuring for an argument resting on a Vulkan capture standing in for both. Said here rather than tuned to fit, as #109's own falsifier instructs.\nWHAT SILICON ADDS, AND WHAT IT DOES NOT. The existing golden already answers the headline question at one point: hardware drew the same image at pitch 512 and pitch 256, so it ignores pitch at the one-half ratio for a power-of-two width. That is the whole of what the current disc can reach -- Surface_pitch has exactly one capture, Swizzle. HARDWARE LEG, PENDING SILICON, keys named by intent because a prediction whose keys match no golden is refused at queue time and these tests do not exist yet: (1) EXTENT -- place a second surface at colour_offset + pitch*height, inside the region this tree treats as free; Model H says the swizzled store occupies width*height*bpp and clobbers it, a pitch-extent rule says it does not. THE SITE LIST IS A GREP, NOT A COUNT -- an earlier draft of this prediction said 'seven sites' and the PR body said 'four', and both read as exhaustive; the real answer at master on 2026-09-19 is SEVENTEEN, and any number written here drifts. Run: git grep -n 'pitch \\* .*height' -- hw/xbox/nv2a/pgraph/gl/surface.c hw/xbox/nv2a/pgraph/vk/surface.c. It returns fourteen in vk/surface.c and three in gl/surface.c across eight functions: two permissive DMA-limit asserts (populate_surface_binding_entry_sized in gl, populate_surface_binding_target_sized in vk); two surface_vram_written calls that under-report, so a shelved overlapping surface is not marked vram_newer (download_surface_record_deferred AND download_surface -- the SYNCHRONOUS path, the same defect on the route this prediction elsewhere calls Model H by construction); the g_malloc in pgraph_vk_complete_staged_downloads; and TWELVE memory_region_set_client_dirty calls whose dirty range misses the second half of what was written -- ten in vk (complete_staged_downloads, download_surface_complete_deferred, download_surface, process_pending_downloads, download_dirty_surfaces, two each) and two in gl (surface_download). The earlier 'the four dirty-range calls' was short by eight. A lane that patched seven and read the arm as confirmation would have left the bug alive with a list saying the work was done. MUST MOVE on hardware if Model H holds: the new extent capture. MUST NOT MOVE: Surface_pitch/Swizzle itself, whose golden already reflects the rule. (2) RATIOS THAT ARE NOT ONE HALF, e.g. pitch = 3/4 * width * bpp, where pitch % bpp == 0 but pitch/bpp is not a power of two. (3) pitch % bpp != 0, which populate_surface_binding_entry_sized's assert says cannot happen and which is a crash path if silicon accepts it. NOTE THAT (3) IS NOT THE ONLY CRASH PATH AND IS THE LESS URGENT ONE: surface_download_to_buffer's Path C assert fires on pitch < width*bpp ITSELF, reachable TODAY from this very arm on any scaled desktop GL run, which is why the scale is pinned above.\nSEPARATELY OBSERVED, NOT PART OF THIS ARM, RECOMMEND A TRACKER ROW. Vulkan's DEFERRED download also applies Model E: download_surface_record_deferred() sets dl->use_compute_to_swizzle = false unconditionally, so one surface gets a different layout depending on which route serves it. pgraph_vk_complete_staged_downloads() sizes its intermediate g_malloc(dl->pitch * dl->height) rather than from surface->size, so the swizzle_rect(..., dl->pitch, ...) beside it reads its last row width*bpp wide from (height-1)*pitch and runs width*bpp - pitch bytes past the allocation -- 256 bytes, 64 pixels, for 128x128 pitch-256. Bounded and read-only. NOT ESTABLISHED: that this route is reached for a swizzled undersized-pitch surface on the disc; there is no guard excluding swizzled surfaces from download_surface_record_deferred, but 'not excluded' is not 'reached'. THE GL PATH HAS NO SUCH BUG and I nearly reported that it did: entry->size = height * MAX(surface->pitch, width * bpp) in populate_surface_binding_entry_sized() already oversizes the allocation, which is also why GL's divergence is 8,128 and not 8,192 -- its last row survives intact in both halves.\nCONTROLS, AND THEIR PROVENANCE. The must_not_move list is drawn from the two dated cross-renderer records on file, not guessed. #87's comment of 2026-09-18 enumerates THE EIGHTEEN captures where GL and Vulkan differ at 0.4.0-j1-368-g8c938792; no capture in this eight-suite disc is on that list except Surface_pitch/Swizzle itself. docs/testing/gl-vs-vulkan-surf1-2026-09-13.tsv gives per-capture GL and Vulkan counts for all 236 captures of iso_surf1, and every control below is equal between the renderers there, with nonzero ones preferred because a control at 0 and 0 shows only that both arms ran. Six Surface_clip/rt_* captures differed on 2026-09-13 and are EXCLUDED even though #87's investigation records them as repaired to 0, because using a stale value as a control is how a baseline gets believed twice. Image_blit, Blend_surface, Color_zeta_overlap and Texture_render_target are off the disc entirely: the first three carry known cross-renderer differences (the BLEND_AND divisor, and Swap tied at 165,447 with different bytes), and Texture_render_target's 40 TexFmt_* captures render flat black unless RenderTextureLoop is skipped. ONE GUARD IS LEFT DELIBERATELY STRICT: must_not_move fails if a score holds while the bytes move, which across two renderers can happen innocently; no score-equal/byte-different pair is documented for any suite on this disc, so IF IT FIRES ON A CONTROL THAT IS A FINDING AND NOT AN EXCUSE -- two renderers drawing different pixels at the same distance from a surface golden would deserve its own row. FOUR OF THE FOURTEEN CONTROLS ARE (gl 0, vk 0), IN TENSION WITH THE RULE JUST STATED, AND ALL FOUR ARE FORCED RATHER THAN CARELESS: Null_surface and Color_Zeta_Disable have EXACTLY ONE capture each on this disc (XemuBug893, MaskOff_ZB) and both are 0/0, and Surface_clip has 47 captures with NOT ONE nonzero cross-renderer-equal row among them -- its only nonzero rows are the six rt_* that DIFFER and are excluded above. So a Surface_clip control is either 0/0 or stale, and 0/0 is the right side of that to fall on. AND ONE EXPECTATION IS REGISTERED RATHER THAN LEFT TO BE DISCOVERED ON THE DAY: must_not_move is BIT-IDENTICAL (ab_compare.py's byte leg fires on pixels_moved_attributable), ten of the fourteen controls are nonzero-but-score-equal cross-renderer in the 2026-09-13 tsv (4,419 .. 32,667), and SCORE EQUALITY THERE IS NOT EVIDENCE OF BYTE EQUALITY. So the likely first result of this arm is a control violation that MASKS the discrimination rather than a clean verdict. If that happens the correct response is to re-scope those ten to must_not_regress and RE-REGISTER -- not to explain the violation away, and not to read the strictness clause above as licence to do so.\nHOW TO RUN IT, AND WHY IT IS BOUND RATHER THAN QUEUED. This arm needs NO SILICON: one commit, two arms differing only in the renderer. It is nonetheless not dispatchable today -- request.sh has no renderer selector (its options are --suites --tests --skip-tests --only-tests --ref --arm --runs --device --title --seconds --pull --audio-capture --base-iso --perflog --env --frames-every --expect --no-expect --wait), the renderer is an xemu.toml key, and no HAKUX_* variable reads it. The arms job will skip this registration with \"a_ref does not resolve\", which is the correct outcome for a prediction nothing can run yet and is deliberate. CHEAPEST UNBLOCK, ONE LINE: a renderer override the dispatcher can set -- an xemu.toml write in request.sh, or a HAKUX_RENDERER read where the toml is parsed. WHATEVER SETS THE RENDERER MUST SET quality.surface_scale = 1 IN THE SAME PLACE, or a desktop GL arm takes Path C and aborts on the assert instead of measuring either model. The desktop route runs both renderers but the desktop build is unmeetable on this host (AGENTS.md, verified 2026-09-14: no libcurl4-openssl-dev). Run it at --runs 3 in both arms: this capture has a documented GL band of 13,160-15,360 and a single-run arm cannot separate the change from the device.",
  "a_ref": "4129a349e6 with xemu.toml [display] renderer = OpenGL, [display.quality] surface_scale = 1",
  "b_ref": "4129a349e6 with xemu.toml [display] renderer = Vulkan, [display.quality] surface_scale = 1",
  "must_not_move": [
    "Surface_format/Fmt_A8R8G8B8",
    "Surface_format/Fmt_R5G6B5",
    "Surface_format/Fmt_X8R8G8B8_Z8R8G8B8",
    "Surface_format/Fmt_X1R5G5B5_Z1R5G5B5",
    "Texture_Framebuffer_Blit/FBToTexture",
    "Texture_Framebuffer_Blit/FBToZetaAsTex",
    "Texture_Framebuffer_Blit/FBToOldRenderTarget",
    "Texture_perspective/tex_tex_pers_y_quad",
    "Texture_perspective/tex_tex_pers_n_bitri",
    "Texture_perspective_enable/TexPerspective_Untextured",
    "Surface_clip/x0y0_w512h384_A8R8G8B8",
    "Surface_clip/x8y16_w632h464_A8R8G8B8",
    "Null_surface/XemuBug893",
    "Color_Zeta_Disable/MaskOff_ZB"
  ],
  "expect": {
    "Surface_pitch/Swizzle": 0
  },
  "expect_counts": {
    "better": 1,
    "worse": 0
  },
  "must_not_regress": [],
  "disc": {
    "suites": [
      "Color Zeta Disable",
      "Null surface",
      "Surface clip",
      "Surface format",
      "Surface pitch",
      "Texture Framebuffer Blit",
      "Texture perspective",
      "Texture perspective enable"
    ],
    "skip_tests": [],
    "only_tests": [],
    "source": "stated on the --register command line"
  }
}

{
  "registered_utc": "2026-09-13T05:55:41Z",
  "who": "line-bresenham-agent",
  "issue": "13",
  "prediction": "#13, line rasterisation mode. Arm A is 0499184e2d, the branch tip, unchanged.\nArm B is 8f84f5a8ab, which enables VK_EXT_line_rasterization + the bresenhamLines\nFEATURE at device creation and chains VkPipelineRasterizationLineStateCreateInfoEXT\nwith lineRasterizationMode = VK_LINE_RASTERIZATION_MODE_BRESENHAM_EXT onto the\nrasterisation state of line-drawing pipelines only. Nothing else. For every\nnon-line pipeline the VkGraphicsPipelineCreateInfo is byte-identical to arm A's.\n\nWHY THIS AND NOT ANOTHER CONSTANT. #13's previous arm applied +0.5 to VkViewport.x\non line pipelines and DELIVERED IT EXACTLY -- fitted centre into the golden's\ninterval 18/18 on the quad strip's vertical edge, 7/7 on the fan's half-integer\none, y untouched 16/16, all 112 fill-mode captures at zero delta -- and scored\n+394,027 px. It refuted a model rather than failing to land. What survives it is:\n+0.5 in x holds on a steep edge, nothing holds in y on a shallow one, and no\nglobal translate is both. What IS both is a rule biasing the MINOR axis by half a\npixel with the sign depending on which axis is major. A viewport cannot express\nthat, because a viewport does not know which axis is major. Bresenham can, and\nthe device reports bresenhamLines = 1.\n\nWHAT MY CHANGE GUARANTEES, AND WHAT IT DOES NOT. It guarantees that a mode is\nREQUESTED on the right pipelines. It guarantees nothing about where the driver\nthen puts a line's centre or how tall a diagonal's column is -- Turnip decides\nthat, and the goldens decide whether Turnip's answer is silicon's. Every leg\nbelow is therefore a leg my change can lose.\n\nTHE FALSIFIER IS THE FITTED CENTRE, ON BOTH AXES, NOT A PIXEL TOTAL. Run\ndocs/testing/line_footprint.py --captures <arm> --edge on BOTH arms, and\n--footprint on both. A pixel total is what would have left the last arm ambiguous\nbetween inert, mis-scoped and wrong-headed; the centre fit is what said\n\"delivered exactly, and wrong anyway\".\n\nLEG 1 -- THE TWO STEEP (y-major) EDGES MUST MOVE ONTO THE GOLDEN.\n  (a) The quad strip's left edge, vertical at screen x = 160.0, row 230, widths\n      3.0-48.0. Arm A must fit ours to (159.500, 160.000] (the vertex), 18/18.\n      Arm B must fit ours to (160.000, 160.500], 18/18 -- the golden's own\n      interval. Per width the lit column run moves one right at the seven ODD\n      widths (3,5,7,9,11,13,15) and holds at the eleven even ones.\n  (b) The fan's hub-to-v4 edge, vertical at screen x = 318.5, widths 3-9. Arm A\n      must fit ours to (318.000, 318.500], 7/7. Arm B must fit ours to\n      (318.500, 319.000], 7/7, with the run's left boundary moving at the three\n      EVEN widths (4,6,8) and holding at the odd ones -- the parity FLIPPED\n      relative to (a), because 318.5 is already a pixel centre.\n  FAILS IF: Turnip's Bresenham biases the minor axis the other way (-0.5), or\n  applies Bresenham only at width 1 and falls back to rectangular for wide\n  lines, or the feature never reached the device. Check the new device log line\n  first -- \"line raster: ... -> bresenham ENABLED\" -- because \"off\" there makes\n  every leg below vacuous rather than informative.\n\nLEG 2 -- THE SHALLOW (x-major) EDGE MUST NOT MOVE IN y. The polygon's closing\nedge, near horizontal through column 399 at y = 279.674, sixteen widths. Arm A:\nours and golden light exactly the same rows, 16/16, fitted centre\n(279.500, 280.000] for both. Arm B must be IDENTICAL to arm A here.\n  THIS IS THE LEG THAT WAS DECORATION LAST TIME AND IS NOT NOW. A viewport x\n  bias could not move rows by construction, so it passed for free. Bresenham\n  changes exactly this axis for exactly this edge -- y is the MINOR axis of an\n  x-major line -- and we are already right there. So this is the sharpest\n  measurement in the arm.\n  FAILS IF: Bresenham's y convention on an x-major line disagrees with silicon's.\n  If leg 1 lands and leg 2 breaks, the minor-axis story is half right at best:\n  silicon biases the minor axis on a y-major line and not on an x-major one,\n  which is not what a single rasterisation mode gives, and the next move is\n  generated line geometry rather than another mode.\n\nLEG 3 -- THE RECORDED DISAGREEMENT, AND WHICH WAY IT RESOLVES. Two measurements\nin docs/investigations/line-width-residual.md disagree about Bresenham and are\nrecorded as open. This arm settles one of them. On the isolated QUADS edge from\nlocal (58.5, 425.4) to (12.75, 407.5) -- x-major, cos(theta) = 0.9313 -- the\nGOLDEN's column height runs 10, 14, 19, 29 for widths 8, 12, 16, 24. Arm A runs\n9, 13, 17, 26; a perpendicular rectangle predicts 8.6, 12.9, 17.2, 25.8;\nBRESENHAM PREDICTS EXACTLY 8, 12, 16, 24.\n  PREDICTED: arm B goes to exactly 8, 12, 16, 24, which is FURTHER FROM THE\n  GOLDEN than arm A. I am registering a prediction that one leg of my own change\n  makes a measurement worse, because that is what the model says and hiding it\n  would make the arm unfalsifiable. If it lands, silicon is WIDER than any\n  line-rasterisation mode on a diagonal, no mode reproduces it, and the QUADS\n  half of the disagreement wins.\n  FAILS IF: the heights stay at 9, 13, 17, 26 (Bresenham not applied at these\n  widths) or move towards 10, 14, 19, 29 (the model is wrong about which\n  direction Bresenham moves a diagonal). Caveat already on the record: the quad\n  is 60x85, so at these widths its four edges overlap and the probe may not be\n  of one edge. Line_0016.0's LineLoop cluster is the clean version.\n\nLEG 4 -- WIDTH 1, THE OTHER HALF OF THE DISAGREEMENT. 3D primitive's lines are\nall at the default width 1.0, where arm A's ink matches the golden at IoU 0.958\n(shifting it a column drops that to 0.605). Bresenham changes width-1\nrasterisation too -- it is the diamond-exit rule, not a rectangle. PREDICTED: the\n48 line-primitive captures in 3D primitive do not get worse in aggregate.\n  FAILS IF: they regress. That would mean the \"width 1 says we are already right\"\n  measurement wins, Bresenham is wrong at width 1, and the mode cannot be applied\n  unconditionally even if leg 1 lands.\n\nLEG 5 -- THE CLEAN PROBE. Line_width/Line_0016.0, the LineLoop cluster: moderate\nwidth, no polygon-mode question, no rewrite-order choice, an order we already\nmatch, and 2,259 structural channels at arm A that nothing so far explains. No\ndirection registered -- I do not have a model that predicts it. Recorded so the\nnumber is read rather than reconstructed later.\n\nWHAT MUST NOT MOVE, each a real test rather than a decoration:\n  Line_width/Line_0000.0 -- the suite's only pixel-exact capture, 0 differing.\n    Width 0 covers no pixel centre so the line draws are dropped, and the test's\n    sixteen POINTS are the whole draw -- issued while the fill mode is still\n    LINE. This is precisely the capture that says whether the gate understands\n    that a POINT topology does not rasterise lines whatever the polygon mode\n    says. #13's first viewport draft failed exactly here and never reached a\n    device; this arm's gate requires a TRIANGLE topology before polygonMode LINE\n    counts. If this moves, the gate is wrong and nothing else in the arm is\n    believable.\n  Line_width/Fill_0000.0 -- the ONLY genuinely line-free Fill capture, coverage\n    exact.\n  Line_width/Fill_0001.0 and Fill_0032.0 are DELIBERATELY NOT ON THIS LIST, and\n    were wrongly on the last one. \"Fill\" in this suite only means\n    NV097_SET_FRONT_POLYGON_MODE is FILL; LineWidthTests::Draw() issues\n    PRIMITIVE_LINE_LOOP unconditionally, so those captures contain real lines and\n    are EXPECTED to move.\n  3D_primitive, all 112 fill-mode captures -- the tripwire for the mode leaking\n    to triangle pipelines, and the reason this is a pipeline-creation change with\n    a gate rather than a global one. Honest note: for these the create info is\n    byte-identical to arm A's, so at the API level this is close to tautological.\n    What it is NOT tautological about is the DEVICE-LEVEL change -- enabling the\n    extension and the bresenhamLines feature on the VkDevice -- which is the only\n    mechanism by which this arm could reach non-line work at all.\n  Point_size and Point_params, 46 captures -- the second half of the same gate\n    question, on a suite whose whole content is point topology.\n  Texture_format, 40 captures -- the cheap corpus tripwire for that device-level\n    enable reaching work with no lines in it. Blend tests (1,673) and Depth\n    buffer (784) are deliberately NOT queued: they sit behind a 96-request\n    full-corpus sweep and the device cost is not justified by a change that is\n    byte-identical at the API level for every pipeline they build. If\n    Texture_format moves, they are the immediate follow-up and this decision was\n    wrong.\n\nTHE INERT-CHANGE DETECTOR. If every Line_width capture comes out at delta\nexactly 0, the mode never arrived -- read the device log's \"-> bresenham\" verdict\nbefore concluding anything about silicon. This change has an obvious way to\nmeasure nothing (a feature query that comes back false leaves the flag cleared\nand chains nothing, by design), and distinguishing \"correctly fell back\" from\n\"wrongly fell back\" is a log line, not a pixel count.\n\nDELTAS, NOT ABSOLUTES. Arm A is a fresh run at the branch tip, and every\ncomparison is B - A on that pair. The figures quoted above as context -- the\n1,000,125 structural channels / 342,679 structural pixels, the IoU 0.958, the\n2,259 channels on Line_0016.0, the 9/13/17/26 column heights -- come from\nz-sweep-044-Line_width at APK fb4dfafc6d38 and from the ea879bd647/c85391e29d\npair. They are CONTEXT AND TOLERANCES, not thresholds: a binary in between may\nhave moved any of them, and arm A is what they are re-measured against.\n\nWHAT THIS ARM CANNOT DECIDE. 76.3% of #13's structural residual -- 261,634 of\n342,679 structural pixels -- is edge priority: both renderers ink the pixel and\ndisagree about which of several overlapping wide edges is on top. That class sits\nON TOP of this one and cannot be subtracted from it, so the Line_width totals\nwill not fall by anything like three quarters and a modest improvement is the\ncorrect expectation. Nor does this arm speak to the sixteen sub-2px widths, which\nthe device's own lineWidthRange[0] = 1.0 and lineWidthGranularity = 0.5 decide and\nwhich still need generated geometry for silicon's dashes.",
  "a_ref": "0499184e2d",
  "b_ref": "8f84f5a8ab",
  "must_not_move": [
    "Line_width/Line_0000.0",
    "Line_width/Fill_0000.0",
    "3D_primitive/Points*",
    "3D_primitive/Triangles*",
    "3D_primitive/TriStrip*",
    "3D_primitive/TriFan*",
    "3D_primitive/Quads*",
    "3D_primitive/QuadStrip*",
    "3D_primitive/Polygon*",
    "Point_size/*",
    "Point_params/*",
    "Texture_format/*"
  ],
  "expect": {},
  "expect_counts": {}
}

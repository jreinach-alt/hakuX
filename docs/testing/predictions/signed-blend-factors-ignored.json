{
  "registered_utc": "2026-09-13T01:13:30Z",
  "who": "blend-signed-agent",
  "issue": "43",
  "prediction": "MECHANISM: FUNC_ADD_SIGNED = clamp(signed(S)+D) and FUNC_REVERSE_SUBTRACT_SIGNED = clamp(D-signed(S)) with signed(S)=S-256 for S>=128 and BOTH FACTORS IGNORED. This change implements only the factor half (force ONE/ONE); the sign fold provably cannot live in blend state, because silicon's output is discontinuous in the source at S=128 and every blend op is a continuous map of it. So the fix is exact where the source byte is below 128 and untouched above it, in the SAME captures -- a whole-capture pixel count mixes the two halves and must not be the falsifier. FALSIFIER, as a measurement of the quantity the mechanism changes: run docs/testing/signed_blend_source_halves.py on arm B's result directory. It recovers (S,D) per channel from the goldens alone (never from our render), with the suite's plain-ADD capture as a control that is bit-exact against silicon today. Of the 856,098 recovered channels whose source byte is below 128, 594,738 are wrong today in txt_A8R8G8B8_SADD and 592,409 in txt_A8R8G8B8_SREVSUB. After this change EVERY ONE of them must be exact: the predicted count is 0 and 0. If any remain wrong, 'both factors are ignored' is false and the change should come straight back out. The 365,491 channels with source byte >= 128 are predicted to stay wrong -- 365,491 wrong in both captures, which for SREVSUB is 23,688 WORSE than today's 341,803, because today's SRC_ALPHA factors land on the right answer for those by arithmetic accident. That is a real regression inside a net gain of -594,738 and -568,721 channels, and it is predicted here rather than discovered. DERIVED, ADVISORY, NOT THE FALSIFIER: the whole-capture differing-pixel counts should be txt_A8R8G8B8_SADD 244,324 -> 186,724 and txt_A8R8G8B8_SREVSUB 236,168 -> 186,464 (both reach 175,360 differing pixels outside the pb_print label rows, identically, since the unreachable half is the same geometric region in both). Those two numbers carry a sensitivity of at most 320 and 60 pixels respectively, localised to the pb_print label glyphs, whose 7,211 channels the recovery cannot solve and which this prediction holds unchanged; a miss within that is the label, not the mechanism. Neither capture becomes exact, and scoring this on exactness will read as no progress. ALSO EXPECTED TO MOVE, DELIBERATELY UNPREDICTED: the 30 Blend_tests #spot_*_SADD / #spot_*_SREVSUB captures. They reach the screen through a render target and an alpha blit, and their DrawColorAndAlphaStack region is dominated by #50, so any number I gave for them would be a guess about overlapping residuals. The offline oracle says the DrawColorStack region of the retired 448-capture set goes 14,426,608 -> 18,350,080 of 29,360,128 channels. The 75 unsigned #spot_ captures must not move at all, and neither must Blend_surface, which is the guard on the destination-alpha fold this change restructures. ONE READING RISK, PRE-REGISTERED BEFORE ANY ARM EXISTS: with both factors at ONE the blend is an integer add with no multiply, so clamp(S+D) should be bit-exact -- but blend-unit-model.md measured a host float32 store landing one step low on 2,071 of 54,000 sampled channels on lavapipe, and whether Adreno splits the same way is unmeasured. So if the S < 128 half comes back non-zero but EVERY residual channel there is off by exactly one step (the TSV's off_by_one column, or the tool's own channel values), that is the known precision floor and not a refutation of 'both factors are ignored'. A residual with any channel off by more than one step IS a refutation. Stating this now, with no results in existence, so it cannot be a widening later.",
  "a_ref": "3996a62804",
  "b_ref": "04d1262552",
  "must_not_move": [
    "Texture_signed_component_tests/A8R8G8B8_0x*",
    "Blend_tests/#spot_*_ADD",
    "Blend_tests/#spot_*_SUB",
    "Blend_tests/#spot_*_REVSUB",
    "Blend_tests/#spot_*_MIN",
    "Blend_tests/#spot_*_MAX",
    "Blend_surface/*"
  ],
  "expect": {
    "Texture_signed_component_tests/txt_A8R8G8B8_ADD": 0
  },
  "expect_counts": {}
}

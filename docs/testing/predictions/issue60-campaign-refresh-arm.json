{
 "registered_utc": "2026-09-14T00:30:00Z",
 "who": "remote-lane-agent",
 "issue": "60",
 "a_ref": "1a879e56",
 "b_ref": "a105a51a",
 "prediction": "THE ARM THE TRACKER ASKED FOR, AND I EXPECT IT TO FAIL AS SPECIFIED. nv2a_issues.toml's #60 entry on the campaign branch says: 'The arm it needs is already specified in the thread: a105a51a00 against its parent on the GL lane, predicting that no capture moves at all, which is a real falsifier of the format-table audit.' I am running it because I hold the GL renderer as a capability and no other lane can. I am registering the OPPOSITE outcome first, because I already have the measurement. WHY I EXPECT MOVEMENT. docs/investigations/issue60-blocked-by-surface-to-texture.md measured the colour-refresh arm on this lane at head d662ba2b with a runtime switch, so the off arm was byte-identical to the head: 'colour refresh only -- 4 worse, 0 better, +31,948 px', and restricting the refresh to the single X8R8G8B8_Z8 binding pair reproduced the WHOLE regression. The mechanism is not the format read: it is that refreshing the field flips thirty surface-to-texture decisions from false to true, and the GL s2t fast path was wrong for every format that exercised it (#71). #60's stale format was HIDING #71 by refusing the path for the wrong reason. THE PREREQUISITE IS NOT ON THE CAMPAIGN BRANCH. #71's fix is f75a4aad -- render_surface_to_texture() forces GL_LINEAR to make a single-level texture complete and apply_texture_parameters() then skips reapplying the guest's filter behind a cache guard, so a REUSED binding keeps the forced filter; the fix drops the cached value. git merge-base --is-ancestor says f75a4aad is NOT an ancestor of origin/claude/es-de-launcher-disc-error-ojnl14, and `min_filter = 0xFFFFFFFF` appears ZERO times in that branch's gl/surface.c. So the campaign branch carries #60 WITHOUT the prerequisite the ordering note in territory.toml wave 27 states -- '#71 before #60' -- which is precisely the configuration measured to regress. CORRECTION TO MY OWN EARLIER NUMBER, made here rather than after the result. The regression was reported as 4 captures and +31,948 px. One of the four was Surface_pitch::Swizzle 14,848 -> 15,360, which is 512 px and is INSIDE the noise band measured since: that capture's GL band is at least 2,200 wide, not the 512 first recorded, and three runs of one unchanged binary on the full disc scored 14,848 every time while differing by up to 2,341 px. So Swizzle carries no number in either direction here. The real claim is THREE captures and +31,436 px. WHAT I PREDICT, directionally rather than by exact value, because the campaign tree differs from d662ba2b by 599 commits and the baselines will not match: (1) the arm is NOT inert -- at least one capture moves; (2) the movers are Blend_surface::ARGB8_Add_SrcA_1-SrcA, Blend_surface::ARGB8_Add_SrcA_DstA and Blend_surface::DstAlpha_XA_O1A7RGB8, all WORSE; (3) no capture outside Blend_surface and Surface_pitch moves; (4) Vulkan is unaffected -- this is gl/*.c only. WHAT WOULD MAKE ME WRONG, stated so the result can refute me: if the campaign's rebased a105a51a differs materially from the version I measured, or if something in the 599 commits already closes the s2t hole by another route, the arm is inert and the tracker's prediction is right. That is a real possibility and is why this is being run rather than argued. METHOD, and it is not optional here: TWO runs of each arm, compared as BYTES with sweep_agreement.py before any arm-to-arm number is quoted. Surface_pitch::Swizzle is nondeterministic under OpenGL on this disc and a single run of it carries no number.",
 "must_not_move": ["Vulkan/*"],
 "expect": {},
 "expect_counts": {"better": 0, "worse": 3}
}

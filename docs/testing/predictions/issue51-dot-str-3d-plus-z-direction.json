{
 "registered_utc": "2026-09-14T04:35:00Z",
 "who": "remote-lane-agent",
 "issue": "51",
 "a_ref": "HEAD",
 "b_ref": "",
 "prediction": "THE COORDINATE CANDIDATE, registered before it is written, because the step from a measured rule to an expression has been wrong twice on this issue. WHAT IS MEASURED AND CARRIED IN: (1) the sign-to-corner table, 100.0% pure on all eight sign classes across all six DotSTR3D_* captures, with sign(dot_3) selecting nothing; (2) reconstructing every face from GenerateMaxContrastNoisePattern, the three POSITIVE faces offer exactly {R,B,G,W} at their corners and the three NEGATIVE faces exactly {magenta,black,cyan,yellow}, which are the four colours the goldens never show; (3) under the guest's verified SZ_A8B8G8R8 format exactly one assignment reproduces the measured R,B,W,G with no flips and it is the +Z face, (s1,s2) -> (x,y) directly; (4) the path is a CUBEMAP lookup -- dim_tex[3]=2 in all seven shader generations and tex_cubemap[3]=1 in six of seven, measured at the branch, refuting this issue's two state claims; (5) silicon produces flat regions of 13,000-29,000 px of one corner from a texture that alternates corners every one or two texels, so the coordinate is constant over large screen areas and the magnitudes are discarded. THE DERIVATION, in the GL cube convention: the +Z face is selected when rz is the major axis and rz > 0, and there sc = rx, tc = -ry, ma = rz, so s = (rx/rz + 1)/2 and t = (-ry/rz + 1)/2. Wanting x = s1 ? 63 : 0 gives rx = s1*k; wanting y = s2 ? 63 : 0 gives ry = -s2*k; and rz = 1 with |k| < 1 makes +Z strictly the major axis. So THE CANDIDATE IS vec3(s1*k, -s2*k, 1.0) with s1 = (dot_1 >= 0) ? 1 : -1 and s2 = (dot_2 >= 0) ? 1 : -1. Note this is NOT the expression the investigation proposed -- vec3(1.0, -sign(dot_2)*k, -sign(dot_1)*k) puts 1.0 in x, making +X the major axis, and uses -sign(dot_1) in z. That one came through the +X face assumption; this one comes from the +Z measurement. THE ONE DESIGN CHOICE IS k, and it is de-risked rather than guessed: k must be strictly below 1 so +Z wins the major axis outright, and any k in (1 - 2/N, 1) lands in the outermost texel of an N-wide face. The risk that a sample at the face edge blends across faces is CLOSED: GL_TEXTURE_CUBE_MAP_SEAMLESS is never enabled anywhere in the GL renderer and is disabled by default, so an edge sample clamps within its own face. LEGS, structural first because they do not depend on our dot signs matching silicon's: (1) EVERY quad pixel is an odd-parity corner, i.e. from a positive face -- a candidate emitting black, magenta, cyan or yellow is refuted whatever its pixel count does; (2) distinct quad colours become 4 on DotSTR3D_-1to1, _-1to1D3D and _-1to1GL, and 2 on _0to1, _HiLo_1 and _HiLoHemi, against 8 on all six today (count the quad only -- a whole-image count reads one higher because the white label text is itself a corner colour); (3) the sign-to-corner table reproduces at the same purity; (4) Texture_2D_as_cubemap::DotSTR3D_Bad2D stays at zero differing channels -- it was exact before and the earlier remapCubeTo2D arm broke it by 59,142. DIRECTION PREDICTED, VALUES NOT: the six captures get better, and the residual after the fix is bounded by whether our dot SIGNS agree with silicon's per pixel. The 100% purity of the table is strong evidence they do, but it is evidence and not an identity, so I predict a large drop and explicitly do NOT predict zero. NOT PREDICTED: any particular pixel count. WHAT WOULD REFUTE ME: any even-parity colour appearing; distinct quad colours not collapsing to 4/4/4/2/2/2; Bad2D moving; or the six captures getting worse. SCOPE WARNING THAT MAKES THIS UNLIKE EVERY OTHER CHANGE THIS LANE HAS LANDED: glsl/psh.c is SHARED -- pgraph_glsl_gen_psh is called from both gl/shaders.c and vk/shaders.c -- so this is NOT a GL-only change and BOTH renderers must be measured. Every previous fix from this lane was gl/*.c and Vulkan was a must_not_move control; here it is not, and registering Vulkan/* as must_not_move would be wrong. METHOD: clear all four shader caches (spv_cache, shader_module_keys.bin, shaders/, shader_cache_list) or nothing regenerates; two runs per arm per renderer, compared as BYTES.",
 "must_not_move": ["Texture_2D_as_cubemap/*"],
 "expect": {},
 "expect_counts": {}
}

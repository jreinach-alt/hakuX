{
  "registered_utc": "2026-09-13T14:19:52Z",
  "who": "desktop-lane-agent",
  "issue": "67",
  "prediction": "ROUTED to this lane by the orchestrator; target/** is lane.remote in docs/testing/territory.toml wave 10, verified in the live file rather than from the routing message. The analysis is the #67 lane's, in docs/investigations/guest-frndint-ignores-rounding-mode.md. THE THREE CLAIMS I CHECKED MYSELF BEFORE WRITING ANYTHING, from primary source. (1) target/i386/tcg/fpu_helper.c really defines both floatx80_round and floatx80_round_to_int as ((void)(s), rint(a)), so the float_status argument carrying the guest's rounding-control field is the argument being discarded, and rint() follows the HOST environment which QEMU leaves at nearest-even. (2) The scoping: the '#if defined(XBOX) && defined(__aarch64__)' at line 105 has no #endif before those macros -- traced with a preprocessor-depth scan, not by eye -- so the whole block is aarch64-only, and fpu_helper_hard.c's own gate is '(defined(__x86_64__) || defined(__aarch64__))', which means the desktop build compiles the file but not this override. (3) helper_frndint at line 2948 is the ONLY caller of floatx80_round_to_int in the file, and floatx80_round has no caller at all. The rule itself I re-checked offline against the five call sites the investigation names: floor() returns silicon's value on all five, and disagrees with rint() on exactly the three the investigation says moved -- 24.75, 85.714, 138.667 -- while agreeing on 99.2 and 136.0, which are the controls that make it a rule rather than a fitted constant. WHAT THIS LANE CAN AND CANNOT MEASURE, stated because it decides what this registration is worth. The fix is inside the aarch64 gate, so EVERY pixel leg needs an Android device arm and this lane has none. The desktop disc, the 236-capture GL/Vulkan baseline and the measured noise floor are all blind to it. FALSIFIERS. (1) THE ONE I CAN RUN: the desktop lane does not move. All 236 captures of iso_surf1 byte-identical under renderer='OPENGL', and Vulkan likewise. Genuine and not a formality -- it is the direct test of claim (2), and if the scoping reading is wrong the captures move and the change is far riskier than it looks. (2) NOT RUNNABLE HERE, registered so the device lane can run them as stated: 75 unsigned Blend_tests/#spot_* to 0 differing px each, 1,168,902 -> 0; Point_size/SmallestPointSize_FF and _VS to 0 with recovered spacing 85 rather than 84, which is the mechanism-shaped half since 84 would mean the model is wrong in the same direction as the fix; fifteen #spot_*_SADD to 96,855 each from 104,205, leaving #43's ink; and Viewport/* and Stencil/* unmoved, on the SAME disc -- an unmatched must_not_move fails ab_compare outright. (3) The Android build compiles. This is the only automated check that reaches the changed code, since CI builds aarch64 and the desktop build does not instantiate the block. NOT PREDICTED: any desktop pixel change whatsoever, in either direction. NOT A FALSIFIER: that the switch returns floor/ceil/trunc for the three directed modes -- that is the edit.",
  "a_ref": "df8adddf",
  "b_ref": "",
  "must_not_move": [
    "Vulkan/*"
  ],
  "expect": {},
  "expect_counts": {
    "worse": 0
  }
}

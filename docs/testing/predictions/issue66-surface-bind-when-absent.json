{
  "registered_utc": "2026-09-13T11:42:31Z",
  "who": "desktop-lane-agent",
  "issue": "66",
  "prediction": "MECHANISM, measured before this was written, not inferred. update_surface_part() gates binding creation on 'is the surface dirty' when the precondition it needs is 'is the surface dirty OR is there no binding at all'. One log line at the gate over a full GL run of iso_surf1 splits colour uploads into exactly three populations: buffer_dirty=0/binding=SET/SKIP 136,157 (correct -- nothing stale, binding present); buffer_dirty=1/binding=null/PASS 1,281 (correct -- stale, so it creates one); and buffer_dirty=0/binding=null/SKIP 378 (THE DEFECT -- no binding and nothing dirty, so none is ever made). That 378 is the same 378 clears that raise GL_INVALID_FRAMEBUFFER_OPERATION, and the chain is: guest issues NV097_CLEAR_SURFACE with colour bits -> update_surface_part skips -> color_binding stays null -> framebuffer is INCOMPLETE_MISSING_ATTACHMENT (0x8CD7, NOT 0x8CDD UNSUPPORTED, so this is us building it wrong and not the driver declining) -> glClear raises 0x0506 and DOES NOTHING -> the error sits pending until gl/shaders.c:413 asserts on it and aborts the process. Also established: mem_dirty = !tcg_enabled() && ... is identically 0 on all 137,816 samples because tcg_enabled() is 1 on every build we run, so the gate is surface->buffer_dirty alone and the memory-dirty half has never been exercised. THE CHANGE: let the gate also pass when the corresponding binding is absent. FALSIFIERS, as measurements. (1) GL_INVALID_FRAMEBUFFER_OPERATION raised by glClear over a full iso_surf1 GL run goes 378 -> 0, counted with a glDebugMessageCallback rather than glGetError (glGetError returns and CLEARS one error per call, so it counts pendings not raises -- that mistake is why an earlier report on this issue said 'one per run'). This is a genuine falsifier and not an assertion about my own output: the change creates a binding, but whether that binding actually COMPLETES the framebuffer is not guaranteed by it, and a binding that attaches nothing would leave the count non-zero. (2) CAPTURE COUNT 44 -> 236 on iso_surf1 under renderer='OPENGL'. The abort currently caps the disc at 44 of 236, so if dropping the clears is what leaves the stale error, removing them removes the abort. Falsifiable in both directions: another error source, or a new one introduced by binding surfaces that were previously absent, leaves it short. (3) The 44 captures that DO exist today must not regress -- byte-identical between arms, diffed rather than counted. (4) The Vulkan lane must be byte-identical: this changes gl/surface.c only, so any Vulkan capture that moves means the change reached shared state. NOT A FALSIFIER, and registered as such so it is not totted up: 'the 378 buffer_dirty=0/binding=null/SKIP samples go to zero' is forced by the patch -- the gate is changed to pass in exactly that case -- so it cannot fail and proves only that the fix does what I think it does. A falsifier your own change guarantees is not a falsifier. NOT PREDICTED: any pixel delta. Nobody has ever scored this disc to completion, so its first full run establishes WHICH CAPTURES EXIST, not a delta against a baseline that does not exist. expect_counts better=0 worse=0 refers to the 44 comparable captures only. NOT ESTABLISHED: whether the dropped clears cost pixels in the 44 captures that do exist -- 378 clears that did not happen is suggestive and has never been tied to one; and whether a second GL implementation agrees, though glClear on an incomplete framebuffer is a spec error anywhere.",
  "a_ref": "6e65d674",
  "b_ref": "",
  "must_not_move": [
    "Vulkan/*"
  ],
  "expect": {},
  "expect_counts": {
    "better": 0,
    "worse": 0
  }
}

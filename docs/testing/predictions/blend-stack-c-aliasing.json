{
  "registered_utc": "2026-09-13T02:04:47Z",
  "who": "blend-stack-c-agent",
  "issue": "50",
  "prediction": "MECHANISM (judge this, not a pixel total): Blend tests' TestDetailed stack C is NOT drawn in reverse vertical order and its diffuse colours are NOT swapped. Its blit is served the image left behind by the PREVIOUS render-to-texture block. All three blocks point the colour surface at texture memory for stage 1 (RenderToTextureStart(1, pitch)), and blocks 2 (DrawColorStack, stack A) and 3 (DrawColorAndAlphaStack, stack C) use the same address, width, height, pitch, surface format and texture state, differing only in the draws between them. Block 3's texture bind reaches download_surface_to_buffer() via pgraph_vk_surface_download_if_dirty(); that function did not flush the reorder window or the draw queue, so the copy is taken before block 3's four draws are recorded and returns block 2's pixels. It reads as a reversal only because DrawColorStack and DrawColorAndAlphaStack draw the same four colours in opposite order.\n\nTHE MEASURED QUANTITY IS WHICH RENDER TARGET STACK C SHOWS, not the swatch rows and not a pixel count. The swatch rows are the same in ours and the golden already -- band boundaries at screen rows 176/240/304 and centres at 144/208/272/336 in both, both checkerboards in phase, 0 of 1568 captures matching a vertical flip, a 64-row block reversal, a column mirror or a 180-degree rotation -- so a fix that moves a row has broken something that was right. Measured with docs/testing/blend_stack_c_aliasing.py on the 1,568-capture oracle in ~/hakux-work/res_oldblend:\n  BEFORE, model-based (--model --unsigned): our stack C == model of stack A blitted at stack C's position on 1,119/1,120 unsigned captures (73,346,400/73,400,320 channels); == model of stack C on 1/1,120 (20,107,648/73,400,320). Our stack A == model A 1,120/1,120 and our stack B == model B 1,120/1,120, both bit-exact.\n  BEFORE, model-free (--aliasing): our stack C == our stack A on the 1,024 px/capture where the screen checker phases agree, bit-exact on 1,567/1,568 including the 448 signed captures; goldens 0/1,568 on the same test.\n  AFTER: our stack C == model of stack C must be 1,120/1,120 on the unsigned set and our stack C == model of stack A blitted at C must be 0/1,120; model-free ours stack C == stack A must fall to at most the golden's own 0/1,568. Whole-capture: all 1,120 unsigned oracle captures must become BIT-EXACT, 14,628,880 differing pixels to 0, because the entire whole-capture residual on that set is stack C -- verified per capture, with 0 differing pixels outside the three blits. Current per-capture residuals are 16,384 on 529 captures, 8,192 on 248, 11,568 on 155, 12,512 on 115, 8,304 on 40 and a tail.\n  FALSIFIED IF: stack C stops equalling stack A's render target but does not reach model C. That would mean the copy is now taken at the right time and something else in that region is also wrong, and this fix would be only part of #50. Residual classes overlap and cannot be subtracted: a capture that improves without reaching bit-exactness has NOT confirmed the mechanism.\n  NOT falsified by the 448 signed captures. Stack A is wrong on all of them for #43's reasons, so stack C inherits that; they can only become exact once #43 lands too. Score the unsigned 1,120.\n\nTHE ONE THING NOT EXPLAINED: 0_ADD_1 is already bit-exact in stack C and is the single exception to every count above. It is both the only capture whose stack-C draws are a mathematical no-op (sfactor=ZERO, dfactor=ONE writes the destination back on all four channels) and the first TestDetailed capture of the run. A missing queue flush should be missing there too. Either the cold-start path differs or the ordering race resolved the other way once in 1,568 -- which is what a fence-and-queue defect looks like. That is why every figure here is a count over captures and no claim rests on a single one.\n\nMUST NOT MOVE, and why these: the change makes every synchronous surface download record queued draws first, so it can only ever make a download see MORE work, never less -- it cannot lose pixels, but it can change timing and it costs a flush on that path. The surface- and texture-path suites listed are the ones that read a rendered surface back or sample it: Blend_surface, Surface_format, Surface_pitch, Surface_clip, Color_mask_blend, Color_zeta_overlap, Color_Zeta_Disable, Null_surface, Image_blit, Texture_render_target, Texture_Framebuffer_Blit, Texture_format, Texture_perspective, Texture_perspective_enable, Texture_CPU_Update. Depth_buffer_fixed_function is on the list specifically because the comment this fix copies was written about its readbacks; if it moves, the two download paths disagree about something beyond ordering. Blend_tests/#spot_* must not move at all: TestDetailed is interactive-only and absent from the release disc, and the #spot_ captures use ONE 512x512 render target with ONE blit, so they have no second block to alias and this change cannot reach them. Stack A (cols 16-79) and stack B (cols 192-447) of the oracle must stay bit-exact at 1,120/1,120 -- they are #43's clean oracle and losing them would cost more than #50 is worth.",
  "a_ref": "da9ca9537e",
  "b_ref": "9c5e892032",
  "must_not_move": [
    "Blend_surface/*",
    "Blend_tests/#spot_*",
    "Texture_render_target/*",
    "Texture_Framebuffer_Blit/*",
    "Surface_format/*",
    "Surface_pitch/*",
    "Surface_clip/*",
    "Color_mask_blend/*",
    "Color_zeta_overlap/*",
    "Color_Zeta_Disable/*",
    "Null_surface/*",
    "Image_blit/*",
    "Depth_buffer_fixed_function/*",
    "Texture_format/*",
    "Texture_perspective/*",
    "Texture_perspective_enable/*",
    "Texture_CPU_Update/*"
  ],
  "expect": {},
  "expect_counts": {}
}

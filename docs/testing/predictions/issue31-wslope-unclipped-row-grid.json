{
  "registered_utc": "2026-09-13T15:06:49Z",
  "who": "wslope-anchor-agent",
  "issue": "31",
  "prediction": "#31, the first arm this issue has ever had. It predates ab_compare, so bdc26fa5c8 landed with no registered prediction and there is no judged verdict on any part of it. The change under test is ONE line of emitted GLSL: the slope-offset anchor's ROW snaps to 4*floor(r/4)+2 when the Sutherland-Hodgman window clip did not cut the triangle, and keeps the 2x2-quad snap when it did.\n\nTHE BLOCKER THIS REPLACES. The tracker says 619,349 structural px remain \"under the 4-pixel anchoring regime the available goldens cannot select between\". That sentence is false about the anchors and I measured it rather than believing it. docs/testing/wbuf_anchor_recover.py (committed with the fix) recovers hardware's offset as an EXACT INTERVAL, not an estimate: the offset is one constant per triangle and the stored depth is floor(w + offset), so over a triangle's pixels offset lies in [max(z-w), min(z+1-w)], typically 0.002 wide on a value of 1e5, while one pixel of anchor is worth 0.5%-5% of the offset. That pins all 66 anchors in the suite to a few thousandths of a pixel. The previous reading used the FRACTION of pixels sitting on the higher of two adjacent integers, which is biased by up to 0.1 of a unit because frac(w) is not uniform over a triangle.\n\nORACLE VALIDATION, two controls, both printed by the tool. (1) floor(w) from the recovered plane equals hardware's own ZS0 depth on 100.00% of the pixels of FloorQuad (221,970), RoofQuad (234,955), WallQuad (233,760) and TriH (26,400) and on 99.45% of TriV. An instrument that could not reproduce the UNBIASED capture has no business arbitrating the biased one. (2) The same file emulates wbufSlopeStep in float32 and reproduces the DEVICE's arm-A numbers to the pixel on the six WBuf24D rows whose error is structural: ClipF-150-032 281/16,520/189,489, ClipF-150-128 0/0/159,250, ClipF-150-224 0/0/112,210, ClipW 227,737/1,703/0 and 167,539/12,941/0 and 124,410/7,110/0, TriH wrong 13,200, TriV wrong 26,400.\n\nWHAT THE 64 DISCRIMINATING ANCHORS SAY (LargeZ's two are excluded and must be: w is 1.7e7 changing by 1 across the surface, so its offset is 136 at EVERY anchor, and counting it as agreement would be as wrong as counting it as disagreement). All 24 TriH triangles anchor their row on an absolute 4-grid at phase 2, inside hardware's interval for every one of them. All eleven triangles the clip CUT anchor exactly where the shipped 2-snap puts them. Three ClipF t1 anchors want clip_top+2. All 24 TriV anchors fit NEITHER.\n\nLEG M1 -- THE MECHANISM LEG, AND THE ONE THAT DECIDES THIS. For TriH the quantity is the per-triangle offset bounded the same way from OUR capture: off in [max(z-w), min(z+1-w)] over the triangle's pixels, computed by the committed tool on arm B's WBuf24D_TriH_V1_ZB0_ZS1_ZB. Arm A's recovered offsets are 480,392.5 for triangles k=0 mod 4 and 503,086.25 for k=1 mod 4 -- the ladder rungs for anchor rows 0 and 0, i.e. our 2-snap landing two and one rows above hardware's. Hardware's intervals are [439,380.4325, 439,380.4454] and [459,200.4059, 459,200.4077]. PREDICTED: arm B's recovered offsets for k=0 and k=1 mod 4 land in 439,380.44 +/- 0.04 and 459,200.44 +/- 0.04 respectively, i.e. one float32 ULP (0.03125 at this magnitude) of hardware's own interval, and the k=2 and k=3 mod 4 offsets are UNCHANGED at 480,392.5 and 503,086.25. That is four values, two moving to named targets and two required to stand still. THE WORLD IN WHICH IT FAILS: if the 4-grid phase is 0 rather than 2, arm B lands on 480,392.5 and 459,200.5 -- a whole rung out. If the grid is relative to the triangle rather than absolute, all four residues move together instead of two of four. My patch fixes the anchor row; it does not fix which ladder rung hardware chose, and the rung comes from the goldens.\n\nLEG M2 -- INTERMEDIATE VALUE, THE HALF THAT CAN FAIL. TriV must NOT be fixed, and its exact current value is registered: all four WBuf24{D,F}_TriV_V1_ZB{0,1}_ZS1_ZB stay at exactly 26,400 differing, not lower. Reason, measured: TriV's dominant axis is x and its pb is exactly 0 (v0 and v2 share x and share w=325.0, so the plane's y derivative is 0 in any arithmetic), so a change to the anchor ROW cannot reach it -- the four captures should be BYTE-IDENTICAL between arms. A change that \"merely helped\" the small-triangle class would drag TriV down too, and a bug in the gate would move it. This is also why the column is deliberately left on the 2-grid: the 4-grid column recorded for TriV in docs/investigations/wbuffer-slope-offset.md is REFUTED, not unselected. Column 164 predicts 403,404.8 / 420,816.6 / 439,380.5 / 459,200.5 against hardware's intervals [403,283.1552, 403,283.1566] / [421,475.6559, 421,475.6689] / [440,927.2732, 440,927.2878] / [461,755.6224, 461,755.6255] -- errors of -122, +658, +1,547 and +2,555 units where this model is inside hardware's interval on the other 37 anchors, and no other integer column is closer. So 105,600 of the 619,349 residual px are a THIRD mechanism and a 4-grid column would replace a wrong answer with a wrong answer while moving no pixel.\n\nLEG M3 -- INTERMEDIATE VALUE, THE LARGEST CLASS STAYS PUT. ClipF is 460,949 of the 619,349 structural px and is deliberately untouched: 206,009 / 159,250 / 112,210 exactly. Its triangles ARE cut by the clip, so the gate must read false for them; if the gate misfires they move by six figures. This is the leg that tests the gate rather than the grid.\n\nLEG M4 -- THE BLAST-RADIUS CONTROL. Every ZBuf* capture must be byte-identical, because z_perspective is false there and the emitted GLSL is provably unchanged. 288 shaders were emitted through the shipped psh.c and this one over z_perspective x depth_needed x window_clip_count x window_clip_exclusive x stipple x zeta format x three renderers; 216 are BYTE-IDENTICAL -- every shader with z_perspective false or depth_needed false, which is everything in the corpus outside W buffering and Depth Clamp -- and the 72 that change do so by exactly one of three texts differing only in line numbering: the bool cut declaration, the two assignments to it, and the row snap. No other emitted statement differs anywhere. Depth Clamp is on the disc as the live half of that control: it is W-buffered and it never writes POLYGON_OFFSET_SCALE_FACTOR, so if the sticky register reaches it the extra state shows up there and nowhere else.\n\nLEG M5 -- FLAGGED AS TAUTOLOGICAL, DO NOT COUNT IT. \"TriH's four ZS1 depth captures get better.\" Any change that moves the TriH anchor toward hardware's forces that. It is worth asserting that the fix does what I think, and it is not a falsifier; M1's named offsets are.\n\nTHE PIXEL BAND FOR TriH IS A BAND AND IS REGISTERED AS PROSE, NOT AS A VALUE. Arm A: 13,200 differing, 0 off-by-one, on each of WBuf24D/24F TriH V1 ZB0/ZB1 ZS1_ZB. Predicted arm B: the structural half goes to 0 -- my float32 emulation reproduces arm A's structural count exactly and gives 0 here -- but the off-by-one remainder is NOT predictable to a value, because it depends on which way the driver rounds the last ULP of the offset. Sweeping our offset over +/-1 ULP gives 318 to 1,338 differing px, with 546 at my emulation's value and 96 if the driver lands mid-interval. So: EACH of those four captures lands in [0, 1,400] and the suite's structural total falls by exactly 52,800. Registering 0 as an exact expect value would be registering a tolerance as a value, which has already turned one passing measurement into a violated check in this campaign. Note also that arm A's exact/pm1 split for TriH is the one row my emulator gets wrong by 84 px, in exactly this way, which is why the band is this wide and not narrower.\n\nWHAT IS DELIBERATELY LEFT, AND THE REFUTING MEASUREMENT. 566,549 of the 619,349 px stay. ClipF t1's clip_top+2 is unselectable from this corpus and that is now a measurement rather than an assertion: ClipF-150-032 t0 and t1 share the 1/w plane (one planar quad), the window clip, the topmost vertex (both are cut on the v0-v2 diagonal so both have v0) and the first covered row 32 -- hardware's own coverage is byte-identical to the geometric prediction on all 206,290 px, checked row by row -- and they anchor at rows 32 and 34. So no function of (plane, clip rect, first covered pixel, top vertex) can reproduce the table; if one existed those two would be equal. And FloorQuad t1 versus ClipF-150-032 t1 is the SAME TRIANGLE, identical vertices and plane and orientation, differing only in clip_top 0 against 32, anchoring at clip_top+0 and clip_top+2 -- so the selector cannot be a function of the triangle alone either. Four cells, one interaction term, one data point: any rule reproducing it is fitted to that cell. The measurement that would refute this needs new geometry and not more fitting: one triangle translated in y across the 4-grid phase with the clip held fixed, or a quad at clip_top>0 whose BOTH triangles are truncated at the top.\n\nWHAT I AM LEAST CERTAIN OF. The gate is \"the window clip did not cut the triangle\", and the corpus CANNOT separate it from \"the triangle's topmost vertex lies inside the window clip\", because no triangle in the suite is partially clipped while having its top vertex inside. The two differ for real guests: a game that sets no window clip gets region 0 covering the whole surface, and then a triangle running off the top of the screen is still \"cut\" under my gate (clip.y = 0 clips it) but would be \"top vertex outside\" under the other reading only if the vertex is off-surface. LargeZ would discriminate -- its top vertex is at x=0 against a clip at 150 -- and it cannot, because its offset is 136 at every anchor. So the gate's exact form is inferred from the one thing the corpus does distinguish (unclipped small triangles versus clipped quads) and is registered as a candidate. This is the AGENTS.md case where an inference can be valid and still wrong because the model under it was never measured, and it is why the fix is confined to the ROW, which is the half measured exactly on 24 triangles.",
  "a_ref": "a00910d346",
  "b_ref": "320d4dda03",
  "must_not_move": [
    "W_buffering/ZBuf*",
    "W_buffering/WBuf*_ZS0*",
    "W_buffering/WBuf*_TriV_*",
    "W_buffering/WBuf*_FloorQuad_*",
    "W_buffering/WBuf*_RoofQuad_*",
    "W_buffering/WBuf*_WallQuad_*",
    "W_buffering/WBuf*_LargeZ_*",
    "W_buffering/WBuf*_LineStrip_*",
    "W_buffering/WBuf*_ClipW-*",
    "W_buffering/WBuf*_ClipF-*",
    "W_buffering/WBuf16*_TriH_*",
    "Depth_Clamp/*"
  ],
  "expect": {
    "W_buffering/WBuf24D_TriV_V1_ZB0_ZS1_ZB": 26400,
    "W_buffering/WBuf24D_TriV_V1_ZB1_ZS1_ZB": 26400,
    "W_buffering/WBuf24F_TriV_V1_ZB0_ZS1_ZB": 26400,
    "W_buffering/WBuf24F_TriV_V1_ZB1_ZS1_ZB": 26400,
    "W_buffering/WBuf24D_ClipF-150-032_V1_ZB0_ZS1_ZB": 206009,
    "W_buffering/WBuf24D_ClipF-150-128_V1_ZB0_ZS1_ZB": 159250,
    "W_buffering/WBuf24D_ClipF-150-224_V1_ZB0_ZS1_ZB": 112210,
    "W_buffering/WBuf24D_WallQuad_V1_ZB0_ZS1_ZB": 263,
    "W_buffering/WBuf24D_ClipW-159-000_V1_ZB0_ZS1_ZB": 1703,
    "W_buffering/WBuf24D_ClipW-261-000_V1_ZB0_ZS1_ZB": 12941,
    "W_buffering/WBuf24D_ClipW-363-000_V1_ZB0_ZS1_ZB": 7110
  },
  "expect_counts": {},
  "notes": [
    "a_ref/b_ref name the lane worktree's shas (branch wt/psh-31-10). Re-point them to the integration shas actually built at fold-in time, BEFORE the request is queued and therefore before the content hash is taken, exactly as ab_compare requires. Nothing between a00910d346 and the tip touches the emitter.",
    "THE DISC MUST CARRY 'W buffering' AND 'Depth Clamp' AND NOTHING ELSE, and both arms must run that same disc. Every must_not_move glob here matches a capture on it; a prediction naming captures from two discs cannot pass on any single arm, and an unmatched glob is an ab_compare failure by design. Depth Clamp is not decoration: it is the only other suite in the corpus that enables Z_PERSPECTIVE, it never writes POLYGON_OFFSET_SCALE_FACTOR, and it therefore tests whether the sticky ZOFFSETFACTOR carries the new anchor anywhere outside W buffering.",
    "DELIBERATELY UNCONSTRAINED: the four TriH ZS1 COLOUR captures (WBuf24{D,F}_TriH_V1_ZB{0,1}_ZS1, no _ZB suffix) will move and their direction is not predicted. WBufTests::Test reads the depth buffer it just wrote and pb_print's it into the colour frame, so a correct depth change necessarily changes those glyphs; and all of them carry status label-differs against the goldens, which AGENTS.md says to treat as void rather than as a defect. They are absent from must_not_move and from expect for that reason. No expect_counts is registered either, for the same reason: 'worse = 0' cannot be asserted over a void capture whose text this fix is supposed to change. ab_compare will report them under 'the prediction allowed for them; the regressions are still real', which is the correct outcome and not a pass being bought.",
    "The TriH ZS1 DEPTH captures are the only rows this prediction expects to improve, and their value is registered as a prose band [0, 1400] rather than as an expect value, because the off-by-one remainder depends on one float32 ULP of the offset in the driver. The machine-checked half of this registration is therefore entirely GUARDS: twelve must_not_move globs and eleven exact values on classes the mechanism must NOT reach."
  ]
}

{
  "registered_utc": "2026-09-20T07:42:50Z",
  "who": "lane.remote",
  "issue": "60",
  "prediction": "#60 READ SIDE, GL ONLY.  X1A7R8G8B8_{Z,O} stores seven bits of real alpha\nunder a pad bit that reads back as 0 (_Z) or 1 (_O), and the texture unit\nhands the shader (X << 7) | (stored >> 1).  A per-stage uniform applies it at\nthe sample, keyed on the SAMPLED surface's drawn format.  The write side\n(R1, Ad8 = expand7(stored >> 1)) is deliberately NOT in this change and is\nwhat the residual below is.\n\nWHAT MOVES, AND WHY NOTHING ELSE CAN.  The transform is gated on\npgraph_glsl_surface_sampled_pad_alpha_mode(), which answers X1A7R8G8B8_Z and\n_O and NONE for every other format, and on\npgraph_gl_check_surface_to_texture_compatibility() being true for that stage.\nTen of the disc's 236 captures name A7; the other 226 are in must_not_move,\nwhich also catches the second-order risk that adding a uniform to every\ngenerated shader perturbs something unrelated.\n\nTHE MODEL COVERS HALF THE SWATCHES AND THAT IS NOW CHECKED RATHER THAN\nASSUMED.  TestDstAlpha draws EIGHT swatches in two rows -- kBackgroundAlphas\nis {0x00,0x40,0x80,0xFF}000000 then the same four alphas over RED -- and\nx1a7_forward_model.py models the first row only.  The second row must be\nidentical because the blend is {sfactor, ZERO}, so the background RGB is\nmultiplied by zero and cannot reach the surface.  On the goldens the two rows\nare byte-identical on all four captures, 0 of 65,536 px each, so the model's\n32 halves describe all 64.  The earlier note that \"the four captures go to 0,\nwhich is 303,104 px\" rested on this without saying so.\n\nR2 CAN ONLY MOVE TOP HALVES.  The bottom half of every swatch is drawn with\nSetFinalCombiner1Just(SRC_ZERO, true, true) -- alpha forced opaque -- so the\nsampled alpha does not enter it at all.  Sixteen top halves per capture pair\nare in scope; sixteen bottom halves are not, and the four bottom halves at\nbg 0x80 (two rows x two DstAlpha captures) are R1's, off by exactly one\n(127 vs 126, 128 vs 129).\n\nPREDICTED, per capture, as differing px against the golden:\n\n  1-DstAlpha_XA_Z1A7RGB8   65,536 -> 16,384   (2 halves: bg 0x80 bot, x2 rows)\n  DstAlpha_XA_O1A7RGB8     90,112 -> 32,768   (those 2, plus the first-swatch\n                                               anomaly's top and bottom)\n  1-DstAlpha_XA_O1A7RGB8   81,920 -> 16,384 OR 32,768\n  DstAlpha_XA_Z1A7RGB8     65,536 -> 16,384 OR 32,768\n\nThe first two are in expect because every one of their top halves lands on\nthe golden under floor, round AND ceil of the emulator's stored alpha.  The\nother two have one ambiguous top half per row (bg 0x40 on the first, bg 0x80\non the second) whose predicted value straddles the golden by one, and the two\nrows move together, so each reads 16,384 or 32,768 and nothing else.  THE\nAMBIGUITY IS NOT THE RULE'S, IT IS OURS: no rounding of\nstored = f(0x22 * surface/255) reproduces all 32 of today's top halves --\nceil misses 4, floor and round miss 8 -- so the emulator's own write-side\narithmetic is not pinned to a step, which is precisely what R1 will settle.\nA third value on either of those two captures falsifies this file.\n\nFOUR-CAPTURE TOTAL: 303,104 -> between 81,920 and 114,688.  FAILS IF above\n114,688, or if any of the four exceeds 32,768.\n\nTHE OTHER SIX A7 CAPTURES.  Surface_format/Fmt_X1A7R8G8B8_{Z,O} (16,414 and\n32,774 on GL today) samples the surface it drew, and it is the suite the\n2026-09-12 derivation used, where truncation scored 2,097 differing of 32,755\nagainst 32,581 for no pad handling at all.  PREDICTED: both strictly improve.\nFAILS IF either grows.  The four XA_*_Add_SrcA_* captures (9,547, 9,547,\n43,328, 43,328) are NOT predicted in either direction: the Add_SrcA_DstA pair\nis where the known approximation bites -- hardware quantises after the blend\nand fixed-function cannot -- and nothing here models them.  They are named so\nthat a move is a result rather than a surprise.\n\nTHE UNCOMFORTABLE OUTCOME, named in advance.  If the four DstAlpha captures\nland at their floors but a must_not_move capture also moves, the uniform\nreached a path this model says it cannot, and the model is wrong even though\nthe headline improved.  If Surface_format improves and the DstAlpha captures\ndo not, the transform is live but the surface-to-texture gate is answering\ndifferently in the two suites.\n\nNOT PREDICTED: Vulkan, which is unchanged by construction -- its\nsurface-to-texture decision lives in vk/texture.c, another lane's file, so it\nstages NONE and keeps today's 449,850 px.  GL and Vulkan are byte-identical\non nine of these ten captures today; after this change they will not be, and\nthat is the cost of splitting the fix by backend rather than an accident.",
  "a_ref": "01047cf3",
  "b_ref": "9e9da01d",
  "must_not_move": [
    "Blend_tests/*",
    "Texture_format/*",
    "Color_zeta_overlap/*",
    "Image_blit/*",
    "Texture_render_target/*"
  ],
  "expect": {
    "Blend_surface/1-DstAlpha_XA_Z1A7RGB8": 16384,
    "Blend_surface/DstAlpha_XA_O1A7RGB8": 32768
  },
  "expect_counts": {},
  "must_not_regress": [],
  "disc": {
    "suites": [
      "Blend_surface",
      "Blend_tests",
      "Color_zeta_overlap",
      "Image_blit",
      "Surface_format",
      "Texture_format",
      "Texture_render_target"
    ],
    "skip_tests": [],
    "only_tests": [],
    "source": "stated on the --register command line"
  }
}

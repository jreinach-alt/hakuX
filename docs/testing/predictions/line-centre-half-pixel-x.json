{
  "registered_utc": "2026-09-13T04:41:06Z",
  "who": "line-centre-agent",
  "issue": "13",
  "prediction": "#13, the line centre. Arm A is ea879bd647 (tooling only); arm B is c85391e29d, which adds a +0.5 * surface_scale_factor bias to VkViewport.x on pipelines that rasterise lines, and nothing else. The renderer diff is ~80 lines in vk/draw.c, most of them comment, plus a comment in gl/draw.c.\n\nRE-REGISTERED before either arm was served, and for a reason worth recording rather than hiding. The first registration named b_ref 56ddd764ec, whose gate read `polygon_front_mode == POLY_MODE_LINE || primitive_mode == PRIM_TYPE_LINES`. The first half is wrong alone: the polygon mode only reaches the rasteriser for a triangle topology, and the Line width test sets the fill mode once per test and then draws its sixteen POINTS under it. That build would have biased the points in all 61 captures, and Line_0000.0 -- where the lines are dropped at width 0 and the points are the whole draw -- would have gone from 0 differing pixels to some. The must-not-move list below is what made me look. Nothing in the predictions changed; only the ref, and the two queued requests were pulled from the queue unserved.\n\nTHE FALSIFIER IS A FITTED CENTRE, NOT A PIXEL TOTAL. Run `docs/testing/line_footprint.py --captures <arm> --edge` on both arms. It fits the centre of a wide line from the columns (or rows) each width lights, intersected over widths, on three segments whose geometry is known exactly. Three predictions, all of arm B, all exact:\n\n(1) THE QUAD STRIP'S LEFT EDGE, vertical at screen x = 160.0, row 230. Arm A fits ours to a centre in (159.500, 160.000] -- the vertex -- matching cx=160.0 on 18/18 of the widths 3.0-48.0 and cx=160.5 on 11/18. Arm B must fit ours to (160.000, 160.500], matching cx=160.5 on 18/18 and cx=160.0 on 11/18, which is the golden's own fit. Per width, the lit column run must move one column right at the seven ODD widths and stay put at the eleven even ones:\n  3.000  158..160 -> 159..161\n  5.000  157..161 -> 158..162\n  7.000  156..162 -> 157..163\n  9.000  155..163 -> 156..164\n  11.000 154..164 -> 155..165\n  13.000 153..165 -> 154..166\n  15.000 152..166 -> 153..167\n  4, 6, 8, 10, 12, 14, 16, 24, 32, 40, 48: unchanged, and equal to the golden's.\nFALSIFIER: any of the seven not landing exactly on the golden's run, or any of the eleven moving, means the bias is the wrong magnitude or is not reaching the rasteriser, and the entry point is re-examined before anything else here is believed.\n\n(2) THE FAN'S HUB-TO-v4 EDGE, vertical at screen x = 318.5. This is the one that says the rule is a translate and not a snap, so it is also the one that says whether I implemented a translate. A half-integer x is already a pixel centre; the golden moves it anyway, fitting (318.500, 319.000] on 7/7 widths 3-9. Arm A fits ours to (318.000, 318.500]; arm B must fit ours to (318.500, 319.000], with the run's left boundary moving at the three EVEN widths and holding at the odd ones -- the parity flipped relative to (1), which a constant +0.5 predicts and a snap does not:\n  4.000  lo 316 -> 317\n  6.000  lo 315 -> 316\n  8.000  lo 314 -> 315\n  3, 5, 7, 9: lo unchanged.\nFALSIFIER: if (1) lands and (2) does not, I have implemented a snap somewhere and the model in the commit message is wrong.\n\n(3) THE POLYGON'S CLOSING EDGE, near horizontal through column 399 at y = 279.674. The y half of the rule gets NOTHING: golden and our own output already light exactly the same rows on 16/16 widths, fitted centre (279.500, 280.000] for both. Arm B must be byte-identical to arm A here, still 16/16 against the golden.\nFALSIFIER: any movement in y means the viewport bias has leaked into the other axis and the change is wrong whatever the pixel totals say. A +0.5 in y would move every odd width by a row; a snap to the nearest pixel centre would move width 4 by one. Neither may appear.\n\nWHAT MUST NOT MOVE, and why each is a real test rather than a decoration:\n  Line_width/Line_0000.0 -- the suite's only pixel-exact capture, 0 differing. Width 0 covers no pixel centre so the line draws are dropped; the test's sixteen points and its text are all that survives. The points are drawn while the fill mode is still LINE, so this is precisely the capture that says whether the gate understands that a point topology does not rasterise lines whatever the polygon mode says. If this moves, the gate is wrong -- and on the ref this prediction first named, it would have.\n  Line_width/Fill_* -- 22,000 / 22,506 / 27,478 differing. Filled polygons, POLY_MODE_FILL, so the bias must read zero for them. These sit in the same suite and the same frame as the lines, which is what makes them the sharp test of the gate rather than of the diff's blast radius.\n  3D_primitive/Points*, /Triangles*, /TriStrip*, /TriFan*, /Quads*, /QuadStrip*, /Polygon* -- 112 captures the suite draws with the default fill mode. Same gate, a different suite and a different vertex path.\n\nWHAT MUST MOVE, which is the other half of the same check. The even-width Line_width captures must NOT come out flat. Every line in every capture shifts, not just the vertical edge: the line loop, the triangles' outlines, the quad strip's diagonals, the QUADS at the bottom left. Only the vertical edge's COLUMN COVERAGE is parity-invariant. So a flat even-width count would itself be the evidence that the bias never arrived -- this change has an obvious way to measure nothing, since VK_EXT_extended_dynamic_state3 makes the static pipeline state dead on Turnip, and the reason it is applied through a vkCmdSetViewport is exactly that. I am deliberately not naming expected pixel counts for the movers: I cannot compute them, and a number I cannot compute is not a prediction.\n\nWHAT THIS ARM CANNOT DECIDE. Three quarters of #13's residual -- 261,634 of 342,679 structural pixels -- is edge priority: both renderers ink the pixel and disagree about which of several overlapping wide edges is on top. That class sits on top of this one and cannot be subtracted from it, so the Line_width totals will not fall by anything like three quarters, and a modest improvement is the correct expectation rather than a disappointment. Nor does this arm speak to the sixteen sub-2px widths, which the device's own lineWidthRange[0]=1.0 and lineWidthGranularity=0.5 decide.",
  "a_ref": "ea879bd647",
  "b_ref": "c85391e29d",
  "must_not_move": [
    "Line_width/Line_0000.0",
    "Line_width/Fill_*",
    "3D_primitive/Points*",
    "3D_primitive/Triangles*",
    "3D_primitive/TriStrip*",
    "3D_primitive/TriFan*",
    "3D_primitive/Quads*",
    "3D_primitive/QuadStrip*",
    "3D_primitive/Polygon*"
  ],
  "expect": {},
  "expect_counts": {}
}

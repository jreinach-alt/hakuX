{
  "registered_utc": "2026-09-13T17:24:39Z",
  "who": "blendstack-agent",
  "issue": "52",
  "prediction": "#52 -- THE FULL 784-GOLDEN DEPTH ORACLE, POST-FIX, FOR THE FIRST TIME. Both arms are the SAME ref on the SAME 2025-03-14-based disc (--base-iso /home/justin/hakux-work/iso-2025-03-14.iso, disc_id tags iso:<hash>/Depth_buffer). Do NOT compare any absolute here against a stock-disc column: the stock disc emits 9 of the 49 depth cutoffs per cell and scores 144 of 784 Depth_buffer goldens, so every figure #52 currently carries for the fixed cells rests on that subset.\n\nWHY. Cell 2 (z24 fixed) was closed at 91b0897e01 on the stock 144-capture subset: 72,138 -> 43,690 differing px, and ours == floor(exact) on all 18 of its _ZB captures via docs/testing/depth_exact_floor.py, which computes the test's own geometry in exact integers and never consults a golden. That is a strong result on 9 of 49 cutoffs. The 2025 release emits all 49, the base_iso plumbing now exists, and the run costs 338s, so the claim can be tested at 5.4x the cutoff density. Separately, every capture set on disk for the full oracle (res_136_depth_buffer 2026-09-10, olddepth.tsv 2026-09-12 01:59) PREDATES the cell-2 fix by two days and 19 hours respectively, so no post-fix state of the full oracle has ever been observed.\n\nL1 ABSOLUTE, STRUCTURAL. Each arm scores EXACTLY 784 Depth_buffer captures, with captures_vs_goldens.Depth_buffer.goldens = 784 and partial = false. The stock disc gives 144 and partial = true. FAILS IF the 2025 release does not in fact emit all 49 cutoffs per cell, or --base-iso does not honour this image. Checkable from result.json alone.\n\nL2 ABSOLUTE, GOLDENS-INDEPENDENT, THE MAIN LEG (docs/testing/depth_exact_floor.py <arm>/captures1). On all 98 DepthFmt_z24_C{n,y}_FZn_*_ZB captures -- the full 49 cutoffs, against the 18 the stock disc carries -- ours must equal floor(exact) on EVERY pixel: the oracle-ours column must be 0 on 98 of 98. FAILS IF any cutoff outside the stock nine lands differently, which would mean cell 2's barycentric-normalisation fix is a property of the nine cutoffs it was measured on rather than of the geometry. This leg is registered on the ORACLE and not on a differing-pixel total against the golden, because a pixel can be wrong for our reason and silicon's at once and the total cannot separate them.\n\nL3 ABSOLUTE, ab_compare-checkable. Every DepthFmt_z16_C{n,y}_FZn_*_ZB capture is 0 differing pixels -- all 98, where the stock disc shows 18 of 18 at 0. NOT TAUTOLOGICAL: the reason z16 fixed is exact is a margin argument, worst normalisation error 0.0059 against an integer margin of 0.00625, and that is tight to 6%. 49 cutoffs sample it 5.4x more densely than the 9 it was established on, so a cutoff that crosses would fail this leg and refute the margin table in the same stroke.\n\nL4 ABSOLUTE, MY OWN LOCALISATION, THE ONE MOST LIKELY TO FAIL. Measured today on the stock subset over all 18 z24 float captures: the 12,816 px split into a quad-split family of 11,768 px (|delta| > 1) and a +-1 floor family of 1,048 px, and 11,648 of the quad-split -- 99.0% -- sit in GRID ROW 0, 112 in grid row 1, 8 anywhere else, with the f24 exponent field of our word in 1..7 on 11,760 of 11,768. Predicted to hold across all 49 cutoffs: the quad-split family stays confined to grid rows 0 and 1, at least 99% of its pixels in grid row 0, and its exponent field within 1..7 on at least 99.9% of them. FAILS IF the family appears in grid rows 2-31 at cutoffs the stock disc does not carry, which would refute the near-zero-binade reading and make it a general interpolation defect after all. This is the leg that decides whether #52's last cell is a narrow encoding-range question or a broad one, and it is registered before the cutoffs that could break it are run.\n\nL5 NOT A DEFECT, and excluded from every count above. 85,192 px per z24 float capture are fed a NaN vertex depth by the test itself: CreateGeometry passes fixed_to_float(max_depth - 1), which for a float Z surface is z24_to_float(0xFFFFFE) = bitcast(0x7FFFFF00) -- exponent field all ones, mantissa non-zero. What any renderer writes there is a property of its NaN handling, not of its depth arithmetic, and no oracle can adjudicate it. (The guest's z24_to_float and float_to_z24 are not inverses: float_to_z24 subtracts a 0x3000000 bias the former never adds back, src/pbkit_ext.cpp:24,34.)\n\nL6 GUARD, ab_compare-checked. Same binary, same disc, so no capture may move between the arms: the A/B measures the run-to-run band on a 784-capture disc that has never been run through the dispatcher. FAILS IF the disc is non-deterministic, in which case every absolute above is a one-sample coin flip.\n\nTAUTOLOGY DECLARED. None of L1-L4 is forced true by any change of mine -- psh.c is unmodified in this arm and the ref is the plain tip. L2 and L3 are the strongest because their oracle and their zero are independent of the goldens and of each other.\n\nINSTRUMENT NOTE, so it is on the record before the numbers are in: docs/testing/depth_exact_floor_float.py, added today, has a float-cell oracle whose z24 columns are WRONG -- ours-oracle and gold-oracle come back equal to the digit, which convicts the oracle rather than either renderer -- and it prints a self-check saying so. L4 therefore does NOT use that oracle. L4 is a direct measurement of where the differing pixels are and what their exponent field is, which needs no oracle at all.",
  "a_ref": "49afee8889",
  "b_ref": "49afee8889",
  "must_not_move": [
    "Depth_buffer/*"
  ],
  "expect": {
    "Depth_buffer/DepthFmt_z16_Cn_FZn_*_ZB": 0,
    "Depth_buffer/DepthFmt_z16_Cy_FZn_*_ZB": 0
  },
  "expect_counts": {
    "better": 0,
    "worse": 0
  }
}

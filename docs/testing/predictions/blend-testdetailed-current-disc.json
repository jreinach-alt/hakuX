{
  "registered_utc": "2026-09-13T17:01:24Z",
  "who": "blendstack-agent",
  "issue": "50",
  "prediction": "#50 -- FIRST TestDetailed RUN ON A CURRENT-REVISION SCORABLE DISC. Both arms are the SAME ref on the SAME patched disc (iso:85b525/Blend_tests, /home/justin/nxdk_pgraph_tests_xiso_interactive.iso). This is a baseline-and-determinism arm, not a change test: its absolutes are the thing being registered, and the A/B legs measure the run-to-run band on a 1,673-capture disc that has never been run. Do NOT compare any figure here against a stock-disc or 2025-disc column.\n\nWHY THIS AND NOT THE BENEFIT HALF OF blend-stack-c-aliasing.json. That file's benefit half (our stack C == model C going 1/1,120 -> 1,120/1,120, and 14,628,880 differing px -> 0) is registered against b_ref 771c8eb4f1, and that commit CANNOT EXECUTE on any dispatcher run. Verified at source, not inferred: g_xemu_draw_reorder and g_xemu_draw_merge are `static bool = false` (vk/draw.c:31,32); their only setters are the Android JNI's GetPrefBool(env, activity, \"draw_reorder\", false) and (\"draw_merge\", false) (xemu_android.cpp:949,954); the desktop build calls neither. 771c8eb4f1's flush drains the reorder window and the draw queue, and both are empty in every run. A leg whose own gating forces it FALSE tests nothing -- the mirror of a tautology -- so it is not registered as a device arm, and the device time goes to the measurement nobody has made instead.\n\nL1 GUARD (ab_compare, must_not_move Blend_tests/*). Same binary, same disc, so all 1,673 captures must report identical differing-pixel counts in both arms. NOT TAUTOLOGICAL: these 1,568 TestDetailed captures have never been through the dispatcher, this disc is 16x the largest Blend disc ever run, and #50's own mechanism is an ORDERING hazard -- which is exactly what shows up as run-to-run variance. FAILS IF the disc is non-deterministic on TestDetailed, in which case every absolute below is a one-sample coin flip and no #50 fix may be judged on single runs.\n\nL2 GUARD (ab_compare, expect_counts better=0 worse=0). Same claim at the tally level.\n\nL3 ABSOLUTE, STRUCTURAL. Each arm scores EXACTLY 1,673 Blend_tests captures = 105 #spot_ + 1,568 TestDetailed, with captures_vs_goldens.Blend_tests.goldens = 1673 and partial = false. FAILS IF the one-byte XBE patch does not lift interactive_only_tests_ on this disc; or lifts more than Blend (any other suite's count changes); or the current test binary names TestDetailed captures differently from the goldens, in which case they score as no-golden and the count falls short. This leg is what turns \"28.0% of the oracle is reachable\" from a source argument into a measurement.\n\nL4 ABSOLUTE, OFFLINE READER (blend_stack_c_aliasing.py --aliasing --captures <arm>/captures1 --goldens ~/goldens/results/Blend_tests). Predicted on BOTH arms: our stack C == our stack A bit-exact on AT LEAST 1,560 of 1,568 captures, and the golden control EXACTLY 0 of 1,568. FAILS IF the aliasing is not live at this ref -- i.e. one of the ~700 commits since 771c8eb4f1 closed it, or it was always a property of the 2025 test binary rather than of our renderer. Named world: if #50 is already fixed at the tip, ours-C == ours-A collapses toward the control's 0 and #50 should be CLOSED rather than worked. The >= 1,560 is the only tolerance in this file, and it is here solely because this is a different test binary from the one the 1,567/1,568 was measured on; a figure outside it in EITHER direction is a finding, not a pass.\n\nL5 MECHANISM / PREMISE (logcat). Both arms must print `draw reorder: OFF` and `draw merge: OFF`. FAILS IF a device pref has either ON, in which case the source finding above does not apply to this run and L4's baseline is not the un-flushed one.\n\nL6 ABSOLUTE, DISC-COMPARABILITY PROBE (reported, not gating; covered by L1's glob for the machine verdict). Blend_tests/0_ADD_1 differing = 0 in both arms. On the 2025 disc it is the single bit-exact TestDetailed capture of 1,568 -- sfactor=ZERO, dfactor=ONE writes the destination back unchanged on all four channels, so stack C's region is pure background. FAILS IF the current binary renders that frame differently from the 2025 one (glyphs, background, swatch constants), which would mean the two discs' TestDetailed captures are NOT comparable and every 2025-disc figure on #50 must be re-measured on this disc before reuse.\n\nTWO INSTRUMENT CORRECTIONS ESTABLISHED OFFLINE BEFORE THIS ARM, both against real data:\n(a) The benefit half's target figure 14,628,880 is an RGB-ONLY count. Measured over the same 1,120 unsigned captures: 16,217,792 differing pixels counting RGBA, 14,628,880 counting RGB alone -- 1,588,912 alpha-only pixels discarded. score_sweep's own `differing` column is RGBA and reports 16,217,792, so the registered target is stated in a metric the scorer does not use. This matters beyond bookkeeping: the clinching evidence for the mechanism in this issue is that the recovered texel's ALPHA is 221 rather than clamp(221+51), and the target metric drops that channel. CONFIRMED in the same pass: the residual really is all stack C -- stack A region 0, stack B region 0, stack C region 16,217,792, outside the three blits 0.\n(b) oldblend.tsv's 546 `label-differs` rows are a STALE SCORER ARTEFACT, not void captures. Reproducing score_sweep's own label test per capture gives label_delta = 0 and content_white_delta = 2048, so today's scorer classifies them `white-content`, which is scorable. oldblend.tsv was written 2026-09-12 02:29; the label-band fix (4a6a98dce4, \"scope the label check to the label, so a white palette stops voiding a row\") landed 2026-09-13 06:20, 28 hours later. Directly measured: 0 differing pixels in rows 0-63 across all 1,120 unsigned captures, all four channels. So all 1,568 TestDetailed captures are scorable and #50's evidence base is not 35% void.",
  "a_ref": "49afee8889",
  "b_ref": "49afee8889",
  "must_not_move": [
    "Blend_tests/*"
  ],
  "expect": {},
  "expect_counts": {
    "better": 0,
    "worse": 0
  }
}

{
  "registered_utc": "2026-09-26T14:39:33Z",
  "who": "lane.blinx372d",
  "issue": "372",
  "prediction": "MUST-NOT-MOVE guard for #372's GPU handoff (0f9735049c: an incompatible eviction of a draw-dirty binding whose shelved partner has the same width, height, pitch, swizzle and bytes per pixel is recorded as image to buffer (depth-stencil packed) to buffer (unpacked) to image, with no download or finish; the partner owes the download and is marked dirty with its CPU-access watch live). A is 9540a21f79, master a5b5b628f2 plus the [evict372] counter only (no behaviour change), so both arms print the counter. Prediction: every capture in the five suites is byte-identical between arms. What would move each: Depth_buffer_fixed_function flips Z24 between fixed and float at one address and pitch (D24S8 and D32FS8, handed off through pack then unpack); it moves if pack followed by unpack on the GPU is not the byte stream the VRAM round trip gave, or if a guest readback of the depth buffer misses the download the partner now owes (the clear and no quad, the defect its comment on update_surface_part records). Its Z16 and Z24 flips differ in bytes per pixel and are declined, so they cannot move from this change. Color_zeta_overlap binds colour and zeta at one address (a colour and depth-stencil role swap); it moves if the role swap through the compute buffers differs from the VRAM route or the overlap guard (another active binding over the range declines the handoff) is wrong. Surface_format renders several colour formats at one address (a raw texel-block copy between two host formats of one size); it moves if a reinterpretation through a buffer differs from one through VRAM. Texture_CPU_Update and Texture_render_update_in_place are PR #387's arm: the guest CPU writes and reads surfaces, and they move if a handed-off partner owes a download without a live watch (a CPU read of stale VRAM) or the evicted binding keeps a stale obligation that writes back over newer data. Read B's [evict372] handoffs= in the logcat before reading a pass: zero handoffs across all five suites makes this arm inert, not a pass.",
  "a_ref": "9540a21f79",
  "b_ref": "0f9735049c",
  "must_not_move": [
    "Depth_buffer_fixed_function/*",
    "Color_zeta_overlap/*",
    "Surface_format/*",
    "Texture_CPU_Update/*",
    "Texture_render_update_in_place/*"
  ],
  "expect": {},
  "expect_counts": {},
  "must_not_regress": [],
  "disc": {
    "suites": [
      "Color zeta overlap",
      "Depth buffer fixed function",
      "Surface format",
      "Texture CPU Update",
      "Texture render update in place"
    ],
    "skip_tests": [],
    "only_tests": [],
    "source": "stated on the --register command line"
  }
}

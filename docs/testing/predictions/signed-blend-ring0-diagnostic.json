{
  "registered_utc": "2026-09-13T22:41:15Z",
  "who": "signfold",
  "issue": "43",
  "prediction": "#43 RING 0 DIAGNOSTIC. Instrumentation only, on top of the two-pass fold with\npass 2 emitting (8d2bf50075). No fix attempted, deliberately: ring 0's cause is\nnot yet established and a change made now would be a curve fit.\n\nWHAT RING 0 IS. DrawAlphaStack's outer annulus, 8,280 px per capture (124,200\nacross the 15 cells the region tool aggregates). Its diffuse is 0x7F00CC00 --\nalpha 0x7F = 127, BELOW the sign bit, so the fold must leave it alone, and it\nis this issue's own published control at \"124,200 px bit-exact\". It went from 0\ndiffering to all of it when pass 2 started emitting, while every capture TOTAL\nimproved and no capture regressed from exact. Only a region instrument sees it.\n\nR1 THE DECIDING LEG: staged_high == folds, read from logcat tag `hakuX-lane`,\n   lines matching `[signfold]`.\n     staged_high == 0      -> pass 2's uniform never reaches the GPU, so pass 2\n                              runs with the LOW mask. That predicts exactly\n                              what ring 0 shows: alpha clamp(D + 127 + 127)\n                              saturating to 255 where the golden is 226, the\n                              low half applied TWICE. The fix is then in the\n                              uniform path, not the arithmetic.\n     staged_high == folds  -> the uniform arrived, pass 2 is carrying f2, and\n                              ring 0 is something else entirely.\n     0 < staged_high < folds -> it arrives for some draws and not others, which\n                              matches the fold being bit-exact on\n                              Texture_signed_component and broken here.\n   These count STAGINGS, not pass requests -- different events, and no pixel\n   measurement can separate them.\n   VOID, NOT PASSED, if no [signfold] line appears at all.\n\nR2 emitted == 2*folds and empty == 0 must still hold, as they did at\n   8d2bf50075 (folds=5376 emitted=10752 empty=0). This is the control on R1: if\n   emitted also fell, the two counters are measuring one upstream failure\n   rather than two independent things.\n\nR3 ALL 156 CAPTURES MUST NOT MOVE. A counter that changes a pixel is not a\n   counter, and this is registered rather than assumed -- the campaign has had\n   instrumentation present as a renderer deadlock. The identical leg already\n   PASSED once at 156/156 on the earlier counter arm, so a failure here is the\n   new staging increments, not counters in general.\n   NOTE both arms carry pass 2 emitting, so the signed captures stay at their\n   8d2bf50075 values -- 0 and 0 on the two Texture_signed_component signed\n   captures, 80,007 and 76,897 on the #spot pairs -- NOT at any pre-fix value.\n\nNOT CLAIMED: anything about whether ring 0 is fixed, or about the #spot ink\nresidual (fold alone 54,015 plus 8,280 in neither class), which is a separate\nmechanism and will be described separately rather than folded into #43.\n\nCONTEXT THAT CHANGES WHAT EARLIER ARMS ESTABLISH: `draw reorder: OFF` and\n`draw merge: OFF` in both arms' logcat. The signed rejections in\nclassify_draw_safe() and check_draw_mergeable() are therefore inert on this\ndisc and unexercised by any arm so far -- so ring 0 cannot be attributed to\nthem, that class being empty.",
  "a_ref": "8d2bf50075",
  "b_ref": "63d913eb65",
  "must_not_move": [
    "*"
  ],
  "expect": {},
  "expect_counts": {}
}
